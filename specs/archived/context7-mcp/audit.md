# Audit: context7-mcp

> Seeded by the architect; rows are `PENDING` until the auditor fills in status and
> verification after implementation. Every row cites `intent.md` or `contract.md`.

## Requirements Checklist

| ID | Requirement | Source | Status | Notes |
|---|---|---|---|---|
| R1 | After `init` in a clean repo, every selected tool has a `context7` MCP server at its own native path, so the `mcp__context7__*` and `@context7` tokens already emitted resolve to a real server | intent.md G1, SC1, SC2 | PASS | Auditor ran `runInit` (via `dist/`, freshly rebuilt) over an empty temp dir with all five tools: exactly five new artifacts beyond the prior set — `.mcp.json`, `.cursor/mcp.json`, `.vscode/mcp.json`, `.kiro/settings/mcp.json`, `.codex/config.toml` — each carrying `context7` → `https://mcp.context7.com/mcp` under its own root key. `tests/init.test.ts:818` asserts the same over `src/`. |
| R2 | All five tools ship together, with per-tool facts sourced and dated, and live-install gaps carried as open reservations rather than blocking | intent.md G2, SC1, SC17 | PASS | All five generators declare `mcpConfig` with a dated (2026-09-15) source comment; `cursor.ts` records `R-Cursor`, `codex.ts` records `R-Codex` + `CG-1`/`O4`, `kiro.ts` records `AL-30`. `tests/generators/registry.test.ts:309` is table-driven against contract.md § "Verified per-tool MCP facts", with Copilot's `servers` asserted separately. |
| R3 | No pre-existing MCP server, unrelated Codex setting, or other top-level key is ever removed or clobbered | intent.md G3, SC4, SC5, SC6, SC7 | PASS | Traced `mergeJsonMcpConfig` (`src/mcp.ts:125`): spread-preserving `{...root}` / `{...servers}` keeps every other key and its insertion position; non-parsing / non-object root / non-object `rootKey` all return `skipped` before any content is produced. `mergeTomlMcpConfig` only ever appends after the existing bytes. Confirmed end-to-end by `tests/init.test.ts:903` (two unrelated VS Code servers survive), `:941` (Codex bytes are an exact prefix), `:1019` (JSONC left byte-identical) and by the auditor's own `dist/` run. |
| R4 | A pre-existing MCP config file never makes the whole `init` run refuse; every other path keeps `CLI-5` | intent.md G4, SC8 | PASS | `src/writer.ts:38–39` skips conflict collection for `file.merge` only, after `assertContained` still runs. Auditor reproduced both sides: a repo with all five MCP files pre-seeded completes without `--force`, while a pre-existing `.claude/agents/sdd-architect.md` still raises `CONFLICT` and none of the five MCP paths appears in `details` (`tests/init.test.ts:1087`, `:1110`; `tests/writer.test.ts:143`). |
| R5 | Running `init` twice leaves all five MCP files byte-identical | intent.md G5, SC9 | PASS (behavior and, after the AF-1 fix, automated coverage) | Auditor ran `runInit` twice over one temp dir against the built CLI: all five files byte-identical after run 2 (both with `force:true` on the second call, and with `force:false` where the second run still emitted all five `unchanged` warnings and left every MCP file byte-identical before failing on the 80 unrelated non-MCP paths). The implementation is correct; the intended automated proof (`tests/init.test.ts:972`) was itself mis-written and red at first audit, and has since been corrected and re-verified — AF-1 (resolved), AF-6. |
| R6 | Trust posture: config file only. No `enabledMcpjsonServers`, `autoApprove`, `disabledTools`, or trust designation is written; native approval prompts stay the gate | intent.md G6, SC12 | PASS | Token scan over the real fresh output of all five generators (`tests/mcp.test.ts:478`) plus auditor grep over the five generated artifacts: none of `enabledMcpjsonServers`, `disabledMcpjsonServers`, `autoApprove`, `disabledTools`, `trust_level`, `[projects]`. `.claude/settings.json` is neither read nor written by `src/mcp.ts` and is unchanged in the working tree; it still carries exactly the two hook registrations. |
| R7 | One serializer and one source of facts: all JSON via `json.ts`, all TOML via `toml.ts`, both constants literalled once | intent.md G7, SC14, SC15 | PASS | `src/mcp.ts` builds plain objects and a dotted header string and delegates to `renderJson` / `renderTomlComments` / `renderTomlTable` / `tomlBasicString`; the only `[`-header literal in the codebase is `src/generators/toml.ts:90`. Auditor grep: `'https://mcp.context7.com/mcp'` appears once in `src/` (mcp.ts:32); the quoted string `'context7'` appears once in `src/` (mcp.ts:26) — the pre-existing `mcp__context7__*` / `@context7` tool tokens are different literals and are untouched by this feature. |
| R8 | The now-false "harny does not write MCP configuration" claim is retracted in the same change | intent.md G8, SC16 | PASS | `src/generators/kiro.ts:44–46`: the sentence is gone, `.kiro/settings/mcp.json` is named, and the absence of `autoApprove` is stated. `tests/generators/kiro.test.ts:123` and `:225` assert both the mapping and that the new note reaches a rendered `.kiro/agents/<role>.md` (`CLI-9`). |
| R9 | No credential, credential placeholder, or env-var reference is written; `process.env.CONTEXT7_API_KEY` is never read | intent.md G9, SC11 | PASS | `tests/mcp.test.ts:443` scans real generated output for `Authorization`, `"headers"`, `bearer_token_env_var`, `http_headers`, `env_http_headers`, `"env"`, `${`, `CONTEXT7_API_KEY`; `:468` scans all of `src/`. Auditor grep independently confirms `process.env` appears in `src/mcp.ts` only inside a doc comment. |
| R10 | A sixth generator cannot skip the MCP question — omitting the member is a compile error | intent.md SC13 | PASS | `mcpConfig: McpConfig \| undefined` is declared required (not `?:`) on `Generator` (`src/generators/types.ts:105–112`), the `guidancePath` device. `tests/generators/registry.test.ts` additionally asserts `'mcpConfig' in generator` for every registered generator. `npm run typecheck` clean. |
| R11 | Selecting a subset of tools writes only that subset's MCP files; a skipped tool contributes none | intent.md SC3 | PASS | `buildMcpFiles` iterates the `resolvedGenerators` argument only and `continue`s on `mcpConfig === undefined` (`src/mcp.ts:232–234`); `tests/init.test.ts:881` (claude-code + codex only) and `tests/mcp.test.ts:361` (undefined-`mcpConfig` generator contributes nothing) both pass. |
| R12 | `--dry-run` writes nothing and still lists the MCP paths | intent.md SC10 | PASS | `tests/init.test.ts:1144` asserts all five appear in `result.planned` and none exists on disk; auditor's `dist/` dry-run against an empty dir reproduced it. |
| R13 | Containment holds: every MCP path is relative and inside `targetDir`; no user-home file is written | intent.md SC19 | PASS | All five `mcpConfig.path` values are relative; `assertContained` still runs for merge-marked files (it precedes the new `continue` in `planWrites`). Auditor grep: no `homedir`, `process.env.HOME`, or `~`-prefixed path anywhere in `src/`. `tests/init.test.ts:854` asserts relativeness, containment and the single trailing `\n` for every written MCP artifact. |
| R14 | `CLI-11` still holds with the new module: no runtime import cycles, `src/vocabulary.ts` imports nothing | intent.md SC18 | PASS | `tests/vocabulary.test.ts:121`/`:128` (DFS over the runtime, non-type-only import graph) pass with `src/mcp.ts` present. `src/mcp.ts` imports only the two serializer modules at runtime and `types.ts` type-only; no generator implementation is imported by it. |
| R15 | `templates/mcp/README.md` is behavior-first and tool-neutral (`S7`); packaged count 30 → 31 | intent.md SC20 | PASS | The doc states the mechanism in six numbered behavior clauses before naming any tool, then names each tool as an attributed row in the fact table (`S7`). `tests/packaging.test.ts` asserts thirty-one `templates/**` files including `templates/mcp/README.md`, and passes against real `npm pack` output. |
| R16 | Every declared non-goal is absent from the delivered code: no `harny mcp add`, no new flag/prompt/verb, no arbitrary servers, no auto-trust, no user-home writes, no vendor-CLI shell-out, no stdio transport, no OAuth, no TOML/JSON parser dependency, no doctor entries, no capability or vocabulary change, no Copilot CLI surface | intent.md § Non-Goals | PASS | Checked one by one against `git status`/`git diff`: `src/cli.ts`, `src/prompts.ts`, `src/config.ts`, `src/vocabulary.ts`, `src/doctor.ts`, `src/engine.ts`, `src/feedback.ts`, `src/errors.ts`, `src/templates.ts`, `src/generators/index.ts`, `json.ts`, `markdown-yaml.ts` are all byte-unchanged — so no verb, flag, prompt, config key, vocabulary member, capability mapping change, or doctor entry exists. `context7` is the only server name in the codebase and is not selectable. No `child_process`, `fetch`, `command`/`npx` stdio entry, or OAuth/login path in `src/mcp.ts`. `package.json`/`package-lock.json` unchanged (no parser dependency). Copilot's MCP surface is VS Code's `.vscode/mcp.json`, not a Copilot-CLI file. |
| R17 | The five declared amendments (`TG-1`, `TG-10`, `CLI-1`, `CLI-4`, `CLI-5`) are delivered in their amended form, and every unamended statement in `specs/current/tool-generators.md` and `specs/current/cli-init.md` still holds | intent.md § Constraints; contract.md § Amendments | PASS | `TG-1`: one declarative member added, no new method. `TG-10`: exactly one MCP artifact per resolved generator declaring `mcpConfig` (e2e counts 30→31 single-tool, 80→85 five-tool, 86→91 and 77→82 for the `--skills` scenarios). `CLI-1`: still 13 steps; `buildMcpFiles` joins step 11 (`src/init.ts:261–271`). `CLI-4`: mergers are pure by signature; only `buildMcpFiles` reads, never writes. `CLI-5`: the exemption keys on the per-file `merge` flag only — never a path prefix or tool id — and `--dry-run` still writes nothing. Unamended `TG-3`/`TG-4`/`TG-5`/`TG-6`/`TG-7`/`TG-8`/`TG-9`/`TG-11`/`TG-12`, `CLI-2`/`CLI-3`/`CLI-6`–`CLI-9`/`CLI-11` and `PR-4` all still hold (full suite green except AF-1). Updating `specs/current/**` prose itself remains the documentation role's `harny-sync` archive-mode job, not this feature's. |
| R18 | The six open questions (OQ1–OQ6) are resolved as built or explicitly re-raised, not silently drifted | intent.md § "What is deliberately *not* decided here" | PARTIAL | OQ1 merge-write: built as recommended. OQ2 no API-key affordance: none in v1; manual upgrade documented in `templates/mcp/README.md` § "Using an API key". OQ3 `--force` asymmetry: built as recommended and surfaced in the TOML `unchanged` warning text itself. OQ4 JSONC bail-out: warn-and-skip, never mangle. OQ6 auto-trust: not implemented, no `autoApprove` anywhere. **OQ5 (commit vs. gitignore) is the gap**: `intent.md` asked for "one line in the README either way", and neither `templates/mcp/README.md` nor any delivered artifact says whether these files are meant to be committed or what happens when a repo gitignores `.vscode/`. Not silently *drifted* (nothing contradicts it), but neither answered nor re-raised — see Audit Log AF-2. |

## Contract Compliance

| ID | Contract Item | Status | Verified By |
|---|---|---|---|
| C1 | MC-1 — out-of-the-box wiring: fresh file per resolved generator with `context7` at `CONTEXT7_MCP_URL` under that tool's `rootKey` | PASS | Read `src/mcp.ts:118–130` (`renderFreshJson`) and `:171–177` (`renderFreshToml`); both fresh outputs match `contract.md`'s worked examples byte-for-byte (`tests/mcp.test.ts:64–78` pins the exact bytes, and the auditor's own `dist/` run over an empty dir produced the same five files). |
| C2 | MC-2 — `mcpConfig` is a required `Generator` member; all five values match the verified-facts table exactly | PASS | Read all five generator declarations against the contract table row by row: `.mcp.json`/`mcpServers`/`{type,url}`, `.cursor/mcp.json`/`mcpServers`/`{url}`, `.vscode/mcp.json`/**`servers`**/`{type,url}`, `.kiro/settings/mcp.json`/`mcpServers`/`{url}`, `.codex/config.toml`/`mcp_servers`/`{url}`. Required (non-optional) member; `npm run typecheck` clean. |
| C3 | MC-3 — derived from `resolvedGenerators`; unselected and generator-less tools contribute zero files | PASS | `src/init.ts:267` passes `resolvedGenerators`; `src/mcp.ts:232–234` skips `mcpConfig === undefined`. Subset run (claude-code + codex) writes exactly two MCP files. |
| C4 | MC-4 — nothing else in the file is touched (JSON: every other key and server survives in order; TOML: prior bytes are a prefix) | PASS | Traced the spread-based reconstruction (`src/mcp.ts:155–157`) — an existing `rootKey` keeps its position, an existing sibling server keeps its position and value. TOML path only ever does `prefix + '\n' + table` (`:215–216`). Verified end-to-end on real files in the auditor's own run (Codex's four original lines survived as an exact prefix; VS Code's unrelated server survived). |
| C5 | MC-5 — an existing `context7` entry yields `unchanged`, byte-identical file, exactly one warning | PASS | `src/mcp.ts:150–153`: `alreadyPresent && !force` → `unchanged`, no `GeneratedFile`, one warning naming the path. Observed live: re-running `init` over a scaffolded repo emitted exactly five `unchanged` warnings, one per path, and left all five files byte-identical. |
| C6 | MC-6 — unparseable/non-object input yields `skipped`, nothing written, a warning carrying the exact snippet, and a still-successful run (never a `HarnessError`) | PASS | Three `skipped` returns (`:136`, `:140`, `:146`); `jsonSkippedWarning` embeds the exact `renderJson` snippet to paste. Nothing in `src/mcp.ts` throws. `tests/init.test.ts:1020` proves a JSONC `.vscode/mcp.json` leaves the file byte-identical while the run still writes `.mcp.json` and the role files and returns successfully. |
| C7 | MC-7 — merge-marked paths never enter `WritePlan.conflicts`; every other pre-existing planned path still raises `CONFLICT` (exit 3) | PASS | `src/writer.ts:35–42` — `assertContained` first, then `if (file.merge) continue;`. The exemption keys on the per-file flag only. Both sides asserted in one file (`tests/writer.test.ts:143`, `:166`) and end-to-end in `tests/init.test.ts:1110`; auditor reproduced the CONFLICT-on-80-non-MCP-paths behavior against the built CLI. |
| C8 | MC-8 — idempotent across two consecutive runs | PASS (behavior **and** automated coverage, after the AF-1 fix) | Auditor ran `runInit` twice over one directory via `dist/`: all five files byte-identical, and even the `force:false` second run emitted all five `unchanged` warnings and left every MCP file untouched before raising the unrelated non-MCP `CONFLICT`. The implementation satisfies MC-8. The dedicated test (`tests/init.test.ts:972`) was mis-written and red at first audit; it has since been corrected by the test-writer and re-verified by the auditor, including a mutation probe — Audit Log AF-1 (resolved) and AF-6. |
| C9 | MC-9 — `--force` replaces only the JSON `context7` entry and is a documented no-op on the TOML path | PASS | JSON: `:155` rebuilds only `<rootKey>.context7`. TOML: `mergeTomlMcpConfig` destructures `config, existing` and never reads `force` (`:203`), with the asymmetry stated in-code (`:209–210`) and in the emitted warning. `tests/mcp.test.ts:155` and `:293` cover both; auditor's `--force` re-run left the Codex table unchanged and the four JSON files byte-identical. |
| C10 | MC-10 — no credential token, `${…}` expansion, or `CONTEXT7_API_KEY` anywhere in output or `src/` | PASS | Auditor grep over the five generated artifacts and over `src/**/*.ts`; `tests/mcp.test.ts:443`, `:468`. The only `${` occurrences in `src/mcp.ts` are TypeScript template-literal interpolations in warning strings and the header/regex builders — none reaches generated output. |
| C11 | MC-11 — no trust/approve key in any artifact; `.claude/settings.json` unread and unchanged | PASS | Token scan over real generated output (`tests/mcp.test.ts:478`); `src/mcp.ts` contains no reference to `.claude/settings.json`, and the file is byte-unchanged in the working tree with exactly the two hook registrations (`PostToolUse` accumulator + `Stop` runner) `agent-feedback-controls` put there. |
| C12 | MC-12 — no MCP JSON/TOML syntax literal outside `json.ts` and `toml.ts`; `src/mcp.ts` builds objects and a dotted header string only | PASS | `src/mcp.ts:163` composes `` `${rootKey}.${MCP_SERVER_NAME}` `` and hands it to `renderTomlTable`; the single `'[' + header + ']'` literal lives in `src/generators/toml.ts:90`. Auditor grep: no `[mcp_servers.` literal anywhere in `src/` (`tests/mcp.test.ts:517` asserts the same, exempting only `src/generators/toml.ts`). |
| C13 | MC-13 — `MCP_SERVER_NAME` and `CONTEXT7_MCP_URL` literalled exactly once in `src/`, imported elsewhere | PASS | Auditor grep confirms one occurrence each, both in `src/mcp.ts`; all five generators `import { CONTEXT7_MCP_URL } from '../mcp.js'`, mirroring their existing `FEEDBACK_RUNNER_PATH` import. `tests/mcp.test.ts:538`/`:555` enforce the count and the owning file. |
| C14 | MC-14 — determinism (amended `CLI-4`), containment, single trailing newline; both mergers pure | PASS | Both mergers take `existing: string \| undefined` and reference no `fs`, `Date`, `Math.random` or `process.env` (verified by reading the whole module); `buildMcpFiles` only `readFile`s. Every fresh/append output ends in exactly one `\n` (`renderJson` and `renderTomlKeyValues` both newline-terminate; the append branch normalizes the prefix first). Containment asserted per-file in `planWrites`, plus `tests/init.test.ts:854`. |
| C15 | MC-15 — `DOCS_LOOKUP_NOTE` rewritten, still reaching rendered output via `CapabilityMapping.notes` (`CLI-9`) | PASS | `src/generators/kiro.ts:44–46`; still a `notes` entry, still reaches `.kiro/agents/sdd-architect.md` (`tests/generators/kiro.test.ts:225`). Minor prose nit recorded as AF-5. |
| C16 | MC-16 — a whitespace-only file is treated as absent | PASS | Single shared predicate `isAbsentOrWhitespace` (`src/mcp.ts:75–77`) used by both mergers (`S5` honoured — one place, not two). Covered for both formats (`tests/mcp.test.ts:90`, `:214`). |
| C17 | MC-17 — warnings emitted in generator order, one per non-`written` outcome | PASS | `buildMcpFiles` pushes into one array in `generators` iteration order and `runInit` forwards them in that order (`src/init.ts:269–271`). `tests/mcp.test.ts:361` asserts both files and warnings come back in generator order. |
| C18 | Public API — `src/mcp.ts` exports exactly the declared surface with the declared signatures | PASS | Exported: `MCP_SERVER_NAME`, `CONTEXT7_MCP_URL`, `McpMergeOutcome`, `McpMergeInput`, `McpBuildOptions`, `McpBuildResult`, `mergeJsonMcpConfig`, `mergeTomlMcpConfig`, `buildMcpFiles` — no more, no fewer; every signature matches `contract.md` § "Public API — `src/mcp.ts`" verbatim. The six helpers (predicates, warning builders, renderers, the table detector) are module-private. |
| C19 | Public API — `src/generators/types.ts`: `McpConfigFormat`, `McpConfig`, required `Generator.mcpConfig`, optional `GeneratedFile.merge` | PASS | Diffed against the contract's declared block: field names, types, `readonly` modifiers and the `true \| undefined` choice all match, including the rationale comments the contract asked to be findable in code. |
| C20 | Public API — `src/generators/toml.ts`: `renderTomlTable` with bare-key header validation throwing `HarnessError('TEMPLATE')` | PASS | `src/generators/toml.ts:66` (`BARE_DOTTED_HEADER`, exactly the contract's regex) and `:79–91`; delegates the value lines to the existing `renderTomlKeyValues`. Shape, ordering, and the throw for spaced/quoted/empty headers covered by `tests/generators/toml.test.ts:274–350`. |
| C21 | Public API — `planWrites` signature unchanged, behavior amended only for merge-marked files; `applyWrites`/`assertContained`/`WritePlan` untouched | PASS | The whole `src/writer.ts` diff is five lines: four comment lines and one `if (file.merge) continue;`. No signature, no other function, no type changed. |
| C22 | Public API — `runInit`/`InitOptions`/`InitResult` unchanged; step 11 widened, still 13 steps | PASS | The whole `src/init.ts` diff is the `buildMcpFiles` import plus a 12-line block inside step 11, before the unchanged `// 12. Plan writes.` comment. `InitOptions`/`InitResult` untouched; MCP paths flow through `planned`/`written` like any other artifact. |
| C23 | Verified per-tool MCP facts table matches the five declared `mcpConfig` values and `plan.md:140–146` | PASS | Cross-checked the contract table against the five declarations and against `plan.md`'s independent five-row list: same five files, same root keys (including `servers` for `.vscode/mcp.json`), same project-scope choices. Facts remain documentation-verified only — see Carried reservations. |
| C24 | Error Handling Contract — every row's behavior observed, especially the read-error row (`skipped`, never treated as absent) | PASS | Walked all thirteen rows. The read-error row is implemented as an explicit `ENOENT`-vs-other branch (`src/mcp.ts:240–249`): only `ENOENT` becomes `existing = undefined`; every other error warns with the reason and `continue`s, so the unreadable file is never overwritten with fresh content (`tests/mcp.test.ts:417` covers it with a real chmod-style failure). The `renderTomlTable` header row throws `HarnessError('TEMPLATE')` before any write. |
| C25 | Dependencies — no new runtime or dev dependency; the pinned five are unchanged; no import cycle introduced | PASS | `git diff` shows `package.json` and `package-lock.json` byte-unchanged; the pinned set is still `commander@15.0.0`, `@clack/prompts@1.7.0`, `typescript@7.0.2`, `vitest@4.1.10`, `@types/node@26.1.2`. `tests/vocabulary.test.ts`' DFS cycle check passes with `src/mcp.ts` in the graph. |
| C26 | Integration Points — only the files named in the roadmap's File Change Map changed; every file on the "Not modified" list is byte-unchanged | PARTIAL | Every file on the "Not modified" list is byte-unchanged (verified against `git status --porcelain`: no `src/doctor.ts`, `feedback.ts`, `engine.ts`, `config.ts`, `cli.ts`, `prompts.ts`, `vocabulary.ts`, `errors.ts`, `templates.ts`, `generators/index.ts`, `json.ts`, `markdown-yaml.ts`, no `templates/roles/**`, `skills/**`, `conductor/**`, `hooks/**`, `ci/**`, `doctor/**`, `shared/**`, no `.claude/skills/harny-*`). But three *test* files changed that the File Change Map does not list: `tests/e2e-init.test.ts`, `tests/templates.test.ts`, `tests/canonical-fidelity.test.ts` — Audit Log AF-3. |

## Test Coverage

| ID | Test Description | Status | Test File |
|---|---|---|---|
| T1 | `mergeJsonMcpConfig`: fresh-file output shape and exact bytes | PASS | `tests/mcp.test.ts:81` — pins the exact bytes of `contract.md`'s worked example |
| T2 | `mergeJsonMcpConfig`: additive into a file with unrelated servers and unrelated top-level keys, asserting preserved regions byte-identically | PASS | `tests/mcp.test.ts:101` |
| T3 | `mergeJsonMcpConfig`: `context7` already present, with and without `--force` | PASS | `tests/mcp.test.ts:145`, `:155` |
| T4 | `mergeJsonMcpConfig`: unparseable input, non-object root, non-object root key | PASS | `tests/mcp.test.ts:169`, `:183`, `:191` |
| T5 | `mergeJsonMcpConfig`: whitespace-only treated as absent | PASS | `tests/mcp.test.ts:90` |
| T6 | `mergeTomlMcpConfig`: fresh-file exact bytes including provenance comments | PASS | `tests/mcp.test.ts:205` — matches the contract's worked TOML example byte-for-byte |
| T7 | `mergeTomlMcpConfig`: append onto unrelated settings, original bytes are a prefix | PASS | `tests/mcp.test.ts:225`, `:243` (no-trailing-newline variant), `:258` (never bites into another server's table) |
| T8 | `mergeTomlMcpConfig`: existing table detected in bare, quoted, indented and inner-spaced forms | PASS | `tests/mcp.test.ts:271` |
| T9 | `mergeTomlMcpConfig`: existing table still `unchanged` under `--force`; file not ending in a newline handled | PASS | `tests/mcp.test.ts:293`, `:243` |
| T10 | `buildMcpFiles`: generator-order files and warnings; `mcpConfig === undefined` contributes nothing; non-`ENOENT` read error yields `skipped` | PASS | `tests/mcp.test.ts:361`, `:397`, `:417` |
| T11 | `renderTomlTable`: output shape, and `HarnessError('TEMPLATE')` on a header needing quoting | PASS | `tests/generators/toml.test.ts:274` and following (spaced, quoted and empty headers all covered) |
| T12 | All five `mcpConfig` values asserted against the contract table, with Copilot's `servers` key asserted separately | PASS | `tests/generators/registry.test.ts:309` (table-driven `it.each`), plus the `servers`-is-unique and `'mcpConfig' in generator` assertions |
| T13 | Merge-marked file at an existing path stays out of `conflicts`; non-merge file at an existing path still populates it and still throws | PASS | `tests/writer.test.ts:143` (both sides in one file, as the roadmap asked) |
| T14 | End-to-end from empty: exactly five MCP paths written with the expected root keys and URL | PASS | `tests/init.test.ts:818` |
| T15 | Subset tool selection writes only that subset's MCP files | PASS | `tests/init.test.ts:881` |
| T16 | Pre-seeded `.vscode/mcp.json` with two unrelated servers: both survive, no other key touched | PASS | `tests/init.test.ts:903` |
| T17 | Pre-seeded `.codex/config.toml` with unrelated settings: bytes preserved as prefix, table appended once | PASS | `tests/init.test.ts:941` |
| T18 | Two consecutive runs: all five byte-identical; run 2 emits five `unchanged` warnings | **PASS** (was FAIL; fixed by the test-writer as Audit Log AF-1 option (ii), re-verified by the auditor — suite now 611/611). Load-bearing assertions: the five `unchanged` warnings (proving every merger took the `unchanged` branch, which is `MC-8`'s own mechanism) and the absence of all five MCP paths from the `CONFLICT` details (`MC-7`). Note the byte-identity loop is tautological *in this scenario* — see AF-6. | `tests/init.test.ts:972` |
| T19 | JSONC `.vscode/mcp.json`: nothing written to that path, warning carries the snippet, run still succeeds | PASS | `tests/init.test.ts:1020` |
| T20 | All five MCP files pre-existing: run succeeds without `--force`; a pre-existing role file still raises `CONFLICT` | PASS | `tests/init.test.ts:1087`, `:1110` — and the second asserts none of the five MCP paths appears in the CONFLICT details |
| T21 | `--dry-run`: nothing written to any MCP path; paths still listed | PASS | `tests/init.test.ts:1144` |
| T22 | Absence: no credential token, `${`, or `CONTEXT7_API_KEY` in output or `src/` | PASS | `tests/mcp.test.ts:443`, `:468` |
| T23 | Absence: no trust/approve key in any artifact; `.claude/settings.json` unchanged | PASS (split coverage) | `tests/mcp.test.ts:478` for the artifact scan; the `.claude/settings.json` clause rests on the pre-existing `tests/generators/claude-code.test.ts:316` two-hook assertion plus the file being byte-unchanged in the diff — no new test asserts it directly, which is adequate but worth knowing |
| T24 | Absence: no `[mcp_servers.` literal or hand-built JSON outside the two serializer modules | PASS | `tests/mcp.test.ts:517` |
| T25 | Single-literal check for `'context7'` and the endpoint URL across `src/` | PASS | `tests/mcp.test.ts:538`, `:555` |
| T26 | `DOCS_LOOKUP_NOTE` retraction, and the note still reaching a rendered Kiro role file | PASS | `tests/generators/kiro.test.ts:123`, `:225` |
| T27 | Import-cycle and vocabulary checks still green with `src/mcp.ts` present | PASS (pre-existing generic check, exercised with the new module in the graph) | `tests/vocabulary.test.ts:121`, `:128` |
| T28 | Packaged `templates/**` count is thirty-one | PASS | `tests/packaging.test.ts:102` (real `npm pack --dry-run` output) |
| T29 | Every generated MCP artifact ends in exactly one `\n` and resolves inside `targetDir` | PASS | `tests/init.test.ts:854` |
| T30 | Manual (not automated): built-CLI smoke run against a scratch dir pre-seeded with a realistic `.codex/config.toml` and a `.vscode/mcp.json`, per open reservation `AL-20` | PASS — independently re-performed by the auditor, not accepted on report | manual — executor's run in Audit Log 2026-09-15; auditor's own re-run recorded as AF-1 |
| T31 | **Gap identified by the auditor**: no automated test exercises the amended `TG-10` artifact count *and* a pre-existing merge-owned file in the same run against `dist/`; `tests/e2e-init.test.ts` only ever runs against empty fixtures | MISSING (non-blocking; the manual `AL-20` smoke run is the compensating control, as the roadmap intended) | — |

## Audit Log

| Date | Auditor | Finding | Severity | Resolution |
|---|---|---|---|---|
| 2026-09-15 | executor (pre-audit smoke run, Task 4.23) | Manual smoke run of the **built** CLI (`node bin/harness.js init <dir> --yes --tools claude-code,cursor,kiro,github-copilot,codex`) against a scratch dir pre-seeded with a realistic `.codex/config.toml` (`model`, `approval_policy`, `[sandbox]`) and a `.vscode/mcp.json` holding one unrelated server (`my-other-server`). Result: exit 0, "Wrote 85 file(s)". `.codex/config.toml`'s original four lines survived byte-for-byte as a prefix, followed by a blank line and `[mcp_servers.context7]` / `url = "https://mcp.context7.com/mcp"`. `.vscode/mcp.json` kept `servers.my-other-server` untouched and gained `servers.context7` (`{"type":"http","url":"https://mcp.context7.com/mcp"}`) with `$schema`/key order otherwise undisturbed (file had none, so N/A). Fresh `.mcp.json`, `.cursor/mcp.json`, `.kiro/settings/mcp.json` matched contract.md's worked examples exactly. Re-running without `--force` printed five "left unchanged" warnings (naming each of the five MCP paths) then correctly raised `CONFLICT`/exit 3 over the 80 *non*-MCP paths only — none of the five MCP paths appeared in the conflict list. Re-running with `--force` left the Codex table's md5 unchanged (TOML `--force` asymmetry, `MC-9`) and left the four JSON files' md5 unchanged too (force replaced `context7` with identical content). `--dry-run` against an empty dir listed all five MCP paths and wrote nothing. `grep` across all five generated MCP artifacts for `Authorization`, `bearer_token_env_var`, `autoApprove`, `enabledMcpjsonServers`, `CONTEXT7_API_KEY`, `${` found none. All matches contract.md's Error Handling Contract table row-for-row. | INFO (confirms expected behavior, no defect) | No action needed; see `tasks.md` "Executor's notes" for the one related, independently-discovered test defect (Task 4.12 / T18's `tests/init.test.ts` idempotence test, which asserts a scenario contradicting the unchanged `CLI-5`/`MC-7` this smoke run independently reproduced). Auditor note: this smoke run was **not** taken on faith — it was re-performed independently (AF-1) and its conclusions reproduced. |
| 2026-09-15 | sdd-auditor (AF-1) | **The single red test (`tests/init.test.ts` > "runInit — Context7 MCP wiring: idempotence across two consecutive runs") is a test-authoring defect, not an implementation defect — confirmed independently.** Reproduced the failure (`HarnessError CONFLICT: Refusing to overwrite 80 existing file(s)`), then re-derived the diagnosis from the implementation rather than from the executor's report: the test calls `runInit` twice with `force: false`, and the *first* call writes ~80 non-MCP artifacts, every one of which is a genuine `CLI-5` conflict on the second call. `CLI-5`/`MC-7` are explicitly *unchanged* by this feature's own contract, so the second call is behaving exactly as specified. The auditor then drove `dist/init.js` directly over temp dirs: (a) second run with `force: true` → all five MCP files byte-identical to run 1; (b) second run with `force: false` → all five `unchanged` warnings emitted and all five MCP files still byte-identical *before* the unrelated non-MCP `CONFLICT` is raised. `MC-8`/`SC9` therefore hold in the implementation, corroborated by the independent "all five MCP files pre-existing" test (`tests/init.test.ts:1087`). **Correction to the recommended fix:** `force: true` on the second call alone is necessary but **not sufficient** — the auditor measured that under `--force` the four JSON paths are legitimately re-written with identical content, so they *do* appear in `resultTwo.written` and only the Codex path emits a warning, which falsifies the test's remaining two assertion blocks (`expect(resultTwo.written).not.toContain(...)` for all five, and "a warning naming each of the five"). A correct fix is either: (i) second call `force: true`, keep the byte-identity loop, and narrow the last two blocks to the contract's actual `--force` semantics (`MC-9`: JSON entry replaced with identical bytes, Codex table untouched with one warning); or (ii) keep `force: false` and wrap the second call in an expected-`CONFLICT` assertion, then assert the five `unchanged` warnings and byte-identity — which is the closest match to `MC-8`'s literal wording ("run 2 finds the entry present and takes the `unchanged` branch on all five"). The executor was right to leave the test red and report it rather than silently rewriting it. | HIGH (blocks a green suite / green CI-able merge; **not** an implementation defect, and `MC-8`/`SC9` are proven to hold) | **RESOLVED 2026-09-15** by the test-writer (the owner of `tests/init.test.ts`), using option (ii); no `src/` change was made, as required. Re-verified by the auditor — see AF-6. |
| 2026-09-15 | sdd-auditor (AF-2) | **OQ5 was neither answered nor re-raised in any delivered artifact.** `intent.md` OQ5 ("Should these files be gitignored or committed? … Worth one line in the README either way; flagging rather than deciding") and `roadmap.md`'s own risk row ("Users gitignore `.vscode/`, so the Copilot wiring never reaches teammates — Mitigation: documentation only") both point at a documentation line that `templates/mcp/README.md` does not contain: grep finds no mention of committing, `.gitignore`, version control, or teammates. Every other open question (OQ1–OQ4, OQ6) is resolved in the delivered code exactly as approved at the spec gate. | MEDIUM (documentation gap on an explicitly-flagged open question; no code impact) | Add one sentence to `templates/mcp/README.md` (these five files are repo-scoped configuration and are meant to be committed; a repo that gitignores `.vscode/` will not propagate the Copilot wiring to teammates, who can re-run `init` or add the entry themselves). Non-blocking; can travel with the documentation role's pass. |
| 2026-09-15 | sdd-auditor (AF-3) | **Three test files were modified that `roadmap.md`'s File Change Map does not list**: `tests/e2e-init.test.ts`, `tests/templates.test.ts`, `tests/canonical-fidelity.test.ts`. The auditor read all three diffs in full and independently confirms the executor's characterization: they contain no new behavioral assertion and no weakened one — only (a) hard-coded artifact counts that this feature's own contracted `TG-10` amendment mechanically falsifies (30→31 per single-tool scenario, 80→85, 86→91, 77→82, tool artifacts 36→41), (b) the `templates/` root listing growing by `mcp` (the contracted `CLI-10` 30→31), and (c) one more entry in `canonical-fidelity`'s `isContractedEntry` allowlist, `templates/mcp/`, which admits exactly the one new canonical file and nothing else. Each carries a `Spec:`/`Covers:` header amendment in the identical style five prior features used on these same three files. This is consequential test maintenance, not scope creep, and it hides no defect. No file on the roadmap's "Not modified (asserted, not assumed)" list was touched. | LOW (accurate change, inaccurate map) | Amend `roadmap.md`'s File Change Map after the fact to list the three files as **MODIFY — consequential `TG-10`/`CLI-10` count amendment**, per this repo's practice of keeping the map accurate. Recommended, not blocking. |
| 2026-09-15 | sdd-auditor (AF-4) | **`harny-feedback` verification.** The per-turn hook is registered in `.claude/settings.json` exactly as `agent-feedback-controls` shipped it (PostToolUse `Edit\|Write` accumulator + Stop runner over `.sdd/feedback/run-feedback.mjs`) and shows evidence of having fired during implementation: `.sdd/feedback/.turns/` has an mtime of 16:30:50 today — after the last source edit (16:22) and after `tasks.md` was finalized (16:28) — i.e. a turn file was created and then consumed-and-deleted by the Stop runner, which is the only process that touches that directory. Its mapped commands resolve to `tsc --noEmit` (gate file `tsconfig.json` present) and `eslint` (probe-skipped — this repo has no eslint config), matching the last CI run's `harny-feedback: 1 of 2 command(s) ran, 1 skipped.` — **not** an `N = 0` probe-skip-everything gap. Findings were heeded: the executor reported typecheck clean and the auditor confirmed `npm run build` and `npm run typecheck` both clean (the one permitted re-run, to verify the claim). **Gap**: `.github/workflows/harny-feedback.yml` is present and its latest run is green, but that run is `34853159815` from 2026-09-14 against the `ship/agent-feedback-controls` PR — the workflow triggers on `pull_request` only, and this feature's work is still uncommitted on `main`, so no CI run has yet exercised it. | LOW (expected pre-PR state; the one command CI would actually run, `tsc --noEmit`, was verified clean locally) | Open the PR and confirm `harny feedback` is green on it before merge. Note the CI gate runs lint/typecheck, not `npm test`, so it would **not** catch AF-1 — AF-1 must be fixed on its own evidence. |
| 2026-09-15 | sdd-auditor (AF-5) | **`harny-standards` S1–S7 check.** S1 (TS/ESM, `.js` specifiers, `node:` prefixes) — pass. S2 (`HarnessError` only; MCP bail-outs are warnings, never errors; no new error code) — pass. S3 (determinism, containment, single trailing `\n`) — pass. S4 (no new dependency) — pass, manifest and lockfile byte-unchanged. S5 (shared constants imported, never re-literalled; warning strings and the whitespace predicate written once) — pass. S6 (vitest, `tests/` mirrors `src/`, `Spec:`/`Covers:` headers on every touched test file, offline default) — **one deviation**: `tests/writer.test.ts:143`'s describe title reads "…exempt from conflict collection (guarantee MC-7)", putting a contract id in a test *name*, which `S6` forbids and `tasks.md`'s own note to the test writer restated. Widespread pre-existing practice in this repo (a dozen older describes do the same), and this feature's other new tests all avoid it. S7 (tool-neutral content names the behavior first, tools as attributed examples) — pass for `templates/mcp/README.md`. Separate LOW prose nit: `kiro.ts`'s new `DOCS_LOOKUP_NOTE` reads "harny writes its default configuration to .kiro/settings/mcp.json, **so** every tool call still prompts…", where the causal "so" does not follow (the prompting follows from the *absence* of `autoApprove`, which the clause then states correctly). `MC-15`'s substantive requirements are all met. | LOW (both items) | **Partially resolved 2026-09-15**: the test-writer removed "(guarantee MC-7)" from the `tests/writer.test.ts` describe title (auditor re-read the diff: the title is now `planWrites — merge-marked paths are exempt from conflict collection`, and no other new test name in this feature carries a contract id — `S6` is now clean for everything this feature touched). The `kiro.ts` "so"/"and" prose nit is **still open** and was correctly declined as out of scope by the test-writer: it is a `src/` string, owned by `harny-implement`. Auditor's call: it does not warrant another implementation round on its own — fold it into the documentation role's pass, or leave it; `MC-15`'s substantive requirements are met either way. |
| 2026-09-15 | sdd-auditor (AF-6) | **Re-verification of the AF-1 fix — verified independently, not accepted on report.** (a) Read the new `tests/init.test.ts:972` in full: it is exactly AF-1 option (ii) — both `runInit` calls keep `force: false`, the second is wrapped in try/catch asserting `isHarnessError` + `code === 'CONFLICT'` and that none of the five MCP paths appears in `err.details`, then all five files are read off disk and compared byte-for-byte against the post-run-1 snapshot, then the `collectingIO()` warnings from the second call are asserted to name all five paths. (b) Re-ran the full suite myself: **30 files / 611 tests, all passing**; `npm run build` and `npm run typecheck` clean. (c) Confirmed the test is not merely "not crashing" by **mutation probe**: I temporarily disabled both `unchanged`-detection branches in `src/mcp.ts` (the `alreadyPresent && !force` guard and the `hasExistingTomlTable` guard) and re-ran the test — it **failed**, at `AssertionError: no warning named .mcp.json`. `src/mcp.ts` was then restored from a pre-mutation backup and confirmed byte-identical by md5 (`761c7e9727ffdb46c530a0031d093107`), with the working tree back to its exact prior state; the full suite is green again at 611/611. (d) One precision note for the record: in this scenario the *byte-identity loop is tautological*, because `applyWrites` throws `CONFLICT` before writing any file at all, so run 2 writes nothing by construction. The assertions actually carrying `MC-8` here are the five `unchanged` warnings (which prove every merger took the `unchanged` branch — `MC-8`'s own stated mechanism) and the `CONFLICT`-details exclusion (`MC-7`). The complementary case where writes *do* occur on run 2 (`--force`, where a byte difference could actually manifest) is covered by the auditor's own `dist/`-level double run recorded in AF-1 and by the unit-level `--force` tests (`tests/mcp.test.ts:155`, `:293`), not by this test. | INFO (fix confirmed effective and mutation-sensitive) / LOW (the tautological byte-identity loop) | No action required. Optional hardening, if this test is ever revisited: add a third `runInit` with `force: true` and keep the byte-identity loop on *that* result, so the byte-level clause of `MC-8` is asserted in the one scenario where writes actually happen. |

## Final Verdict

**Status**: APPROVED WITH RESERVATIONS

**Summary**: The implementation is a faithful, complete delivery of the contract — all
seventeen behavior guarantees (`MC-1`–`MC-17`), all twenty success criteria, all five
declared amendments and every declared non-goal check out against the code. The one
failing test (`AF-1`) was a defect in the test, never in the shipped behavior, and has
since been fixed by the test-writer and re-verified by the auditor (including a mutation
probe): **the suite is now 611/611, with `npm run build` and `npm run typecheck` clean.**
**No Critical Issue remains open.** The verdict stays `APPROVED WITH RESERVATIONS`
because of the documentation gap on `OQ5` (`AF-2`), the fact that CI has not yet run
against this change (`AF-4`), and the four standing carried reservations — none of which
is a defect in the delivered code.

**Critical Issues** (must fix before merge): **none open.**
- ~~**`AF-1` — the red test must be fixed (in `tests/init.test.ts`, not in `src/`).**~~
  **RESOLVED 2026-09-15.** The test-writer rewrote `tests/init.test.ts:972` as AF-1
  option (ii): both calls keep `force: false`, the second is wrapped in an
  expected-`CONFLICT` assertion that also proves none of the five MCP paths is in
  `err.details`, and the five `unchanged` warnings plus on-disk byte-identity are
  asserted independently of the throw. No `src/` file was touched, which is the correct
  outcome — the implementation was never at fault. The auditor re-read the test, re-ran
  the full suite (611/611), and confirmed the test is mutation-sensitive by temporarily
  disabling both `unchanged`-detection branches in `src/mcp.ts` (the test failed, as it
  should) before restoring the file byte-identically. See Audit Log `AF-6`, which also
  records one precision caveat: the byte-identity loop is tautological in this aborted-run
  scenario, so `MC-8` rests here on the five `unchanged` warnings and the `MC-7` exclusion
  — the write-path case is covered by the `--force` unit tests and the auditor's own
  `dist/` double run.

**Warnings** (should fix, not blocking):
- **`AF-2`** — `OQ5` (commit vs. gitignore for the five co-owned files) is answered
  nowhere in the delivered artifacts, though `intent.md` and `roadmap.md` both call for
  one README line. MEDIUM.
- **`AF-4`** — no CI run has yet exercised this change (the workflow triggers on
  `pull_request`; the work is uncommitted on `main`). The latest run is green and
  meaningful (`1 of 2 command(s) ran, 1 skipped` — not an `N = 0` probe-skip), and the
  one command it runs, `tsc --noEmit`, was verified clean locally. Confirm green on the
  PR. Note the CI gate runs lint/typecheck only, never `npm test`, so a green `harny
  feedback` run is not by itself evidence that the test suite is green — the auditor's
  own 611/611 run is that evidence.

**Recommendations** (nice to have):
- **`AF-3`** — amend `roadmap.md`'s File Change Map to list `tests/e2e-init.test.ts`,
  `tests/templates.test.ts` and `tests/canonical-fidelity.test.ts` as **MODIFY —
  consequential `TG-10`/`CLI-10` count amendment**. The auditor read all three diffs and
  confirms they are count/list maintenance only, in the documented house style, hiding
  no defect; the map is simply now inaccurate.
- **`AF-5`** — half done: the `tests/writer.test.ts` describe title no longer carries
  "(guarantee MC-7)" (`S6` is now clean across everything this feature touched). Still
  open, and the only `src/`-side item of any kind left: `kiro.ts`'s new
  `DOCS_LOOKUP_NOTE` says "…writes its default configuration to `.kiro/settings/mcp.json`,
  **so** every tool call still prompts…", where the causal connective does not follow.
  LOW, cosmetic, `MC-15`'s substantive requirements are met; fold into the documentation
  pass or leave — it does not justify another implementation round on its own.
- **`T31`** — consider an e2e scenario that runs the built CLI against a fixture with a
  pre-existing merge-owned file, so the `AL-20` `dist/`-vs-`src/` gap on the merge path
  has automated coverage rather than a manual smoke run.
- Downstream (documentation role, post-audit, already scoped): `README.md` § "Generated
  files per `init` run", `AGENTS.md`, `CHANGELOG.md`, `plan.md:138`'s future-scope
  paragraph (first clause now discharged), and `harny-sync` archive-mode propagation of
  the `TG-1`/`TG-10`/`CLI-1`/`CLI-4`/`CLI-5`/`CLI-10` amendments into
  `specs/current/tool-generators.md` and `specs/current/cli-init.md`.

**Carried reservations** (expected at ship time; confirm each is still accurate and
record it for `harny-sync` archive mode):

- `R-Cursor` — the `.cursor/mcp.json` facts were confirmed from Context7's first-party
  client documentation and corroborated by `plan.md:143`, but Cursor's own MCP
  documentation was unreachable during spec exploration, and one low-confidence
  secondary claim reports MCP support behind a settings toggle defaulting off in some
  installs. `AL-30`-class: correct file, possibly inert tool.
- `R-Codex` — open upstream issue `openai/codex#13025`: Codex Desktop reportedly ignores
  project `.codex/config.toml` MCP servers and loads only the global file. The CLI
  surface is expected to work; the Desktop surface may not. Writing the global file is
  barred by containment (`SC19`), so the mitigation is documentation only.
- `AL-30` and `CG-1`/`O4` (standing) — none of the five per-tool MCP facts has been
  verified by loading a generated config into a live install of the tool. This feature
  joins those reservations rather than resolving them.
- `AL-20` (standing) — `tests/e2e-init.test.ts` validates `dist/`, not `src/`, so a
  `src/`-only regression can pass `npm test` against a stale build. Task 4.23's manual
  smoke run on the built CLI is the compensating control for this feature.

**Auditor confirmation (2026-09-15).** All four reservations above were re-checked and
are still accurate as written. `R-Cursor` and `R-Codex` are recorded in code
(`src/generators/cursor.ts`, `src/generators/codex.ts` source comments) *and* in
`templates/mcp/README.md` § "Known gaps" with their manual workarounds, as
`contract.md` requires; none of the five per-tool facts has been verified against a
live tool install, so this feature joins `AL-30` and `CG-1`/`O4` rather than resolving
them. For `AL-20`, the auditor re-performed the built-CLI check independently rather
than relying on the executor's Task 4.23 record (see Audit Log `AF-1`), after
rebuilding `dist/` from the current `src/`.
