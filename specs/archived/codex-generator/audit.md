# Audit: codex-generator

> **Status: audited 2026-09-02 by `sdd-auditor`.** Every row below traces to
> `intent.md` (goals `G1`–`G10`, success criteria `SC1`–`SC12`), `contract.md`
> (guarantees 1–16 and the named interface sections), or `tasks.md`.
>
> **Read § "Carried reservations" first.** It was pre-seeded, deliberately, with the
> checks that cannot be performed in-session. On the previous feature
> (`specs/cursor-kiro-copilot-generators/`) the equivalent gap — **AL-30** — was described
> once in prose and then stayed open and unremarked across *two* audit passes. Seeding it
> as a numbered reservation before the audit started was the corrective, and this pass
> confirms it worked: **O4 is still open and is stated as such in the Final Verdict.**
>
> **Verification-channel disclosure for THIS audit pass (required by the notes at the
> bottom of this file).** Context7 MCP tools were **not** exposed to this auditor's tool
> set, and no WebFetch/WebSearch tool was available either. However, **outbound HTTPS was
> available from the shell**, and it was used: the three first-party URLs cited in
> `contract.md` were fetched and read during this audit (2026-09-02). Six-plus rows of
> § "Verified Codex facts" were therefore independently re-verified against live
> first-party documentation rather than taken on the contract's word — see Audit Log
> **CG-1**. What could **not** be re-verified is the Context7 `/openai/codex` *Rust source*
> corroboration (`agent_roles.rs`, `agent_role_config.rs`, `config.schema.json`,
> `core-skills/src/loader.rs`), because the Context7 channel was unavailable; those rows
> are marked accordingly. And the live-install check (O4) remains un-performable here.

## Requirements Checklist

| ID | Requirement | Source | Status | Notes |
|---|---|---|---|---|
| R1 | Ship the Codex CLI generator: `src/generators/codex.ts` exporting `codexGenerator`, one `.toml` per enabled role under `.codex/agents/` | intent.md G1, SC1–SC2 | **PASS** | `src/generators/codex.ts` exports `codexGenerator` (and `codexSandboxMode`). A live `node bin/harness.js init <dir> --yes --tools codex` wrote exactly the 5 `.codex/agents/sdd-*.toml` + 1 `SKILL.md` + 6 shared files and exited 0 |
| R2 | The TOML wrapper is a shared sibling module (`src/generators/toml.ts`), not inline in the adapter | intent.md G2, SC8 | **PASS** | All six helpers live in `toml.ts`; `codex.ts` contains no `'''`, no `"""`, no hand-written `key = value`, no `#` serializer. Enforced by a source-scan test, re-verified by reading the file |
| R3 | Every pinned Codex fact carries a first-party source and the date 2026-08-30; `plan.md` is not an accepted source for any of them | intent.md G3, SC5 | **PASS** (1 correction, see CG-2) | Every row of § "Verified Codex facts" carries a Source cell and the section header dates all rows 2026-08-30. This auditor re-fetched all three first-party URLs on 2026-09-02 and independently confirmed ≥ 6 rows (CG-1). One row is now known to be incomplete — see CG-2 (the skills `description` budget). One row's Source cell is `—`, which is correct for a "none found" finding but is exactly the row CG-2 corrects |
| R4 | Canonical fidelity survives embedding in a TOML string: body byte-for-byte **and contiguous** in the emitted file | intent.md G4, SC4 | **PASS** | Verified three ways: the repo's own extended oracle in `tests/canonical-fidelity.test.ts`; and independently by this auditor with an out-of-repo Python script that re-slices each raw template with `open()` and confirms the slice is a contiguous substring appearing **exactly once** in the generated `.toml`, and is the exact prefix of the value decoded by Python's real `tomllib`. All 5 roles + the conductor |
| R5 | A conductor artifact is emitted unconditionally, in a mechanism that can run in the main conversation and pause for a human | intent.md G5, SC7 | **PASS** | `renderConductor` returns `.agents/skills/sdd-conductor/SKILL.md` (Markdown+YAML, not TOML) on every run; frontmatter is `name`/`description` only; both `harny note:` caveats and the `harny:begin`/`harny:end` project-config block are present in the artifact written by the live run |
| R6 | Nothing silently dropped: every capability reaches the artifact as a note, including the auditor's `audit.md only` scope and unknown tokens | intent.md G6, SC6 | **PASS** | The live-generated `sdd-auditor.toml` header carries three `# capability note:` advisory lines plus `# capability note: write-files is scoped to audit.md only`, matching contract § "Illustrative output" line for line. `unmapped capability: ask-human` is asserted for a synthetic role |
| R7 | A stated verdict on whether `src/generators/types.ts` is sufficient for a TOML target | intent.md G7, SC11 | **PASS** | § "Interface sufficiency finding" states the verdict (sufficient, no amendment) and defends it on four axes. The reasoning holds under audit: `GeneratedFile.contents` is opaque, `tokens: []` is an already-exercised path, `codexSandboxMode` is exported outside `Generator` per the `isReadonlyRole` precedent, and the role-TOML/conductor-Markdown asymmetry is genuinely accommodated by `conductorPath` being independent of `agentsDir`. `types.ts` is byte-identical |
| R8 | The "not shipped yet" stub is retired **cleanly**: registry, hint, README hand-off — and the `NO_GENERATOR` / skip-warning coverage is re-pointed, never deleted or weakened | intent.md G8, SC9 | **PASS** (README deferred by design) | **Read most carefully, per `tasks.md`.** All four test files were *re-pointed*, not weakened — see C13 and CG-3 for the line-by-line finding. `README.md:126` and `AGENTS.md:23` still say codex is unshipped; that is the deliberate `sdd-documentation` hand-off recorded in `roadmap.md` § "Deferred", not a miss — but it means **this feature is not user-visibly complete until `sdd-documentation` runs** |
| R9 | Five-of-five tool coverage with no regression: the interface, the shared Markdown layer, the four shipped generators, `templates/**` and every module outside `src/generators/` stay byte-identical | intent.md G9, SC10 | **PASS** | `git status --porcelain` shows exactly: `M src/generators/index.ts`, seven modified test files, and five new files (two in `src/generators/`, three under `tests/`). Nothing else in `src/`, nothing in `templates/`, nothing in `.claude/`, nothing in `package.json` |
| R10 | No new runtime or dev dependency — in particular no TOML package | intent.md G9, SC10 | **PASS** | `package.json` untracked-clean; `dependencies` = `{commander, @clack/prompts}`, `devDependencies` = `{typescript, vitest, @types/node}`, both asserted by name in `tests/packaging.test.ts`. `package-lock.json` also unmodified. Decoding in tests is done by `tests/helpers/toml-decode.ts`, test-only plumbing |
| R11 | `npx harny init --tools codex --yes` exits 0 and writes 6 tool artifacts + 6 shared files, with no skipped-tool warning | intent.md SC2 | **PASS** | Executed by this auditor: exit 0, "Wrote 12 file(s)", the exact 12-path set, no warning on stdout/stderr. Note the *absence of a warning* is not directly asserted by an e2e test — it is structurally impossible once `getGenerator('codex')` is defined (asserted in `registry.test.ts`). Recorded as a LOW recommendation |
| R12 | Generated TOML parses, and its decoded `name` / `description` / `model` / `developer_instructions` equal the contracted values | intent.md SC3 | **PASS** | The suite proves this with the test-local decoder. This auditor additionally parsed all five artifacts with **Python's real `tomllib`** (an independent, spec-complete parser the repo never sees): all five parse, key set is exactly `{name, description, model, developer_instructions}`, `name` = role id, `model` = `gpt-5.6-sol`/`terra`/`luna` per tier, `developer_instructions` carries the pointer block |
| R13 | The live-install verification is recorded as an explicit, named reservation with a reproducible procedure — not as an unremarked gap, and not as done | intent.md G10, SC12 | **PASS** | O4 is carried below as a named, MED-severity, human-gated reservation with the five-step Task 4.6 procedure attached; `tasks.md` Task 4.6 is correctly left `[ ]`/`[!]`; and the Final Verdict names it as the first reservation. It is **not** closed and **not** silently dropped |

## Contract Compliance

| ID | Contract Item | Status | Verified By |
|---|---|---|---|
| C1 | Guarantee 1 — `getGenerator('codex')` resolves; `availableToolIds()` is exactly `['claude-code','cursor','kiro','github-copilot','codex']`; no `ToolId` returns `undefined` | **PASS** | Read `src/generators/index.ts` (Map literal is in `TOOL_IDS` order); `tests/generators/registry.test.ts` asserts all five `getGenerator` calls defined and `availableToolIds()` `toEqual` both the literal five-element array **and** `[...TOOL_IDS]` |
| C2 | Guarantee 2 — `agentsDir` = `.codex/agents`, `roleFileName` = `` `${id}.toml` ``, `conductorPath` = `.agents/skills/sdd-conductor/SKILL.md`, emitted keys ⊆ `{name, description, model, sandbox_mode, developer_instructions}`, each traceable to a dated first-party source | **PASS** | Read `codex.ts` lines 160–171; `registry.test.ts` asserts the three path facts against the real generator; real-`tomllib` parse of the five live-generated files shows key set exactly `{name, description, model, developer_instructions}`. Source traceability re-confirmed live — see CG-1 |
| C3 | Guarantee 3 — every generated `.toml` parses and its decoded values match the contract | **PASS** | `tests/generators/codex.test.ts` (test-local decoder) **plus** an independent `tomllib` parse of the live-generated artifacts by this auditor. Both agree |
| C4 | Guarantee 4 — canonical body byte-for-byte and contiguous in the artifact, and equal to the corresponding prefix of the decoded `developer_instructions`, verified from the **raw** template file rather than through the parser | **PASS** | `tests/canonical-fidelity.test.ts`: `sliceRawRoleBody`/`sliceRawConductorBody` use `fs.readFile` and a from-scratch reimplementation of the slicing rule — `parseRoleTemplate`/`parseConductorTemplate` are never called. Codex is now in `allGenerators()`, so the 5×5 raw-substring oracle and the conductor oracle both cover it, and a Codex-specific decoded-prefix test was added. Independently re-derived by this auditor in Python (contiguous, and present **exactly once** per file) |
| C5 | Guarantee 5 — no TOML syntax literal outside `toml.ts`: no `'''`, `"""`, hand-written `key = value`, or `#`-comment serializer in `codex.ts` | **PASS** | Read `src/generators/codex.ts` in full — it contains no TOML punctuation whatsoever; every emitted byte of syntax comes from `renderTomlComments` / `renderTomlKeyValues` / `tomlBasicString` / `tomlMultilineLiteral`. `tests/generators/codex.test.ts` enforces it with a comment-stripped source scan that also *positively* asserts the two shared renderers are called |
| C6 | Guarantee 6 — `tokens` always `[]`; every capability surfaced as a note; `audit.md only` present in the generated auditor artifact; unknown token renders `unmapped capability: <name>` | **PASS** (see CG-6 for a wording nit in the guarantee itself) | Read `mapCapabilities`; inspected the live-generated `sdd-auditor.toml`, whose comment header matches contract § "Illustrative output" line for line including note ordering. Six tests in `codex.test.ts` cover tokens/dedup/scope/unknown/once-only/empty |
| C7 | Guarantee 7 — `codexSandboxMode` returns `'read-only'` or `undefined`, **never** `'workspace-write'`; the key is emitted only for the read-only case; no real role emits it today | **PASS** | The function's return type is `'read-only' \| undefined` and the only `sandbox_mode` push is guarded by `=== 'read-only'` with `tomlBasicString(sandboxMode)` as the value — emitting `workspace-write` is structurally unreachable, not merely untested. Tests cover the synthetic read-only role, all five real roles, the scoped-`write-files` case, and `not.toContain('workspace-write')`. Grep of all five live artifacts: zero occurrences |
| C8 | Guarantee 8 — conductor emitted unconditionally; frontmatter exactly `name` + `description` from `ConductorMetadata`'s only two fields; `renderConductor` never reads `costTier`/`capabilities`/`invocation`/`handoff`; both `harny note:` caveats present | **PASS** | Read `renderConductor` — it destructures only `template` and `project` and reads only `metadata.id`, `metadata.purpose`, `sourcePath`, `body`. `codex.test.ts` additionally asserts `Object.keys(conductor.metadata).sort()` is `['id','purpose']`, so the type cannot silently grow a field the renderer then ignores. Both caveats confirmed verbatim in the live-generated `SKILL.md` |
| C9 | Guarantee 9 — the spec-schema pointer block is **inside `developer_instructions`**, not in a TOML comment, and names `SPEC_SCHEMA_DIR` imported from `src/engine.ts` rather than a duplicated literal | **PASS** | The *channel* was verified, not just the string: the pointer block is decoded **out of** `developer_instructions` (by both the test-local decoder and, independently, Python `tomllib`), and a separate test asserts the comment header — everything before the first top-level `name =` line — contains **neither** `GENERATED_BLOCK_BEGIN` **nor** `SPEC_SCHEMA_DIR`. `codex.ts` imports `SPEC_SCHEMA_DIR` from `../engine.js`; no `.sdd/spec-schema` literal appears in the generator |
| C10 | Guarantee 10 — exactly one `Record<CostTier, string>`; contains `gpt-5.6-sol`/`gpt-5.6-terra`/`gpt-5.6-luna` and **no** `gpt-5.4` id anywhere in `src/`; `modelOverride` returned verbatim | **PASS** as scoped by the guarantee; see CG-4 | `codex.ts` declares exactly one `MODEL_BY_TIER: Record<CostTier, string>` with the three verified ids. `grep -rn "gpt-5\.4" src/` returns **one** hit: `src/generators/cursor.ts:24` — a pre-existing, separately-verified, read-only-under-G9 Cursor model id. The test is scoped to `codex.ts`, which is exactly what guarantee 10 says; `roadmap.md` 2.1 / `tasks.md` 2.1 said "anywhere under `src/`", which is unachievable without editing a file G9 forbids touching. The narrowing is correct and is documented in-test — recorded as CG-4 |
| C11 | Guarantee 11 — `HarnessError('TEMPLATE')` naming the artifact when a value routed to `tomlMultilineLiteral` contains `'''`, a CR, or a control char other than `\n`/`\t`; thrown before any write | **PARTIAL** — see CG-5 | The throw, the code, the artifact name and the pre-write ordering all verified: `src/init.ts` builds the whole `files[]` array via `renderRole`/`renderConductor` (lines ~179–181) before any write call (~195/204), so the throw genuinely precedes I/O; three tests cover the three rejection classes and each asserts the artifact name is in the message. **Deviation:** the guarantee says the message names "the offending construct", but the implementation emits one fixed sentence listing all three possible constructs rather than identifying which one fired. Non-blocking |
| C12 | Guarantee 12 — `types.ts`, `markdown-yaml.ts`, the four shipped generators, `templates/**` and every module outside `src/generators/` byte-identical | **PASS** | `git status --porcelain` run directly by this auditor: the only `src/` entries are `M src/generators/index.ts` plus the two new generator files. `tests/canonical-fidelity.test.ts` now encodes the same sweep as two `git status --porcelain -- <pathspec>` assertions (including `:!src/generators`). Caveat recorded as CG-7: a git-status oracle goes quiet once the change is committed |
| C13 | Guarantee 13 — `NO_GENERATOR` → exit 4, the per-tool skip warning, and the `generator not shipped yet` hint each still have a **passing** test, driven by a synthetic registry / partial `available` list | **PASS** | **Audited line by line via `git diff`, not by suite colour.** (a) `tests/init.test.ts` — both tests wrapped in a `withCodexUnavailable` helper using `vi.doMock('../src/generators/index.js', …)` that copies the real registry and `delete`s `codex`; assertions **strengthened in form, unchanged in substance**: still `expect(result.skippedTools).toEqual(['codex'])` (exact `toEqual`, not `toContain`), still `isHarnessError` + `code === 'NO_GENERATOR'`, still `expect(entries).toEqual([])`. (b) `tests/cli.test.ts` — same module-mock; still `expect(result).toBe(4)` and `expect(entries).toEqual([])`. (c) `tests/prompts.test.ts` — the hint assertion is **unchanged** and still `expect(codexOption.hint).toMatch(/not shipped yet/i)`, driven by a partial `available` array; a **new** test additionally asserts every option's `hint` is `undefined` under the real `availableToolIds()`. No injection seam was added to `src/init.ts`, `src/cli.ts` or `src/prompts.ts` — all three are byte-identical. **Nothing deleted, nothing loosened** |
| C14 | Guarantee 14 — five-tool run emits 30 tool artifacts plus exactly one copy each of the five spec-schema files and `.sdd/harness.json`, byte-identical to `templates/spec-schema/*.md` | **PASS** | `tests/e2e-init.test.ts` updated 24 → 30 with the five-tool `--tools` list; the shared-file singleton assertions (`specSchemaFiles` length 5, one `harness.json`) are retained unchanged |
| C15 | Guarantee 15 — determinism and containment: identical inputs → byte-identical trees; every path relative and inside the target dir; every artifact ends in exactly one `\n` | **PASS** | New codex-only e2e block asserts all three; the two-run byte-identity check compares every file's contents. Independently confirmed on the live run: every one of the 12 artifacts (TOML and Markdown) ends in exactly one `\n`, no absolute paths, no `..` segments |
| C16 | Guarantee 16 — `package.json` `dependencies` and `devDependencies` byte-identical; no TOML package added | **PASS** | `tests/packaging.test.ts` asserts `git status --porcelain -- package.json` is empty, that no dependency name matches `/toml/i`, and pins both maps by exact value. Confirmed directly; `package-lock.json` is also unmodified |
| C17 | Interface — `src/generators/toml.ts` exports exactly `tomlBasicString`, `TomlKeyValue`, `renderTomlKeyValues`, `renderTomlComments`, `canRenderAsTomlLiteral`, `tomlMultilineLiteral`, with the contracted signatures and decoding semantics | **PASS** | Read the file end to end: exactly those six exports, signatures identical to the contract's TypeScript block including the `artifact` parameter and the `'read-only' \| undefined`-style narrow types. The `'''\n<value>\n'''` → `value + "\n"` decode rule is asserted in `toml.test.ts` and holds under real `tomllib` |
| C18 | Interface — `src/generators/codex.ts` exports `codexSandboxMode` and `codexGenerator`; `codexSandboxMode` is deliberately **not** added to `Generator` (the `isReadonlyRole` precedent) | **PASS** | Those are the only two `export`s in the file; `mapModel` and `mapCapabilities` are module-private and reach the outside only through the `Generator` object. `src/generators/types.ts` is byte-identical, so nothing was added to the interface |
| C19 | § "Interface sufficiency finding" — a stated verdict for the TOML target, including the role-TOML / conductor-Markdown asymmetry and the `markdown-yaml.ts` reuse observation | **PASS** | Present in `contract.md` lines 104–141 with an explicit verdict, four defended axes, and the layering observation about the three format-neutral helpers. Verdict re-derived independently and agreed with (see R7) |
| C20 | § "SUPERSEDES — `src/generators/index.ts`" — registry in `TOOL_IDS` order; `getGenerator`'s return type stays `Generator \| undefined` | **PASS** | Map literal order matches `TOOL_IDS`; `getGenerator(id: ToolId): Generator \| undefined` unchanged; the module doc comment was updated and no longer claims codex "deliberately resolves to `undefined`" |
| C21 | § "SUPERSEDES — reachability" — `src/init.ts`, `src/prompts.ts`, `src/errors.ts` unchanged; no injection seam added to `init.ts` | **PASS** | `git status --porcelain -- src` lists only `src/generators/*`. The re-pointing is done entirely with `vi.doMock` at the module boundary |
| C22 | Never-emitted key list honored: no `model_reasoning_effort`, `mcp_servers`, `skills.config`, `nickname_candidates`, `approval_policy`, `[permissions]`, `[agents]`/`config_file`; no `.codex/config.toml` written or edited | **PASS** | Real-`tomllib` parse of all five live artifacts returns exactly four keys each; no table headers at all. The live `init --tools codex` run wrote no `.codex/config.toml` (12-file listing confirms). This matters concretely — the live docs re-read confirms omitted session settings **inherit from the parent**, which is precisely the contract's stated rationale |
| C23 | Error Handling Contract — the two new rows (unrepresentable TOML literal → exit 5; retired/unknown `--model` override → not an error) behave as specified | **PASS** with the CG-5 caveat | Row 3: `HarnessError('TEMPLATE')` thrown pre-write, three tests. Exit 5 is the pre-existing `TEMPLATE` mapping, unchanged and separately covered. Row 4: `mapModel('mid', 'gpt-5.4-mini')` returns it verbatim and `renderRole` emits `model = "gpt-5.4-mini"` with no error and no `gpt-5.6-terra` fallback — asserted directly |
| C24 | Only the verified conductor location is written: `.agents/skills/sdd-conductor/SKILL.md`, and **no** speculative second copy at `.codex/skills/…` | **PASS** | The live run's complete file listing contains no `.codex/skills/` path; the e2e test asserts the exact 12-path set with `toEqual`, so a stray second copy would fail. Re-confirmed against live first-party docs: `.codex/skills` is still absent from the documented discovery roots (O1 stands) |

## Test Coverage

| ID | Test Description | Status | Test File |
|---|---|---|---|
| T1 | `tomlBasicString` escaping: `\`, `"`, newline → `\n`, tab → `\t`, other control chars → `\uXXXX`; round-trips through the test-local decoder | **PASS** | `tests/generators/toml.test.ts` |
| T2 | `canRenderAsTomlLiteral`: true for ordinary Markdown prose; false for `'''`, CR, or a control char other than `\n`/`\t` | **PASS** | `tests/generators/toml.test.ts` |
| T3 | `tomlMultilineLiteral`: emits `'''\n<value>\n'''`, no escaping, value is a byte-for-byte substring, decodes to `value + "\n"`, throws `HarnessError('TEMPLATE')` naming the artifact when unrepresentable | **PASS** | `tests/generators/toml.test.ts` |
| T4 | `renderTomlKeyValues` order preservation; `renderTomlComments` per-line `# ` prefixing that an interior newline cannot break out of | **PASS** | `tests/generators/toml.test.ts` |
| T5 | The real `templates/**` are literal-safe: no `'''`, no `"""`, no CR, no exotic control characters | **PASS** | `tests/generators/toml.test.ts` (recursive scan of `REAL_TEMPLATES_ROOT`, asserts the file list is non-empty first) |
| T6 | `mapModel` three-row table; `override` verbatim; **no `gpt-5.4` id in `codex.ts`'s own model table** | **PASS** | `tests/generators/codex.test.ts` |
| T7 | `codexSandboxMode`: `'read-only'` for a synthetic read-only capability set; `undefined` for all five real roles; never `'workspace-write'` | **PASS** | `tests/generators/codex.test.ts` |
| T8 | `mapCapabilities`: `tokens` always `[]`; one deduped note per capability; `audit.md only` for the real auditor; `unmapped capability: <name>` for an unknown token; the adapter `harny note:` exactly once | **PASS** | `tests/generators/codex.test.ts` |
| T9 | `renderRole`: path, comment header ordering, exact emitted key set, `sandbox_mode` absent for all five real roles, TOML-comment provenance (not HTML), single trailing `\n` | **PASS** | `tests/generators/codex.test.ts` |
| T10 | Channel split: the `harny:begin`/`harny:end` pointer block is inside the decoded `developer_instructions`, not in a comment, and names `SPEC_SCHEMA_DIR` from `src/engine.ts` | **PASS** | `tests/generators/codex.test.ts` (three tests: decoded-presence, comment-header-absence, and `SPEC_SCHEMA_DIR` identity) |
| T11 | `renderConductor`: `.agents/skills/sdd-conductor/SKILL.md`, Markdown+YAML frontmatter, keys exactly `name`/`description`, both caveats, project-config block, renders against the real canonical conductor | **PASS** | `tests/generators/codex.test.ts` |
| T12 | Decoded-value correctness for every generated role artifact | **PASS** | `tests/generators/codex.test.ts` + independent `tomllib` cross-check by this auditor |
| T13 | Source scan: `codex.ts` contains no `'''`, `"""`, hand-written `key = value`, or `#`-comment serializer | **PASS** | `tests/generators/codex.test.ts` |
| T14 | Registry: all five ids resolve; `availableToolIds()` equals the exact five-element array in `TOOL_IDS` order | **PASS** | `tests/generators/registry.test.ts` |
| T15 | The retained five-target evidence table's `codex` row asserted against the **real** `codexGenerator` (`.codex/agents`, `'toml'`, `.codex/agents/sdd-architect.toml`, `conductorPath` not under `agentsDir`) | **PASS** | `tests/generators/registry.test.ts` (evidence table retained in shape; new cross-check describe block added) |
| T16 | Skip-and-warn branch still exercised, via a `vi.mock` synthetic registry | **PASS** | `tests/init.test.ts` (`vi.doMock` + `withCodexUnavailable`; `skippedTools` asserted with `toEqual`) |
| T17 | `NO_GENERATOR` throw still exercised, via the same synthetic registry; nothing written | **PASS** | `tests/init.test.ts` |
| T18 | `NO_GENERATOR` → exit 4 mapping still exercised; target directory empty | **PASS** | `tests/cli.test.ts` |
| T19 | `generator not shipped yet` hint still exercised via a partial `available` array; **and** no option carries a hint with the real `availableToolIds()` | **PASS** | `tests/prompts.test.ts` (both halves present) |
| T20 | Five-tool run: 30 tool artifacts + exactly one copy of each shared file | **PASS** | `tests/e2e-init.test.ts` |
| T21 | `init --tools codex` writes 6 + 6; two identical runs byte-identical; paths relative and contained; single trailing `\n` per artifact | **PASS** | `tests/e2e-init.test.ts` |
| T22 | Canonical fidelity for the fifth generator: raw-file slice is a contiguous byte-for-byte substring of the Codex artifact **and** equals the decoded `developer_instructions` prefix, for all five roles and the conductor | **PASS** | `tests/canonical-fidelity.test.ts` |
| T23 | No-regression sweep: `templates/**`, `types.ts`, `markdown-yaml.ts`, the four shipped generators, and every module outside `src/generators/` byte-identical | **PASS** (with the CG-7 caveat) | `tests/canonical-fidelity.test.ts` |
| T24 | `package.json` dependency lists byte-identical; no TOML package | **PASS** | `tests/packaging.test.ts` |
| T25 | **[!] Manual, human-only** — live Codex CLI install lists the five agents from `.codex/agents/` and finds `$sdd-conductor` under `.agents/skills/` | **MISSING (blocked — by design)** | none (manual; `tasks.md` Task 4.6). See reservation **O4**. This is the only unclosed row and it is deliberately unclosed |

Full-suite result at audit time: **`npm test` → 20/20 files, 260/260 tests passing** (re-run by this
auditor, not taken from the executor's report). `npm run typecheck` and `npm run build` both
clean. The default run is fully offline: no test in the suite reaches a network service, and
the one external-tool test (`npm pack --dry-run`) runs locally. Nothing is skipped.

## Carried reservations

Pre-seeded by `sdd-architect`; **re-affirmed, not closed, by this audit pass.** They correspond
to `contract.md` § "Open items — explicitly NOT verified".

| ID | Reservation | Severity | Status after this pass | Closes when |
|---|---|---|---|---|
| **O1** | Whether Codex *also* reads `.codex/skills/` in addition to the `.agents/skills/` roots pinned here | LOW | **OPEN — re-confirmed against live docs on 2026-09-02.** `learn.chatgpt.com/docs/build-skills` still enumerates only `$CWD/.agents/skills`, `$CWD/../.agents/skills`, `$REPO_ROOT/.agents/skills`, `$HOME/.agents/skills`, `/etc/codex/skills` and bundled system skills. `.codex/skills` is still absent. Note for the human: `plan.md:14` records that the maintainer's own earlier `mr-engine-app` repo contained `.codex/skills/openspec-*`, which is weak anecdotal evidence the other way — worth resolving in the same session | A live `/skills` check in each location. Current behavior stays correct either way — harny writes only the verified location (C24) |
| **O2** | Whether Codex's agent `name` field accepts hyphens (`sdd-architect`) | MED | **OPEN — narrowed but not closed.** The live docs now state explicitly: *"Codex identifies the custom agent by its `name` field. Matching the filename to the agent name is the simplest convention, but the `name` field is the source of truth."* Still **no charset restriction is documented**, and all six documented examples use underscored names in hyphenated filenames (`.codex/agents/pr-explorer.toml` → `name = "pr_explorer"`). harny emits hyphens in both. Undisproven, unconfirmed | A live install listing the five agents by name. If rejected, the fix is confined to `roleFileName`/`name` and touches no canonical content |
| **O3** | Whether Codex enforces any length limit on `description` or `developer_instructions` | LOW | **OPEN, and the contract's statement of it is now known to be incomplete — see CG-2.** A first-party budget *is* documented for skills, though it is a soft aggregate one, not a per-field rejection limit. No limit of any kind is documented for the *agent* `description` or `developer_instructions` | A documented per-field limit or an observed rejection. The **no-guard decision remains correct** — Codex shortens/omits gracefully rather than erroring, so a guard would fabricate a hard failure the vendor does not have |
| **O4** | **The artifact set has never been loaded into a running Codex CLI.** This is AL-30's other half, for the fifth tool. A generator writing to a directory a tool never reads fails silently with exit code 0 | **MED (open, human-gated)** | **OPEN. Not performed. Not closed. `tasks.md` Task 4.6 correctly remains unchecked.** This audit strengthened the documentary position (CG-1: all three first-party URLs re-fetched and re-read today, every checked row confirmed) and added an execution-adjacent check the spec did not require (real-`tomllib` parse of the emitted artifacts, CG-8) — but neither is a Codex CLI execution check | `tasks.md` Task 4.6's five-step procedure, with the outcome recorded in the Audit Log below. O1, O2 and O3 are all answered by the same session |

## Audit Log

| Date | Auditor | Finding | Severity | Resolution |
|---|---|---|---|---|
| 2026-08-30 | sdd-architect | Reservations O1–O4 seeded before implementation (see above). Recorded here so the audit inherits them rather than rediscovering — or missing — them | INFO | Carried into the audit as open items; O4 is the one that must not be closed without a recorded live-install result |
| 2026-09-02 | sdd-auditor | **CG-1 — Verification-channel disclosure, and an independent re-verification of the Codex facts.** Context7 MCP was *not* available to this auditor and neither was WebFetch/WebSearch, but **outbound HTTPS from the shell was**, and was used. Re-fetched and read on 2026-09-02: (a) `developers.openai.com/codex/subagents` → confirmed **308** to `learn.chatgpt.com/docs/agent-configuration/subagents` (**D6 confirmed**); (b) the subagents page — confirmed `.codex/agents/` **project scope exists** (**D1 confirmed**), the required `{name, description, developer_instructions}` triple, the additional accepted keys `model`/`model_reasoning_effort`/`sandbox_mode`/`mcp_servers`/`skills.config`, `sandbox_mode` values `read-only`/`workspace-write`, "subagents inherit the permission mode selected beneath the composer", and — directly corroborating the contract's omit-don't-invent rationale — *"Other session settings, such as `sandbox_mode`, `mcp_servers`, and `skills.config`, inherit from the parent when the custom agent file omits them"*; (c) `developers.openai.com/codex/models` — confirmed the `gpt-5.6-sol`/`terra`/`luna` triple with the contract's own descriptions, the `gpt-5.6-sol`-at-medium "Power" default, and verbatim *"GPT-5.4 and GPT-5.4 mini retire from Codex on August 31, 2026 … replace `gpt-5.4` with `gpt-5.6-terra` and `gpt-5.4-mini` with `gpt-5.6-luna` in saved configurations, **custom agents**, and scheduled tasks"* (**D2 confirmed — and note the retirement date has now passed**); (d) `learn.chatgpt.com/docs/build-skills` — confirmed the `.agents/skills` repo/user/admin discovery roots, `SKILL.md` requiring `name` + `description`, the `$`/`​/skills` invocation, and the `scripts/`/`references/`/`assets/`/`agents/openai.yaml` layout (**D5 confirmed**). **No contradiction found in any checked row.** Not re-verifiable here: the Context7 `/openai/codex` Rust-source rows (`agent_roles.rs`, `agent_role_config.rs` `deny_unknown_fields`, `config.schema.json`, `core-skills/src/loader.rs`) — the Context7 channel was unavailable, and those rows are accepted on the architect's dated attestation | INFO | R3/C2/C10 pass. This is a materially stronger position than the previous feature's AL-30, which could not re-run even a docs review |
| 2026-09-02 | sdd-auditor | **CG-2 — One "Verified Codex facts" row is incomplete: a first-party skills budget *is* documented.** `contract.md` § "Skills" states `Documented size limit on \`description\` \| **none found** \| —`, and the Error Handling § non-row states "no character limit for Codex's `SKILL.md` `description` was found in any first-party source". `learn.chatgpt.com/docs/build-skills`, read 2026-09-02, in fact documents: *"the initial list … uses at most 2% of the model's context window, or 8,000 characters when the context window is unknown. If many skills are installed, Codex shortens skill descriptions first. For large skill sets, Codex may omit some skills from the initial list and show a warning."* This is an **aggregate, soft, gracefully-degrading budget over the whole skills list**, not a per-field rejection limit — so **the no-guard implementation decision is still correct** (emitting a Kiro/Copilot-style hard guard would fabricate a failure mode the vendor does not have), and the generated conductor `description` is ~190 characters, far inside any plausible share of an 8,000-character floor. But G3 makes fact accuracy a first-class deliverable of this feature, and the row as written is a false negative | **MEDIUM** | **Not blocking. Documentation fix.** Update the § "Skills" row to record the 2%/8,000-character initial-list budget with its source, and reword O3 to distinguish "no per-field hard limit documented" (true) from "no size limit documented at all" (false). No code change |
| 2026-09-02 | sdd-auditor | **CG-3 — Phase 3 stub retirement audited line by line; coverage was re-pointed, not weakened.** This is the finding `tasks.md` and this file both flagged as highest-risk, so it was audited from `git diff`, not from suite colour. `tests/init.test.ts`: both branches now run under a `withCodexUnavailable` helper that `vi.doMock`s `src/generators/index.js`, copies the **real** registry and deletes only `codex`; the skip assertion is still the exact `expect(result.skippedTools).toEqual(['codex'])` and the throw assertion still checks `isHarnessError` + `code === 'NO_GENERATOR'` + `expect(entries).toEqual([])`. `tests/cli.test.ts`: same mock, still `expect(result).toBe(4)` and `expect(entries).toEqual([])`. `tests/prompts.test.ts`: the hint assertion is **byte-unchanged** and still `toMatch(/not shipped yet/i)` against a partial `available` array, with a **new** test added asserting `hint` is `undefined` for every option under the real `availableToolIds()`. **No assertion was deleted; none was loosened to `toContain`; no injection seam was added to `src/init.ts`, `src/cli.ts` or `src/prompts.ts` (all three byte-identical).** `tests/generators/registry.test.ts` retains the five-target evidence table and adds the real-`codexGenerator` cross-check | INFO | C13, R8, T16–T19 pass. The mitigation `roadmap.md` prescribed for this risk worked exactly as designed |
| 2026-09-02 | sdd-auditor | **CG-4 — The `gpt-5.4` scan is scoped to `codex.ts`, not repo-wide, and that is correct.** `roadmap.md` 2.1 and `tasks.md` 2.1 both call for "a source scan asserting **no `gpt-5.4` id appears anywhere under `src/`**", but `contract.md` guarantee 10 scopes the same claim to "`codex.ts` … contains **no** `gpt-5.4` id". The wider wording is unsatisfiable: `grep -rn "gpt-5\.4" src/` returns exactly one hit, `src/generators/cursor.ts:24` (`cheapest: 'gpt-5.4-mini'`), a shipped, separately-verified **Cursor** model id in a file G9 explicitly forbids modifying. The test-writer scoped the scan to `codex.ts` and documented the reason in a comment. The contract wins over the roadmap phrasing; the deviation is correct, disclosed, and the guarantee-level claim is fully enforced | LOW | No action for this feature. `tasks.md` Task 2.1's wording is retroactively imprecise; the executor's in-test comment already records why |
| 2026-09-02 | sdd-auditor | **CG-5 — `HarnessError('TEMPLATE')` names the artifact but not *which* construct offended.** Guarantee 11 and the Error Handling row both say the message names "the artifact **and the offending construct**". `tomlMultilineLiteral` emits one fixed sentence enumerating all three candidates ("three consecutive single quotes, a carriage return, or a control character other than \n/\t") rather than identifying the one that fired. The artifact name *is* present and asserted in all three tests, the code is `TEMPLATE`, and the throw genuinely precedes any write. A maintainer hitting this on a 6 kB canonical body would have to bisect to find which of the three it was | **MEDIUM** | **Not blocking.** Recommend `canRenderAsTomlLiteral` gain a sibling that returns the specific reason (or that `tomlMultilineLiteral` re-test the three conditions in order) so the message says which construct and, ideally, at what offset |
| 2026-09-02 | sdd-auditor | **CG-6 — Guarantee 6's own wording under-describes the two-channel note split.** Guarantee 6 says "every note reaches the rendered artifact as a `# capability note:` line", but § "`mapCapabilities`" a few pages earlier explicitly mandates that the adapter-level sandbox caveat render as `# harny note:` instead, and § "Illustrative output" shows it that way. The implementation follows the specific sections and the illustration (verified against a live-generated `sdd-auditor.toml`, which matches the illustration line for line). Spec-internal wording imprecision, not an implementation defect | LOW | Reword guarantee 6 to "…as a `# capability note:` line, except the single adapter-level caveat, which renders as `# harny note:`". No code change |
| 2026-09-02 | sdd-auditor | **CG-7 — The no-regression sweep (T23, C12) is a `git status` oracle and goes silent once the work is committed.** `tests/canonical-fidelity.test.ts`'s new Task 4.2 block asserts `git status --porcelain` is empty for `templates/`, the four shipped generators, `types.ts`, `markdown-yaml.ts` and `src` minus `src/generators`. That is a faithful and correctly-failing check **right now, pre-commit** (this auditor confirmed the underlying `git status` independently), and it mirrors the mechanism the pre-existing T41 check already uses — but after these changes are committed it becomes vacuously true and stops guarding anything. It is a *this-feature* gate, not a standing regression test | LOW | Accepted as-is (it matches existing repo precedent and this feature's audit window). If a standing guard is wanted later, a content-hash fixture would survive the commit |
| 2026-09-02 | sdd-auditor | **CG-8 — Independent cross-parser and cross-oracle validation of the emitted artifacts (beyond what the spec required).** The repo proves guarantees 3 and 4 with a test-local decoder, correctly (a TOML dependency is forbidden by G9/guarantee 16). To make sure the decoder is not simply agreeing with the emitter's own assumptions, this auditor scaffolded into a scratch directory with `node bin/harness.js init … --tools codex --yes` and re-checked the output with tooling the repo never sees: **Python's `tomllib`** (a complete, independent TOML implementation) parsed all five `.codex/agents/*.toml` successfully, returning exactly `{name, description, model, developer_instructions}` per file with `name` = the role id and `model` ∈ {`gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.6-luna`} per tier; and a from-scratch Python re-slice of each **raw** template confirmed the canonical body is a contiguous byte-for-byte substring appearing **exactly once** per artifact and is the exact prefix of the `tomllib`-decoded `developer_instructions`. Same for the conductor body inside `SKILL.md`. Also confirmed on the emitted set: zero `"""`, exactly one `'''` pair per role file, zero `workspace-write`, zero CR, exactly one trailing `\n` per artifact, and no `.codex/skills/` or `.codex/config.toml` written | INFO | C3, C4, C7, C15, C22, C24, R4, R12 pass on an oracle independent of the repo's own test plumbing |
| 2026-09-02 | sdd-auditor | **CG-9 — Executor's narrowing of one approved red-phase assertion is correct.** `tasks.md` discloses that the red-phase test "renders provenance as a TOML `#` comment … never an HTML comment" originally asserted the whole file contains no `<!--`, and that the executor narrowed it to the comment-header region. Audited independently: guarantee 9 *requires* the spec-schema pointer block inside `developer_instructions`, that block is produced by the shared `renderSpecSchemaPointerBlock`, and its `GENERATED_BLOCK_BEGIN`/`END` constants in `markdown-yaml.ts` are literally `<!-- harny:begin/end generated project configuration -->` — a file G9 forbids modifying. `contract.md`'s own "Illustrative output — Codex auditor role" shows that exact HTML-comment marker inside `developer_instructions`. The original assertion was therefore **unsatisfiable without violating guarantee 9 and G9 simultaneously**. The narrowed form is also the *stronger* channel check, since it pairs with the sibling assertion that the header carries neither `GENERATED_BLOCK_BEGIN` nor `SPEC_SCHEMA_DIR`. Correct fix, correctly disclosed; not a corner cut | INFO | No action. The disclosure in `tasks.md` § "Execution status" is the right way to have handled it |
| 2026-09-02 | sdd-auditor | **CG-10 — The feature is not user-visibly complete: `README.md` and `AGENTS.md` still say Codex is unshipped.** `README.md:126` still reads "`codex` — the CLI recognizes this ID but reports it as 'generator not shipped yet'" and `AGENTS.md:23` still says "Codex (TOML format) is planned future work"; `CHANGELOG.md` likewise. Both are **false as of this implementation**. This is *by design* — `intent.md` Non-Goals and `roadmap.md` § "Deferred to `sdd-documentation`" both assign these to the automatic post-audit documentation step — so it is not an implementation defect. It is recorded here so the human gate does not mistake "audit approved" for "ready to announce" | LOW | Blocks nothing in this audit. **`sdd-documentation` must run before this feature is considered shipped**; G8's "README" clause is discharged by that step, not by this one |
| 2026-09-02 | sdd-auditor | **CG-11 — Forward note, out of scope: `src/generators/cursor.ts` maps `cheapest` → `gpt-5.4-mini`, an id OpenAI retired from Codex on 2026-08-31 (yesterday, relative to this audit).** Cursor's model catalogue is a different vendor surface and may well still offer it, so this is **not** a defect and **not** in this feature's scope (G9 makes `cursor.ts` read-only). Recorded only because this audit had live docs access and saw the retirement land | LOW | Recommend a future spec re-verify Cursor's `cheapest`-tier id against Cursor's own current catalogue. No action here |
| 2026-09-02 | sdd-auditor | **CG-12 — O4 remains open.** No Codex CLI install and no way to observe an agent picker from this session. `tasks.md` Task 4.6 is correctly left unchecked with a `[!] BLOCKED` marker and a five-step procedure; Task 4.7 (the post-audit human gate) is this report's addressee. Recording the non-result explicitly, per this file's own standing instruction, so it cannot lapse the way AL-30 did | **MEDIUM (open, human-gated)** | Carried as reservation **O4**. Named as the first reservation in the Final Verdict. **Must not be marked done without a recorded live-install result** |

## Final Verdict

**Status**: **APPROVED WITH RESERVATIONS**

**Summary**: The implementation is a faithful, complete and unusually well-evidenced
realization of the contract — all 16 behavior guarantees hold, all 12 success criteria are
met, `typecheck`/`build`/`npm test` are clean (260/260 across 20 files, fully offline, nothing
skipped), and the highest-risk item in the feature (Phase 3's stub retirement) was executed
exactly as prescribed, with every `NO_GENERATOR`, skip-warning and prompt-hint assertion
re-pointed onto synthetic registries rather than deleted or weakened. The reservations are
the pre-declared ones: the artifacts have still never been loaded into a running Codex CLI
(O4), and one documented vendor fact needs a correction this audit's live docs access
surfaced (CG-2).

**Critical Issues** (must fix before merge): **None.** No contract violation, no missing
interface, no broken guarantee, no scope creep into any declared non-goal, no dependency
drift, no regression to `templates/**`, `types.ts`, `markdown-yaml.ts` or the four shipped
generators.

**Warnings** (should fix, not blocking):

- **CG-2 (MEDIUM)** — `contract.md` § "Verified Codex facts" → Skills claims no documented
  size limit on `description`. Live first-party docs (read 2026-09-02) do document an
  aggregate initial-skills-list budget: 2% of the context window, or 8,000 characters when
  unknown, with Codex shortening descriptions and then omitting skills with a warning. The
  no-guard implementation decision stays correct (it degrades gracefully; it does not
  reject), but the recorded fact and O3's wording should be corrected.
- **CG-5 (MEDIUM)** — `HarnessError('TEMPLATE')` from `tomlMultilineLiteral` names the
  artifact but enumerates all three possible offending constructs instead of identifying the
  one that fired, where guarantee 11 says it should name "the offending construct".
- **CG-10 (LOW, process)** — `README.md`, `AGENTS.md` and `CHANGELOG.md` still state that
  Codex is unshipped. Deferred to `sdd-documentation` by design; noted so approval here is
  not read as "ready to announce".

**Reservations carried forward** (open, human-gated — this is the AL-30 corrective working
as intended):

- **O4 (MEDIUM) — the live-install check has NOT been performed.** Nothing in this audit is
  an execution check against Codex CLI. Every path, key, field and model id is verified
  against first-party documentation — re-verified live by this auditor today, not merely
  taken from the spec — and the emitted TOML parses under an independent real TOML parser.
  But **a generator writing to a directory the tool never reads fails silently with exit code
  0**, and only a human with an install can rule that out. Run `tasks.md` Task 4.6's five
  steps and record the outcome in the Audit Log above. **Do not mark Task 4.6 `[x]` without a
  recorded result.**
- **O2 (MEDIUM)** — hyphenated agent `name` values (`sdd-architect`) are undisproven but
  unconfirmed; live docs state the `name` field is the source of truth but document no
  charset rule, and every vendor example uses underscores. Answered by the same session.
- **O1 (LOW)** and **O3 (LOW)** — also answered by the same session.

**Recommendations** (nice to have):

- Assert explicitly in `tests/e2e-init.test.ts` that a `--tools codex` run produces **no**
  "Skipped" warning on stderr. SC2 states it; today it is only structurally implied by
  `getGenerator('codex')` being defined.
- **CG-7** — the Task 4.2 no-regression sweep is a `git status` oracle and becomes vacuous
  once this work is committed. Fine for the audit window; a content-hash fixture would make
  it a standing guard.
- **CG-6** — reword guarantee 6 so it matches the two-channel (`# capability note:` /
  `# harny note:`) split the rest of the contract mandates.
- **CG-4** — `tasks.md` Task 2.1's "anywhere under `src/`" phrasing is unsatisfiable given
  `cursor.ts`; the contract's `codex.ts`-scoped wording is the correct one.
- **CG-11** — a future spec should re-verify Cursor's `cheapest` → `gpt-5.4-mini` mapping
  against Cursor's own current catalogue; OpenAI retired that id from Codex on 2026-08-31.

---

### Notes for the auditing role

- **Read `tasks.md` Phase 3 hardest.** Four existing test files use `codex` as their
  "unimplemented tool" fixture. Deleting those assertions, or loosening them to
  `toContain`, leaves the suite green while silently dropping coverage of two documented
  `cli-skeleton` guarantees. C13 and T16–T19 exist specifically to catch that.
  *(2026-09-02: done — see CG-3. Coverage was re-pointed, not removed.)*
- **Check the pointer block's *channel*, not just its presence (C9).** A TOML `#` comment
  containing `harny:begin` would satisfy a naive substring assertion while being invisible
  to the model — defeating the exact purpose AL-5 was raised for. It must decode out of
  `developer_instructions`. *(2026-09-02: done — verified by two independent decoders plus a
  comment-header-exclusion assertion.)*
- **Do not accept `plan.md` as corroboration for any Codex fact.** Contract § "Verified
  Codex facts" is the source of truth; § "Discrepancies from `plan.md`" (D1–D7) records
  where `plan.md` is wrong, stale, or silent. D2 in particular: `plan.md` recommends a
  model that retires 2026-08-31. *(2026-09-02: D1, D2, D5 and D6 independently re-confirmed
  against live first-party sources — see CG-1. The `gpt-5.4` retirement date has now passed.)*
- **If live docs-lookup tooling is unavailable in your audit environment, say so
  explicitly** in the Audit Log, as the previous feature's auditor correctly did. Silence
  on the verification channel is what turned AL-30 into a two-pass blind spot.
  *(2026-09-02: stated in full at the top of this file and in CG-1 — Context7/WebFetch were
  unavailable, outbound HTTPS from the shell was available and was used, and the specific
  rows that remain unre-verified are named.)*
