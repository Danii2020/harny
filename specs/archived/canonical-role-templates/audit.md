# Audit: canonical-role-templates

> Compliance-tracking scaffold. Rows are seeded PENDING; the sdd-auditor fills status
> and verification later. Every requirement traces to `intent.md` (Rn → goal/criterion),
> every contract item to `contract.md` (Cn → guarantee/interface), every verification to
> a contract guarantee or intent criterion. Because this feature's deliverable is content
> (Markdown, not code), the "Test Coverage" section tracks **verification checks** (the
> Phase 5 sweeps and structural checks) rather than executable unit tests.

## Requirements Checklist
| ID | Requirement | Source | Status | Notes |
|---|---|---|---|---|
| R1 | `templates/roles/` contains all five role files (architect, test-writer, executor, auditor, documentation) | intent.md G1 / SC1 | PASS | `find templates -type f` returns exactly 11 files; all five role files present, all non-empty (4,798–7,189 bytes). |
| R2 | `templates/conductor/sdd-conductor.md` describes the 5-role pipeline, 3 unchanged gates, documentation auto-flow; not framed as a Claude Skill | intent.md G5 / SC2 | PASS | Line 9 "sequence the five `sdd-*` roles"; pipeline block lines 15–23 names all 5 roles + 3 `[HUMAN GATE: …]` markers; line 25 "Five roles, three human gates, one automatic post-audit handoff"; opening blockquote explicitly disclaims "Claude-Code-only 'Skill' package"; no `SKILL.md` filename, no Skill frontmatter. |
| R3 | `templates/spec-schema/` contains intent/contract/roadmap/tasks/audit as standalone schema files matching the architect's emitted shape | intent.md G6 / SC3 | PASS | All 5 present and non-empty. Programmatic diff of each file's fenced template against the corresponding inline block in `.claude/agents/sdd-architect.md`: `intent`, `roadmap`, `tasks` byte-identical; `contract` differs only in neutralized prose (see C9); `audit` adds a Final Verdict section (see Audit Log AL-2). |
| R4 | No canonical file hardcodes a Claude `tools:` list, concrete model id, or Claude-only path as its sole source of truth; each role declares cost tier + capabilities | intent.md G3 / SC4 | PASS | **Re-verified 2026-07-26 (post-fix).** Sweeps over `templates/` for `opus\|sonnet\|haiku`, `^(tools\|model\|color\|name\|description):`, `<example>`, `SKILL.md`, `.claude/` all still return zero hits; no file begins with a `---` frontmatter fence. All 5 roles carry `cost_tier` + `capabilities`. The AL-4 fix generalized both `CLAUDE.md` mentions to "`CLAUDE.md`, `AGENTS.md`, or this project's equivalent conventions doc". The criterion's three enumerated items remain clean; the newly reintroduced `AskUserQuestion` tool name is none of them, so R4 holds — that regression is scored under R7 (AL-9). |
| R5 | Verified improvements preserved (test-writer red-first, docstring spec linkage, high-value-tests reference, generalized Project Context); no AI-Radar or mr-engine/Vitest/Supabase residue | intent.md G2 / SC5 | PASS | Red-first at `sdd-test-writer.md:11,63,85`; "Do NOT put contract IDs in test names — spec linkage belongs in a docstring/docblock/comment" at :66–67; high-value-tests rubric at :34; `### Project Context` in architect :17 and executor :17. Drift sweep (`AI Radar\|Bedrock\|LangGraph\|Vitest\|Supabase\|mr-engine\|renderToStaticMarkup\|tailwind\|shadcn\|Radix\|\.live\.test\|SUPABASE_TEST\|IVA\|curation\|card.rank\|npm test\|npx \|tsc\b`) returns zero hits; `uv ` zero hits. The production source's `graph structure, nodes, agents, state, utils` was correctly neutralized to `modules, services, state, shared utilities`. |
| R6 | `sdd-documentation.md` encodes all 8 fixed requirements (cheapest tier + rationale, auto non-gated trigger on APPROVED/APPROVED-WITH-RESERVATIONS, inputs, outputs incl. in-place Shipped header, bootstrap, verified-only, no code comments, optional summary) | intent.md G4 / SC6 | PASS | All 8 located: (1) :7–8 `cost_tier: cheapest` + 3-tier cost rationale; (2) :10–11, :21–27 automatic/non-gated/REJECTED-stops; (3) :31–34 five specs + verdict + real diff; (4) :40–43 README, Keep-a-Changelog CHANGELOG, ARCHITECTURE/AGENTS if present, `Shipped: <date>` in place with explicit "Do NOT move"; (5) :45–47 bootstrap; (6) :51 verified-only; (7) :52 never touch comments/docstrings; (8) :55–57 optional change summary. See Audit Log AL-3 for an internal tension between (4) and (5). |
| R7 | Content is generic across stacks and tools — no single product/language/package-manager/tool named as the only possibility | intent.md G1 / SC7 | PASS | **Restored to PASS 2026-07-26 (rev 3).** Language mentions are enumerated multi-option examples ("TypeScript, Go, Ruby, Python — whatever … exploration found"); no package manager named; AL-4 removed the `CLAUDE.md`-only naming; Context7 always carries "or the target tool's equivalent docs-lookup MCP". The AL-9 regression is fixed: `conductor/sdd-conductor.md:53` now leads with the generic behavior — "Use the tool's structured ask-the-human mechanism" — and demotes the tool name to an attributed illustration, "(e.g. `AskUserQuestion` on Claude Code)". No single tool is named as the only possibility anywhere in `templates/`. |
| R8 | Reflexive traceability holds across this feature's own spec set (contract→intent, tasks→roadmap, audit→intent/contract) | intent.md G7 | PASS | contract.md cites `(Gn)` on every section (manifest G1/G4/G5/G6; role schema G1/G3; vocabularies G3; per-role table G2/G4; conductor G5; spec-schema G6; all 7 guarantees carry a Gn). roadmap.md phases 1–5 each cite contract items + goals. tasks.md groups all 29 tasks under the 5 roadmap phase headings and cites C-ids. This audit's R1–R9 map onto intent G1–G7/SC1–SC7 + the docs-lookup content rule with no unmapped goal; C1–C18 map onto contract.md sections/guarantees. |
| R9 | Roles declaring `docs-lookup` (architect, executor, test-writer) instruct verifying library/API usage via Context7 (or equivalent) before pinning signatures/contracts/tests | contract.md docs-lookup content rule (G3) | PASS | Exactly the three `docs-lookup` roles carry it: `sdd-architect.md:22` ("before pinning a signature, contract, or test"), `sdd-executor.md:24` ("before using them"), `sdd-test-writer.md:37` ("before asserting against it"). All three use the portable "Context7 (or the target tool's equivalent docs-lookup MCP)" phrasing and "do not trust memory for library APIs". No non-docs-lookup role (auditor, documentation) carries a stray Context7 instruction. |

## Contract Compliance
| ID | Contract Item | Status | Verified By |
|---|---|---|---|
| C1 | File manifest is exactly the 11 specified files (5 roles, 1 conductor `sdd-conductor.md`, 5 spec-schema) — no more, no fewer | PASS | `find templates -type f \| sort` → exactly the 11 contracted paths, no extras, no non-`.md` files. Directory names and filenames match the contract tree character for character; conductor file is `sdd-conductor.md`, not `SKILL.md`. |
| C2 | Every `roles/*.md` follows the shared Role Metadata schema (id, purpose, cost_tier, cost_rationale, capabilities, invocation as prose, handoff) + body | PASS | Per-file scan of all 5: each opens `# <role-id>` matching its `id:`, then `## Role Metadata` (line 3) with all 7 keys in contract order, then `## Role body` (line 13). Every `invocation:` is prose; no `<example>` blocks anywhere. Note: the contract's `## <Role body>` placeholder was rendered literally as `## Role body` in all 5 files — uniform, and uniformity is the stated purpose of the schema, so recorded as informational only. |
| C3 | `cost_tier` values respect the enum and mapping (architect/auditor most-capable; test-writer/executor mid; documentation cheapest) | PASS | Extracted `cost_tier` lines: architect `most-capable`, auditor `most-capable`, test-writer `mid`, executor `mid`, documentation `cheapest`. Exact match to the per-role table; no value outside the 3-value enum. Each is accompanied by a non-empty `cost_rationale`; documentation's rationale states the 3-tier cost argument explicitly. |
| C4 | `capabilities` use only the controlled vocabulary; each role declares least-privilege set (auditor read + run-shell + audit.md-only write) | PASS | architect `read-files, write-files, run-shell, web-search, docs-lookup, task-tracking`; test-writer `read-files, write-files, run-shell, docs-lookup`; executor `read-files, write-files, run-shell, docs-lookup, web-search, task-tracking`; auditor `read-files, run-shell, write-files (audit.md only)`; documentation `read-files, write-files, run-shell`. Every token is from the 6-token vocabulary and every set matches the per-role table exactly, including the auditor's scoping parenthetical. |
| C5 | `docs-lookup` vocabulary entry names Context7 MCP as canonical/default provider with per-tool-equivalent mapping | PASS | Contract-side definition is in `contract.md` itself (Data Model — controlled vocabularies); the implementation-side obligation is the docs-lookup content rule, satisfied in all three declaring roles with the "Context7 (or the target tool's equivalent docs-lookup MCP)" formulation — canonical provider named, per-tool substitution allowed. No raw `mcp__context7__*` tool identifiers leaked into `templates/`. |
| C6 | Per-role preserve/strip rules applied exactly as tabled for all four reconciled roles | PASS | Line-level diff of each canonical role against both sources (`mr-engine-app/.claude/agents/*` and this repo's `.claude/agents/*`). Architect: generalized Project Context, written-brief rule, one-file-at-a-time approval, 3 traceability rules, real-language rule all preserved; project-flavored `<example>`s (curation pipeline / card ranking) and all Claude frontmatter removed. Test-writer: red-first, docstring linkage, portable high-value-tests reference, offline-by-default, "Fake the injected seams" preserved; production's "runs AFTER executor" ordering, `tests/CONVENTIONS.md`, Vitest, Supabase, `Db` mock, `renderToStaticMarkup`, and the hardcoded `.claude/skills/high-value-tests/SKILL.md` path all removed; `ImportError`/`AttributeError` neutralized to "an import/attribute error". Executor: Project Context, contract-is-law, no-scope-creep, TDD note, and the "test suite passes via project runner" checklist item preserved. Auditor: 7 steps, verdict enum, 4 severity ratings, "report, don't fix", generalized dependency-manifest+lockfile diff and offline test run preserved; `str`/`Optional[str]` example neutralized. |
| C7 | sdd-documentation content contract (8 items) fully encoded | PASS | **Re-verified 2026-07-26 (post-fix).** All 8 still encoded — 16 targeted greps (tier, 3-tier rationale, both approved verdicts, REJECTED, 5 specs, real diff, README, Keep a Changelog, ARCHITECTURE/AGENTS, `Shipped: <date>`, "Do NOT move", bootstrap heading, "create minimal starting versions", verified-only, never-touch-comments, change summary) each return ≥1 hit. The AL-3 contradiction is gone: `grep -n 'solely for this step'` → no match, and output item 3 (:42) now defers cleanly with "If neither exists yet, see the bootstrap rule below", leaving Step 4 (:47) as the single source of truth. Contract items 4 ("if present") and 5 ("bootstrap if missing") are both still honored. |
| C8 | Conductor content contract met: 5 roles, 3 unchanged gates, documentation auto-flow, existing hard rules preserved, no Claude-Skill-only framing | PASS | (1) five roles named in the pipeline block; (2) exactly 3 `HUMAN GATE` markers, same three points as the source (post-specs, post-red-tests, post-audit), verbatim-equivalent gate wording; (3) new hard rule #4 + the `auditor → documentation` bullet under "FLOW automatically" + the post-run change-summary line at :53; (4) all seven source hard rules preserved (never self-approve :29, default-to-TDD :31, failing-tests-are-a-gate :33, delegate-back-to-same-role :37, verify-don't-trust :60, invocation-failure fallback :69–71, don't-commit-or-push :77); (5) Skill framing removed ("the mistakes this content exists to prevent"), `tsc` → "type-check", `Agent`/`SendMessage` → portable "resuming an existing invocation", `model` switch → "different cost tier", and a portable "Why orchestration lives in the main thread" rationale retained at :63–65. **Re-verified 2026-07-26 (post-fix):** 3 `HUMAN GATE` markers unchanged, all 5 hard rules intact, "three human gates"/"not a fourth gate"/"(auto, non-gated)" all still present; the new `## Metadata` block (AL-7 fix) added `id`/`purpose` above the blockquote without disturbing the pipeline, gates, or hard rules, and the blockquote still disclaims Skill framing. Contract item 5 ("free of Claude-Code-Skill-only framing and any project/stack residue") remains satisfied — the restored `AskUserQuestion` at :53 is a tool name, not Skill framing nor project/stack residue, so it is scored under R7/C18 (AL-9) rather than here. |
| C9 | Spec-schema files match the architect's inline template shape; architect references them without contradiction | PASS | Programmatic extraction + `diff -u` of each fenced template body against the matching inline block in `.claude/agents/sdd-architect.md`: `intent.md`, `roadmap.md`, `tasks.md` (incl. the `[ ] [x] [~] [!]` legend) are byte-identical; `contract.md` differs only by neutralizing "Never use **Python** (or any other placeholder language)" → "Never default to a placeholder language" and "Step 2's exploration" → "codebase exploration" (all headings, tables, placeholders, and language-fence markers identical) — a correct application of the no-drift rule to a now-standalone file; `audit.md` is a superset (adds Final Verdict). Non-contradiction verified: the canonical architect no longer inlines any template, it points at the schema templates and instructs "do not contradict them", so there is exactly one source of truth per artifact (error-handling row 5). **Re-verified 2026-07-26 (post-fix):** all five `spec-schema/*.md` are byte-for-byte unchanged from the first audit (diffed against my retained extracts), and intent/roadmap/tasks remain byte-identical to the architect's inline blocks. The Final Verdict superset is now **explicitly sanctioned** by the contract itself — `contract.md` gained a 4-line "Amendment (C9 clarification)" in the Spec-schema content contract stating `spec-schema/audit.md` MAY carry a trailing Final Verdict section without violating "identical in shape" (AL-2 closed at the spec level, as recommended). |
| C10 | Behavior Guarantee 1 — Portability invariant (no tools:/model-id/Claude-path as source of truth) | PASS (with note) | **Re-verified 2026-07-26 (post-fix).** Sweeps still return `(none)` for `\b(opus\|sonnet\|haiku)\b`, `^(tools\|model\|color\|name\|description):`, `<example>`, `SKILL\.md`, `\.claude`. No file starts with a YAML frontmatter fence; model need is expressed only as `cost_tier`, permissions only as `capabilities`. Scored PASS because the guarantee enumerates exactly three things — a `tools:` array, a concrete model id, and a Claude-only path — and none is present. **Rev 3:** the extended Claude-tool-name sweep's single hit (conductor:53) is now a qualified illustration behind a generic behavior statement, so the guarantee's stated purpose (G3, "no Claude-Code-only mechanics baked in as the sole source of truth") is satisfied in substance as well as by its literal enumeration. AL-9 closed. |
| C11 | Behavior Guarantee 2 — No-drift invariant (no AI-Radar/Python-uv-Bedrock/LangGraph nor mr-engine/Vitest/Supabase/Db/renderToStaticMarkup residue) | PASS | Case-insensitive sweep of the full residue word list plus extras (`tailwind`, `shadcn`, `Radix`, `.live.test`, `SUPABASE_TEST`, `IVA`, `curation`, `card.rank`, `npm test`, `npx `, `tsc`, `poetry`, `pnpm`, `yarn`) → zero hits. Independently confirmed the executor's `CONVENTIONS` claim: every match is the lowercase English word "conventions" (or the heading "Test Conventions"); a literal `CONVENTIONS.md` search returns nothing. `Python` appears only inside an enumerated multi-language example list, never as the assumed stack. |
| C12 | Behavior Guarantee 3 — Improvement-preservation invariant | PASS | All four improvements located by grep and read in context (see R5). Additionally verified that the portable high-value-tests reference actually carries the rubric's substance (the "one question", the Yes/Yes–No–Yes/No decision table, and the five don't-write categories) rather than a bare pointer, satisfying the error-handling row "keep a portable reference … do not inline or vendor the skill file". |
| C13 | Behavior Guarantee 4 — Gate invariant (exactly 3 original human gates; documentation non-gated) | PASS | `grep -c 'HUMAN GATE'` → 3, at review-specs / review-tests / review-audit — the same three points as the untouched source conductor. Line 25 states "three human gates" and "Documentation is **not** a fourth gate"; documentation appears only under "FLOW automatically" and in the pipeline block tagged "(auto, non-gated)". `sdd-documentation.md:11` independently repeats "not a fourth human gate". No gate added, removed, or relocated. |
| C14 | Behavior Guarantee 5 — Documentation-fidelity invariant (8 requirements; never authorizes touching code comments/docstrings) | PASS | **Re-verified 2026-07-26 (post-fix)** — all 8 still encoded after the AL-3 edit (R6/C7), and the edit touched only the ARCHITECTURE/AGENTS bootstrap wording, leaving requirements 1–2 and 4–8 textually untouched. Scope guard is explicit and doubly stated at `sdd-documentation.md:52`: "Never touch inline code comments or docstrings … never to source files". Capability set is `read-files, write-files, run-shell` with the body constraining writes to README/CHANGELOG/ARCHITECTURE-AGENTS/`intent.md` header. No text anywhere in the file authorizes editing source. |
| C15 | Behavior Guarantee 6 — Manifest-completeness invariant (all 11 files exist and non-empty) | PASS | `find templates -type f \| wc -l` → 11; `find templates -type f -size 0 \| wc -l` → 0; smallest file is 1,002 bytes (`spec-schema/intent.md`) and every file was read end to end during this audit. |
| C16 | Behavior Guarantee 7 — Reflexive traceability across this feature's specs | PASS | See R8. Additionally: no orphan rows in this audit — every R cites an intent goal/criterion and every C cites a contract section or numbered guarantee; every contract guarantee 1–7 has a corresponding C-row (C10–C16). |
| C17 | Integration Points MCP-provisioning note present, marked informational/forward-reference only and out of scope; adds no file to the manifest | PASS | `contract.md:232–247` carries the note with the explicit "forward reference — NOT implemented by this feature", "explicitly out of scope", and "adds no file to this feature's manifest" qualifiers plus the 5-tool table. Manifest check confirms zero MCP config files (`.mcp.json`, `mcp.json`, `config.toml`) were created anywhere in the repo. |
| C18 | Error-handling contract rows honored (source conflicts, dropped Claude-only lines logged, high-value-tests kept as portable reference not vendored, no contradictory duplicate copies) | PASS | Row 1 (source conflict): PASS — the repo draft's improvements win, production wording otherwise, project text neutralized (verified by three-way diff). Row 3 (high-value-tests): PASS — rubric described portably, skill file not vendored; `templates/` contains no `high-value-tests` file. Row 4 (documentation ambiguity): PASS — fixed 8-point design encoded without redesign. Row 5 (no duplicate copies): PASS — templates exist only under `spec-schema/`, the architect references them. Row 2 (drop-and-log): **PASS as of rev 3.** Both halves of the row are now satisfied. The drop is disclosed — `tasks.md` Notes carries a "Correction (post-audit, AL-1)" retraction and this log records it. The line itself is now handled by the row's first prescribed option, "rewrite it as a capability/tier/behavior statement": `conductor/sdd-conductor.md:53` states the behavior generically ("Use the tool's structured ask-the-human mechanism") and keeps `AskUserQuestion` only as an attributed per-tool example — the same shape as the contract's own sanctioned "Context7 (or the target tool's equivalent docs-lookup MCP)" formulation. All five error-handling rows now honored. |

## Test Coverage
> Verification checks (content feature — structural/grep sweeps, not executable tests).
> No executable test suite exists or is expected: the repo has no dependency manifest
> (`package.json` / `pyproject.toml` / `go.mod` / `Cargo.toml` / `Makefile`), consistent
> with the contract's "External packages: none (pure Markdown authoring)". Every row below
> was executed independently by the auditor; the "Evidence" column records the check run.
| ID | Test / Verification Description | Status | Evidence (check run by the auditor) |
|---|---|---|---|
| T1 | Manifest check: all 11 target files exist and are non-empty | PASS | `find templates -type f \| sort` → the 11 contracted paths exactly; `find templates -type f -size 0 \| wc -l` → 0; `find templates -type f ! -name '*.md' \| wc -l` → 0. |
| T2 | Portability grep sweep finds no `opus\|sonnet\|haiku`, `tools:`, `color:`, `<example>`, `SKILL.md`, `.claude/skills/` as a source of truth | PASS for the listed patterns; **1 new hit on the extended pattern** | `grep -rniE '\b(opus\|sonnet\|haiku)\b'`, `grep -rnE '^(tools\|model\|color\|name\|description):'`, `grep -rn '<example>'`, `grep -rn 'SKILL\.md\|\.claude'` over `templates/` → all `(none)`, re-run post-fix. Per-file `head -1` scan confirms no YAML frontmatter fence. The extended Claude-tool-name sweep I add beyond this row's literal list — `grep -rn 'AskUserQuestion\|SendMessage\|NotebookEdit\|WebFetch\|TaskCreate\|TaskUpdate\|ListMcpResources\|mcp__'` — returns exactly 1 hit, `conductor/sdd-conductor.md:53`, where the tool name appears as an attributed example ("e.g. `AskUserQuestion` on Claude Code") behind a generic behavior statement, not as the sole mechanism. Judged PASS: this is the same shape as the contract-sanctioned Context7 phrasing. |
| T3 | No-drift grep sweep finds no `AI Radar\|uv \|Bedrock\|LangGraph\|Vitest\|Supabase\|mr-engine\|renderToStaticMarkup\|CONVENTIONS` | PASS | `grep -rniE 'AI Radar\|Bedrock\|LangGraph\|Vitest\|Supabase\|mr-engine\|renderToStaticMarkup\|CONVENTIONS\|tailwind\|shadcn\|Radix\|npm test\|npx \|tsc\b\|\.live\.test\|SUPABASE_TEST\|IVA\|curation\|card.rank'` + `grep -rn 'uv '` + `grep -rniE '\bpython\b\|\bpoetry\b\|\bpnpm\b\|\byarn\b'` over `templates/`. Only hits are the English word "conventions" and the multi-language example list — both confirmed benign by reading each hit in context and by a literal `CONVENTIONS.md` search returning nothing. |
| T4 | Improvement-preservation check: red-first ordering, docstring spec linkage, high-value-tests reference, generalized Project Context all present | PASS | `grep -rn -i 'red-first\|red-phase\|BEFORE the executor' roles/sdd-test-writer.md` → :11,:63,:85; `grep -n 'Do NOT put contract IDs\|Spec-linked header'` → :66,:67; `grep -rn 'high-value-tests'` → :34; `grep -rn '### Project Context' roles/` → architect:17, executor:17. Each hit read in full context. |
| T5 | Role Metadata schema consistency across all 5 role files (all required keys present, valid enum values) | PASS | Per-file loop over `templates/roles/*.md` printing the H1, all `## ` headings, and the ordered `- <key>:` sequence → all 5 emit `id, purpose, cost_tier, cost_rationale, capabilities, invocation, handoff` in contract order under `## Role Metadata` (line 3), followed by `## Role body` (line 13). cost_tier/capabilities values cross-checked against the per-role table (C3, C4). |
| T6 | Documentation-fidelity check: each of the 8 sdd-documentation requirements located in `sdd-documentation.md` | PASS | Eight targeted greps (tier+rationale / trigger+REJECTED / three inputs / four outputs incl. `Shipped: <date>` and "Do NOT move" / bootstrap / verified-only / never-touch-comments / change summary) each returned the expected line; whole file also read line by line. |
| T7 | Gate check: conductor names exactly the 3 human gates and marks documentation as non-gated auto-flow | PASS | `grep -c 'HUMAN GATE' conductor/sdd-conductor.md` → 3 (:16,:18,:21); `grep -i 'three human gates\|not a fourth gate\|non-gated'` → :22,:25,:49; documentation appears under "FLOW automatically" (:49), never under "PAUSE for the human" (:41–45). |
| T8 | Docs-lookup rule check: Context7-before-pinning instruction present in architect, executor, test-writer bodies | PASS | `grep -rn 'Context7' templates/roles/` → exactly 3 hits, one per docs-lookup role (architect:22, executor:24, test-writer:37), each with the "or the target tool's equivalent docs-lookup MCP" portability clause; no hit in auditor or documentation (which do not declare `docs-lookup`). |
| T9 | Spec-schema fidelity: each `spec-schema/*.md` structurally matches the architect's inline template (headings, tables, tasks legend) | PASS | Python extraction of the 5 fenced ` ```markdown ` blocks from `.claude/agents/sdd-architect.md` and the fenced body of each `spec-schema/*.md`, then `diff -u` per pair: intent/roadmap/tasks IDENTICAL (tasks legend `[ ] [x] [~] [!]` byte-identical); contract = neutralized prose only, structure identical; audit = superset with an appended Final Verdict section. **Re-run post-fix**: all five extracts diff clean against my retained first-audit extracts (`UNCHANGED` ×5), so no fix touched `spec-schema/`; the Final Verdict superset is now contract-sanctioned by the C9 amendment (AL-2). |

## Audit Log
| Date | Auditor | Finding | Severity | Resolution |
|---|---|---|---|---|
| 2026-07-26 | sdd-auditor | **AL-1 — One Claude-only line WAS dropped without a portable equivalent, and was not logged.** The source conductor's bullet `- Any decision only the human can make: … Use \`AskUserQuestion\` for these.` lost its final sentence in `templates/conductor/sdd-conductor.md:45`. `AskUserQuestion` is a Claude-Code-only tool name, so removing it is correct per error-handling row 2 — but that row also requires the drop be noted in `audit.md`, and it was not; `tasks.md` Notes and Task 5.7 instead assert that no such drop occurred. The executor's claim #1 is therefore **not accurate as stated**. (Verified by paragraph-normalized diff of the draft vs. canonical conductor; no other draft or production line was found to be dropped without an equivalent — the remaining Claude mechanics `tools:`/`model:`/`color:`/`<example>`/`SKILL.md` path/`Agent`+`SendMessage`/`tsc` all have documented portable replacements.) | LOW | Logged here, satisfying error-handling row 2 and closing Task 5.7. No file change required for compliance. Recommended (non-blocking): restore a portable one-liner such as "use the tool's structured ask-the-human mechanism for these, if it has one", since a generic equivalent does exist. |
| 2026-07-26 | sdd-auditor | **AL-2 — `templates/spec-schema/audit.md` adds a "Final Verdict" section absent from the architect's inline `audit.md` template** (Step 3d of `.claude/agents/sdd-architect.md` stops after "Audit Log"). Weighed both sides: (a) against — C9 says the schema files contain "the blank template body currently inlined in the architect prompt"; (b) for — the addition is a superset, not a contradiction or omission; `tasks.md` Task 1.6 explicitly enumerates "final verdict" as part of the audit template to extract; the canonical `sdd-auditor.md` Step 7 mandates adding exactly this block with exactly this verdict enum, so every real `audit.md` acquires one; C9's stated purpose ("so the extracted schema and the architect's emitted output stay identical in shape") is still met because the canonical architect now defers to `spec-schema/` rather than carrying a rival inline copy; and this feature's own `audit.md` (architect-produced) already has the section. | LOW | **Accepted as a deliberate, correct deviation** — not a contract violation: the required body is present in full, the addition is additive and consistent with the auditor role's mandated output, and the specific instruction (Task 1.6) resolves the tension with C9's general "currently inlined" phrasing. C9 recorded PASS with this note. Recommended (non-blocking, spec-side): amend C9's wording in a future spec revision to say "contains at least the inlined body, plus the Final Verdict block the auditor role emits", so the two spec artifacts stop disagreeing. |
| 2026-07-26 | sdd-auditor | **AL-3 — Contradictory instruction inside `sdd-documentation.md` about creating `ARCHITECTURE.md`.** Step 3 item 3 (:42) says "If neither file exists, do not create one solely for this step", while Step 4 (:47) says "If `CHANGELOG.md` or `ARCHITECTURE.md` do not exist anywhere in the project, do not skip them — create minimal starting versions". For the same state (no `ARCHITECTURE.md` and no `AGENTS.md`) the file gives opposite directions; the bridging parenthetical ("for the case where one is expected but missing project-wide") does not define when that case applies. Root cause is contract-level: contract items 4 ("if present") and 5 ("create if missing") are themselves in tension, and the error-handling row directs the executor to encode the fixed 8-point design without redesigning it — which it did faithfully. | MEDIUM | Report, don't fix (auditor rule). Recommend the executor reconcile the two sentences in `templates/roles/sdd-documentation.md` — e.g. make Step 3 item 3 read "update it if present; if absent, see the bootstrap rule" — after the spec owner decides which behavior is intended. Not blocking: C7/C14 remain PASS because both contract items are encoded, and Step 4 is the more specific rule a downstream role would follow. |
| 2026-07-26 | sdd-auditor | **AL-4 — `CLAUDE.md` is the only project-conventions file named, in two supposedly tool-agnostic role bodies** (`sdd-architect.md:19`, `sdd-executor.md:19`). This is explicitly sanctioned by the per-role content table ("read `CLAUDE.md` if present else derive"), and both occurrences have a real fallback, so it is not a *sole* source of truth and C10/R4 pass. Still, `CLAUDE.md` is a Claude-Code-specific filename in content whose whole purpose is to serve 5 tools; the portable equivalent (`AGENTS.md`) is already used elsewhere in the same template set (`sdd-documentation.md:42`) and in `plan.md`. | MEDIUM | Report, don't fix. Recommend broadening to "the project's agent-conventions file (`CLAUDE.md`, `AGENTS.md`, or the target tool's equivalent), if one exists" in both roles — a wording change that would need a matching one-line amendment to the contract's per-role table. Not blocking. |
| 2026-07-26 | sdd-auditor | **AL-5 — Bare relative references to `spec-schema/*` may dangle once generated.** `sdd-architect.md:50–58` instructs "Follow `spec-schema/intent.md`" etc. Inside `templates/` this resolves; but a per-tool generator emits the role body into a user's repo (e.g. `.claude/agents/`, `.cursor/agents/`), where no `spec-schema/` directory necessarily exists, leaving the architect pointing at nothing. The contract explicitly permits the reference approach ("The architect body MAY then reference these schema files"), so this is compliant today — it is a forward-compatibility hazard for the not-yet-built generator. | LOW | Report only. Recommend recording (in this feature's notes or the future generator's spec) that generators must either copy `spec-schema/*` into the target repo or inline the schema at generation time. No change required to this feature's deliverable. |
| 2026-07-26 | sdd-auditor | **AL-6 — Task 5.7 marked `[x]` for work that was deliberately not performed.** The task reads "Log any dropped Claude-only lines or unresolved items in `audit.md`"; the executor correctly declined to write `audit.md` (the auditor's file) but recorded the task as Completed rather than Blocked/deferred. The inline note makes the deferral transparent, so no information was concealed — but per the auditor's own Step 5 criteria, a task that was not executed should carry `[!]` (or `[~]`) with the explanation. | LOW | No fix required — this audit performs the deferred logging (AL-1, AL-2), so Task 5.7's substance is now discharged. Noted for pipeline hygiene: prefer `[!]`/`[~]` + reason over `[x]` + reason when a task is handed to a different role. |
| 2026-07-26 | sdd-auditor | **AL-7 — `templates/conductor/sdd-conductor.md` carries no Role Metadata block.** The contract's Data Model binds only "Every file in `templates/roles/`", and the manifest note requires only that the conductor be plain portable content and not a Skill — so this is contract-compliant. Consequence: a future generator gets no `id`/`cost_tier`/`capabilities`/`handoff` for the conductor and must special-case it, even though `harness init` (plan.md §4) offers "model per role" with the conductor always on. Related, minor: the auditor's `capabilities` value embeds a free-text qualifier — `write-files (audit.md only)` — inside an otherwise pure token list, which a strict parser will need to tolerate. | LOW | Report only; both are as-specified. Recommend a follow-up spec decide whether the conductor gets its own metadata block and whether capability scoping should be a structured field rather than a parenthetical. |
| 2026-07-26 | sdd-auditor | **AL-1 RE-VERIFIED → PARTIALLY RESOLVED.** The disclosure half is fixed: `tasks.md` Notes now carries a "Correction (post-audit, AL-1)" entry retracting the false "nothing was dropped" claim, and Task 5.7 is reworded. The remedy half is not: `templates/conductor/sdd-conductor.md:53` now restores `Use \`AskUserQuestion\` for these.` **verbatim**, and `tasks.md` justifies this on the grounds that "`AskUserQuestion` is a generic tool-call convention (not Claude-specific)". I checked that claim against the repo's own evidence and it does not hold — see AL-9. | MEDIUM | Superseded by AL-9. The logging obligation of error-handling row 2 is discharged; the rewrite-or-drop obligation is not. |
| 2026-07-26 | sdd-auditor | **AL-2 RE-VERIFIED → RESOLVED (spec-side, as recommended).** `contract.md` gained exactly the described 4-line "**Amendment (C9 clarification)**" in the Spec-schema content contract (file grew 247 → 251 lines), sanctioning a trailing Final Verdict section in `spec-schema/audit.md` as a superset that does not violate "identical in shape". Re-diffed all five `spec-schema/*.md` against my retained first-audit extracts: `UNCHANGED` ×5 — the amendment adjusted the spec to match the deliverable rather than churning the deliverable, which is the right direction given the deliverable was judged correct. | LOW | Closed. C9 remains PASS, now with explicit contract backing instead of an auditor-accepted deviation. |
| 2026-07-26 | sdd-auditor | **AL-3 RE-VERIFIED → RESOLVED.** `grep -n 'solely for this step' templates/roles/sdd-documentation.md` → no match. Output item 3 (:42) now reads "**`ARCHITECTURE.md` or `AGENTS.md`** — update the relevant section … If neither exists yet, see the bootstrap rule below", making Step 4 (:47) the single source of truth. Re-ran the full 8-requirement fidelity check (16 greps): all still present, so resolving the contradiction cost no contracted content. Contract items 4 ("if present") and 5 ("bootstrap if missing") both remain honored. | MEDIUM | Closed. C7 and C14 remain PASS, warning withdrawn. |
| 2026-07-26 | sdd-auditor | **AL-4 RE-VERIFIED → RESOLVED.** Both occurrences generalized: `sdd-architect.md:19` "Read `CLAUDE.md`, `AGENTS.md`, or this project's equivalent conventions doc (repo root) first if one exists"; `sdd-executor.md:19` same construction. No Claude-Code-specific filename is now named as the only conventions doc anywhere in `templates/`; the fallback-to-exploration clause is preserved in both. | MEDIUM | Closed. Warning withdrawn. |
| 2026-07-26 | sdd-auditor | **AL-5 RE-VERIFIED → RESOLVED.** `sdd-architect.md:50` now names the canonical location and assigns deployment responsibility explicitly — "canonically packaged at `templates/spec-schema/` alongside this role; a per-tool deployment of this role is responsible for keeping these schema templates reachable — inlined, bundled, or referenced by whatever path that tool's packaging uses" — and :52–56 reference "the `intent` schema template" etc. rather than bare relative paths that would dangle once emitted into a user repo. The non-contradiction instruction ("do not contradict them") is retained, so C9's second clause still holds. | LOW | Closed. |
| 2026-07-26 | sdd-auditor | **AL-6 RE-VERIFIED → RESOLVED.** Task 5.7 is rewritten to scope the task to what the executor can legitimately do ("Identify/sweep for dropped Claude-only lines or unresolved items, for the auditor to log formally"), records the actual finding, and states that the formal `audit.md` entry is intentionally left to the auditor because `audit.md` is not this role's file. Remaining `[x]` is now accurate for the redefined scope. All 29 tasks still `[x]`, no `[!]`, no silent skips. | LOW | Closed. |
| 2026-07-26 | sdd-auditor | **AL-7 RE-VERIFIED → RESOLVED.** `templates/conductor/sdd-conductor.md:3–6` adds `## Metadata` with `id: sdd-conductor` and a `purpose:` line, and the blockquote (:12–15) explains the deliberate omission of `cost_tier`/`capabilities`/`invocation`/`handoff` because the conductor is not a subordinate pipeline role. Confirmed this broke nothing: 3 `HUMAN GATE` markers, all 5 hard rules, the five-role pipeline block, and the Skill-framing disclaimer are all intact; the 5 `roles/*.md` still carry all 7 keys under `## Role Metadata` at line 3 with `## Role body` at line 13. Contract-wise this remains optional (the Data Model binds only `templates/roles/`), so it is an improvement, not a newly required element. The auditor's free-text `write-files (audit.md only)` qualifier is unchanged and still merits a structured field eventually. | LOW | Closed. |
| 2026-07-26 | sdd-auditor | **AL-9 (NEW, introduced by the AL-1 fix) — a Claude-Code-only tool name is now baked into canonical content with no portable qualifier.** `templates/conductor/sdd-conductor.md:53` reads "Use `AskUserQuestion` for these." The executor's stated justification in `tasks.md` — "`AskUserQuestion` is a generic tool-call convention (not Claude-specific)" — is contradicted by this repo's own evidence: (1) across every source, the string occurs **only** in `.claude/skills/sdd-conductor/SKILL.md:35` and its byte-identical `mr-engine-app` production copy — both Claude Code Skill files; (2) `plan.md:30` cites it specifically as *the* Claude Code mechanic that forces the conductor to be a Skill ("it needs to live in your main thread so it can pause, ask for your approval (`AskUserQuestion`)"); (3) it is absent from all five rows of plan.md §3's cross-tool portability table; (4) it is PascalCase in the same family as `Read`/`Write`/`Bash`/`Glob`/`WebFetch`/`NotebookEdit`/`TaskCreate`/`ListMcpResourcesTool` — every one of which this same executor correctly stripped as a Claude-only mechanic; (5) contract.md states a wrapper "maps each token to the target tool's real tool/permission names", i.e. tool names are per-tool by definition and never canonical; (6) the `capabilities` vocabulary contains no ask-the-human token, so nothing in the contract backs this instruction. Violates intent SC7/R7 ("no … single agent tool as the only possibility") and error-handling row 2. Contrast the correct pattern already used three times in this same template set: "Context7 (**or the target tool's equivalent docs-lookup MCP**)". | MEDIUM | **RESOLVED 2026-07-26 (rev 3).** The recommended edit was applied verbatim to `conductor/sdd-conductor.md:53`: "Use the tool's structured ask-the-human mechanism (e.g. `AskUserQuestion` on Claude Code) for these." Re-verified independently: the generic behavior leads, the tool name survives only as an attributed example, the originally-dropped instruction is restored in substance, and the construction matches the contract's sanctioned Context7 pattern. R7 → PASS, C18 row 2 → PASS, C10 note cleared. Confirmed the edit touched nothing else: manifest 11/0/0, no-drift `(none)`, 3 gates + 5 hard rules, 7 metadata keys × 5 roles, 3 docs-lookup roles, doc-fidelity contradiction still absent, all 5 `spec-schema/*.md` byte-identical to my rev-1 extracts, `.claude/` and `mr-engine-app` untouched, repo still 25 files. |
| 2026-07-26 | sdd-auditor | **AL-8 — Scope-creep and read-only-input checks clean (re-confirmed post-fix).** `git status --porcelain` still shows only `?? .gitignore` (pre-existing) and `?? templates/`; repo file count unchanged at 25; no file created outside `templates/` and `specs/canonical-role-templates/`. `.claude/` has no file modified since 2026-07-22 and `mr-engine-app/.claude/` none since 2026-07-06 — both read-only inputs untouched. Specifically verified that the AL-1 fix did **not** edit the source: `.claude/skills/sdd-conductor/SKILL.md` is still byte-identical to the production copy. No CLI, generator, `agnostic-layer/`, demo app, per-tool wrapper, MCP config, or vendored `high-value-tests` skill was produced: every intent Non-Goal respected. No dependency manifest or lockfile exists in the repo, so no unspecified external package could be or was introduced. | LOW | Informational — no action. |

## Final Verdict

> **Revision 3 (definitive) — 2026-07-26.** Audit history: rev 1 (initial, 7 findings,
> APPROVED WITH RESERVATIONS) → rev 2 (after the architect's C9 amendment and the
> executor's six fixes; six closed, one regression found, APPROVED WITH RESERVATIONS) →
> rev 3 (after the AL-9 fix; all findings closed). Every revision re-ran the checks from
> scratch rather than reading the fix reports; no status in this file rests on a
> self-report by the role that produced the work.

**Status**: APPROVED

**Summary**: All 9 requirements, 18 contract items, and 9 verification checks now pass, and all 9 audit-log findings are closed — 8 by fixes I independently re-verified, 1 (AL-8) informational. The canonical `templates/` tree is a faithful, portable, drift-free realization of the contract: 11 files, exactly as specified, with no Claude-Code mechanic, stack residue, or project residue serving as a source of truth anywhere.

**Critical Issues** (must fix before merge):
- None.

**Warnings** (should fix, not blocking):
- None. All three warnings from earlier revisions (AL-3 documentation contradiction, AL-4 `CLAUDE.md`-only naming, AL-9 `AskUserQuestion` portability regression) are fixed and re-verified.

**Recommendations** (nice to have — forward-looking, none blocking this feature):
- Consider adding an `ask-human` (or similarly named) token to the contract's `capabilities` vocabulary. The AL-1/AL-9 sequence exposed a real gap: the conductor's most important behavior — pausing for a human at three gates — has no capability token backing it, while `docs-lookup` does. Worth settling before the per-tool generators start mapping capabilities to tool permissions.
- Express scoped write access (`write-files (audit.md only)`) as a structured field rather than a free-text parenthetical inside an otherwise pure token list, so a strict generator parser doesn't have to special-case the auditor (AL-7).
- Carry forward the AL-5 note into the generator's own spec: `templates/spec-schema/*` must be copied into the target repo, bundled, or inlined at generation time, or the architect's schema-template references will not resolve once emitted.
- Out of scope for this feature, but worth a decision: the repo's `.gitignore` excludes `specs/`, `.claude/`, and `plan.md`, so this pipeline's own source of truth — including this audit — is not version-controlled.

**Ready for the human sign-off gate.** Per the pipeline's own rules, this verdict is a recommendation to the human, not an approval on their behalf; documentation should run only after the human accepts it.
