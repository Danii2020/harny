# Audit: monorepo-mode

> Rows seeded PENDING by the architect. The auditing role fills in Status,
> Notes/Verified By after implementation. Every row traces to an `intent.md`
> requirement or a `contract.md` guarantee, and every guarantee has at least one row.

## Requirements Checklist

| ID | Requirement | Source | Status | Notes |
|---|---|---|---|---|
| R1 | One install covers N components: exactly one `.sdd/`, one spec-schema set, one copy of every role and skill per distinct skill root, one runner, one `probes.mjs`, one hook config per tool, one CI workflow | intent.md SC1, G1 | PASS | Verified by direct execution, not only by test: `init --component .=python --component apps/web=typescript` against a scratch git repo wrote **31 files**, exactly one of each singleton family. Confirmed by `tests/e2e-init.test.ts` T36 block |
| R2 | A `components`-free config produces a write plan whose path set and whose every file's bytes are identical to the pre-change build, asserted by golden content | intent.md SC2, G2 | PASS | Goldens confirmed to be genuine pre-feature captures: `tests/fixtures/golden/monorepo-mode/ts-root/.sdd/{feedback/run-feedback.mjs,doctor/run-doctor.mjs}` are byte-identical to `git show HEAD:templates/…`. Guard confirmed **non-vacuous by perturbation**: appending two bytes to `templates/shared/probes.mjs` turned both T1 and T2 red (`probes.mjs differs from its golden byte-for-byte`); rewriting a non-comment line of `templates/ci/harny-feedback.yml` turned T1 red on the single-contiguous-insertion assertion. Repo restored clean after both. The three-file exception list is itself capped and presence-checked |
| R3 | This repo's own `.sdd/**`, `.claude/settings.json` and `.github/workflows/harny-feedback.yml` stay byte-identical to fresh `init` output (FC-13), except the contracted workflow header comment | intent.md SC3 | PASS | `templates/hooks/run-feedback.mjs` ≡ `.sdd/feedback/run-feedback.mjs`, `templates/doctor/run-doctor.mjs` ≡ `.sdd/doctor/run-doctor.mjs`, `templates/shared/probes.mjs` ≡ `.sdd/shared/probes.mjs`, all verified by `diff`. `.github/workflows/harny-feedback.yml` diff is the ten-line comment insertion only |
| R4 | An existing `.sdd/harness.json` carrying only `stack` loads, validates, merges, re-serializes and round-trips byte-identically; never migrated, never warned about | intent.md SC4 | PASS | `serializeConfig` emits `components` only when present and non-empty, appended after `stack`; no migration path exists. Covered by T8 and the dedicated pre-feature-config test in `tests/config.test.ts` |
| R5 | Two-component turn dispatch: `eslint` gets only the `apps/web` file with cwd `apps/web`; `ruff` gets only the root file with cwd the install directory; neither sees the other's | intent.md SC5, G3 | PASS | `tests/hooks/run-feedback.test.ts:1422` asserts both argv AND `cwd` (via the fixture's new `cwd` field), and asserts each command's argv excludes the other component's file |
| R6 | `apps/web-admin/x.ts` resolves to `apps/web-admin`, never to `apps/web` (segment boundary, not string prefix) | intent.md SC6 | PASS | `componentDirFor` matches on `rel === d \|\| rel.startsWith(d + '/')`, never bare `startsWith(d)`; `tests/hooks/run-feedback.test.ts:1321` is a positive assertion (`['WEB_ADMIN']`), not a zero-assertion |
| R7 | A touched path belonging to no declared component runs no command and produces exactly one notice line naming the count | intent.md SC7 | PASS | Notice text matches MC-11 verbatim. See F10 (LOW) for a strictness caveat on MC-11's "N is always 0" claim |
| R8 | A `whole-project` command runs once per component with at least one assigned touched path, from that component's directory, and not at all for components with none | intent.md SC8 | PASS | Verified by code reading (`runCommand` drops path args for non-`per-file`) and by `tests/hooks/run-feedback.test.ts:1630` / `:1668` (the ADR 0041 extension-independence case) |
| R9 | The generated workflow for a two-component install has one job, one runner step, at most one install step per component; no `strategy:`, `matrix:`, second workflow, `on.*.paths`, or `defaults.run.working-directory` | intent.md SC9, G4 | PASS | Verified by direct execution: the two-component workflow has one `Install dependencies (TypeScript / Node — apps/web)` step with `working-directory: "apps/web"`, one `harny feedback (Python, TypeScript / Node)` runner step, and none of the six forbidden constructs |
| R10 | `npx harny doctor` on a two-component install runs each component's test command from that component's directory, and the direct runner invocation agrees | intent.md SC10, G5 | **PASS (round 2)** | Was PARTIAL in round 1. Correct for ≥2 components, for a lone `.` component, and — after F1's fix — for a lone non-`.` component. The new e2e guard is the strong form: root `package.json` declares only `build` while `apps/web`'s declares `test` with a script that writes a marker into its own cwd, so the two directories give deliberately **opposite** answers; the test asserts `OK npm-test:apps/web`, marker present in `apps/web`, absent at root. Both directions perturbation-proven |
| R11 | Declaring both `stack` and `components` from one source is `USAGE`, naming both fields; nothing written | intent.md SC11 | **PARTIAL** | Enforced for the flag and `--config` sources (verified). **Not enforced for the third source SC11 names**, `.sdd/harness.json`, at its only production reader — see finding **F2 (HIGH)** |
| R12 | Duplicate normalized component paths, absolute paths, and escaping paths are each `USAGE`, naming the offender | intent.md SC12 | PASS | `normalizeComponentPath` rejects POSIX-absolute, `C:`-style, `..` and `../…`; `validateComponentList` rejects the empty list and duplicates-after-normalization. Reached from `init` at step 7 (`validateConfig`) for flag-supplied paths and at `loadConfigFile` for `--config` paths |
| R13 | An unrecognized component stack is inert, not fatal: all artifacts written, that component contributes no commands, one `io.warn` names component path and value | intent.md SC13 | PASS | `src/init.ts` step 9b loops `project.components`; one warn naming path + value + the built-in profile ids; blank stack not warned. Covered by T31 |
| R14 | The `Generator` interface gains no member and no generator file learns what a component is (grep gate over `src/generators/**`) | intent.md SC14, G7 | PASS | Independently re-run: `grep -ric component src/generators/*.ts` is **0** for all nine files except `markdown-yaml.ts` (9 hits), and all nine hits lie on lines 61–79, strictly between `export function renderProjectConfigBlock` (line 43) and the next `^export` (line 95). `src/generators/types.ts` is unmodified. See F5 for the `renderHook` expression deviation, which is against MC-15's literal form, not against SC14 |
| R15 | Interactive `init` asks repo shape first; "single repo" reproduces today's remaining question sequence; a `--stack`/`--component` flag presets and reports the shape question | intent.md SC15, G6 | PASS (with F11) | Preset/skip/`io.log` behavior correct; single-repo path reaches the unchanged stack question. The question is asked fourth (after tools/roles/skills), not literally first — satisfies G6's "before asking about stacks" and MC-25's operative clause, not MC-25's headline word. F11 (LOW) |
| R16 | `STACK_PROFILES`, `CommandSpec`, `FeedbackCommand` and `ReadinessCommand` carry no component vocabulary (grep gate over `src/feedback.ts`) | intent.md SC16, G7 | PASS | Independently re-run: zero case-insensitive `component` hits in `src/feedback.ts`; the file is not in `git status` at all |
| R17 | `ci-workflow-root` is extended and nothing of it is undone: `src/repo.ts` unmodified, ADR 0032's and ADR 0034's rules intact, one `root: 'repo'` assignment site | intent.md G8, contract.md MC-30 | PASS | `src/repo.ts` absent from `git status`. `root: 'repo'` has exactly one assignment site (`buildFeedbackFiles` in `src/engine.ts`). No `paths:` filter, no `defaults.run` added; the subdirectory slug path still produces `harny-feedback-apps-api.yml` (exercised live by T2) |
| R18 | Gap (3) and the ADR-registry ambiguity actually dissolve for a monorepo-mode install, as `intent.md` § Problem Statement asserts — verified against the write plan, not assumed | intent.md § Problem Statement | PASS | Verified against a real two-component write plan, not the claim: one `.sdd/`, one `run-feedback.mjs`, one `probes.mjs`, one skill root, one spec-schema set, one workflow ⇒ a fix to `templates/hooks/run-feedback.mjs` lands in exactly one scaffolded file, and there is exactly one `specs/`-facing schema set, hence one ADR sequence |

## Contract Compliance

| ID | Contract Item | Status | Verified By |
|---|---|---|---|
| C1 | MC-1 — `stack` and `components` mutually exclusive from one source; `USAGE`, no precedence rule, no silent winner | **PARTIAL** | The check lives at exactly one site, `mergeConfig`, and fires on `override` only. Flag source and `--config` source verified. `validateConfig` — the only reader of a real `.sdd/harness.json` (`src/doctor.ts:390`) — has **no** exclusivity check; hand-editing both fields into `.sdd/harness.json` and running `npx harny doctor` was accepted silently, with `components` winning. **F2 (HIGH)**. The error message names both fields but not the source, where the Error Handling row says "naming both fields **and the source**" |
| C2 | MC-2 — no component/path/dir/cwd field on `StackProfile`, `CommandSpec`, `FeedbackCommand` or `ReadinessCommand` | PASS | `src/feedback.ts` is unmodified (absent from `git status`); zero component vocabulary in it; `ScopedReadinessCommand` is declared in `src/doctor.ts` as an intersection type, leaving `ReadinessCommand`'s `extensions?: never` narrowing untouched |
| C3 | MC-3 — `resolveComponents` is total and never empty; no consumer branches on `config.components === undefined` | PASS | `resolveComponents` returns a one-element `.` list when `components` is absent or empty. No consumer reads `config.components` directly; every one reads `project.components` / `resolveComponents(config)`. (`renderProjectConfigBlock` is the one place where MC-3's erasure of declared-vs-implicit bites — see C16 / F4) |
| C4 | MC-4 — a `components`-free config round-trips byte-identically through load/validate/merge/serialize | PASS | `serializeConfig` gates on `components !== undefined && length > 0`; a `.sdd/harness.json` written for `--stack typescript` is byte-identical to its golden (T1/T2 compare it, and it is **not** in the three-file exception list) |
| C5 | MC-5 — byte-identity by construction: no `if (isSingleRepo)` guard at any rendering site | **PARTIAL (improved in round 2)** | The *outcome* is proven mechanically (T1/T2, verified non-vacuous). The *mechanism* still is not as MC-5's letter states — guards exist at rendering sites — but round 2 removed the part that mattered: the guards are no longer four re-derived expressions in three spellings; they are two **named, exported, documented predicates** with every site importing the one it means. MC-5 and MC-14 remain in genuine tension in the approved spec (MC-14 *mandates* different step-name text for >1 component, which no construction can avoid), so the residue is a spec-wording problem, not a code problem. **F7 downgraded MEDIUM → LOW**: the "four spellings" half is closed; only the "MC-5 says no branch while MC-14 requires one" half remains, and it wants an amendment, not an edit |
| C6 | MC-6 — every declared component reaches `CommandsPayload`, profile-less ones with `commands: []` | PASS | `buildCommandsPayload` maps every entry with `component.profile?.commands ?? []`. Note the CI call site deliberately passes only `resolvedComponents` (profile-less components contribute nothing to a `--whole-project` run, where MC-11's distinction cannot arise); the hook call site in `runInit` passes the full list |
| C7 | MC-7 — normalization, dedup and canonical ascending order applied before anything reads the list; ties therefore impossible | PASS | `validateComponentList` normalizes → rejects empty → rejects duplicates → sorts by codepoint. Reached for flag-supplied lists at `runInit` step 7 and for `--config` lists at `loadConfigFile`. Verified live: `--component .=python --component apps/web=typescript` serialized in canonical order |
| C8 | MC-8 — absolute and escaping component paths rejected; a non-existent component directory warns and proceeds | PASS | Both rejections in `normalizeComponentPath`; the missing-directory warning is a separate loop in step 9b that skips `'.'` and never creates the directory |
| C9 | MC-9 — longest **segment**-prefix match; `rel === d` or `rel.startsWith(d + '/')`, never bare `startsWith(d)` | PASS | Read in full in `templates/hooks/run-feedback.mjs` `componentDirFor`; segment count is `dir.split('/').length`, `'.'` scores 0. `canonicalPath` symlink resolution on both sides is an addition beyond the contract but is correctness-preserving and documented in-file |
| C10 | MC-10 — `'.'` is an ordinary, zero-segment component that wins only when nothing longer matches | PASS | `componentDirFor`, plus `tests/hooks/run-feedback.test.ts:1383` |
| C11 | MC-11 — unassigned paths dropped with exactly one count-naming notice; never reassigned, never silent; `N` is always 0 for a single `.` component | PASS (with F10) | Notice text matches the contract verbatim. The "N is always 0" clause is not strictly true: an out-of-`cwd` touched path now yields `undefined` and is dropped with the notice where it was previously passed through. Affects subdirectory installs only; no generated artifact changes. **F10 (LOW)** |
| C12 | MC-12 — every command's `spawnSync` cwd and its `requires` probe both use the component's directory; touched paths stay absolute | PASS | `requirementMet(command.requires, componentCwd)` then `runCommand(command, paths, componentCwd)` in both `runRunMode` and `runWholeProject`; `assignedPaths` are the accumulator's original absolute paths. The fixture's new `cwd` field makes this directly observable in tests |
| C13 | MC-13 — one job, one runner step scoped to `prefix`, per-component install steps scoped to `stepWorkingDirectory`; none of the six forbidden constructs present | PASS | Verified on a live two-component install and on a live single-non-root-component install; both render exactly one runner step and correctly scoped install steps |
| C14 | MC-14 — step names byte-identical for one component; widened form for >1; workflow header comment is the only canonical-region change | PASS | Single component: `Install dependencies (TypeScript / Node)` / `harny feedback (TypeScript / Node)`. Two components: `Install dependencies (TypeScript / Node — apps/web)` / `harny feedback (Python, TypeScript / Node)` (deduped, canonical order). The workflow's only other change is the ten-line comment block, mechanically proven single-contiguous-comment-insertion by T1/T2 |
| C15 | MC-15 — `Generator` interface unchanged; exactly one expression changed per generator | **PARTIAL** | `src/generators/types.ts` unmodified; exactly one expression changed per generator. The shipped expression is `payload.commands ?? (profile ? profile.commands : [])`, not MC-15's literal `payload.commands`. MC-15's *guarantee* holds in full; the deviation is in the expression. Ruled **acceptable as a recorded reservation** — see **F5 (MEDIUM)** for the ruling and the required follow-up |
| C16 | MC-16 — `renderProjectConfigBlock`'s existing lines unmoved and unchanged; component lines added only when components are declared | **PARTIAL** | Existing lines unmoved and byte-identical for a single-repo config; component lines correct for ≥2 components (verified live). But a declared **single `.` component** (`--component .=python`) renders **neither** a `Project stack:` line nor a `Component:` line — the block silently omits the stack entirely. **F4 (MEDIUM)** |
| C17 | MC-17 — a component with no assigned touched path runs nothing, per-file or whole-project | PASS | `runRunMode` `continue`s before the cwd resolution when `assignedPaths.length === 0`. The T15 test now creates `services/api` and additionally asserts the absence of the `component directory` notice, which closes the previously-vacuous "two possible causes" pass |
| C18 | MC-18 — whole-project gating is "at least one assigned path", not "at least one path passing an extension gate" | PASS | The extension gate is applied inside `perFilePathsFor`, strictly after component assignment and only for `per-file` commands. `tests/hooks/run-feedback.test.ts:1668` asserts the sibling-command independence directly |
| C19 | MC-19 — `--whole-project` covers every component; turn state untouched; summary line text unchanged with summed counters; finding still exits 2 unmediated | PASS | `runWholeProject` sums `totalCount`/`ranCount`/`skippedCount`; summary string unchanged; `process.exit(anyBlockingFinding ? 2 : 0)` unchanged and `stop_hook_active` still never read. For the legacy one-`.`-component payload `totalCount === commands.length`, so the text is byte-identical |
| C20 | MC-20 — per-component `dir` and `:<path>`-suffixed ids when >1 component; both omitted/unchanged for a single `.` component | **PASS (round 2)** | Was FAIL in round 1. `buildDoctorChecks` now reads `const scopedByComponent = !isSingleRootComponent(components)`, MC-20/MC-5's boundary imported from its one owner in `src/engine.ts`. Strictly a superset of the old behavior: identical for `length > 1` and for a lone `.`, corrected for a lone non-`.`. Confirmed load-bearing by perturbation — restoring `components.length > 1`, rebuilding and re-running turned **exactly** the two new tests red and nothing else. **One under-specified consequence to record**: MC-20's letter ties the `:<path>` id suffix to ">1 component", so a lone non-`.` component now gets `npm-test:apps/web` where MC-20's text implies `npm-test`. MC-20's two clauses never covered this case; MC-5 settles the `dir` half in favour of the shipped reading, and suffixing is harmless (uniqueness is not threatened, consistency is gained). `harny-sync` should amend MC-20's wording to the `isSingleRootComponent` boundary rather than leave the gap |
| C21 | MC-21 — `run-doctor.mjs` gains no literal; `checks.json` `version` stays `1`; a pre-feature `checks.json` behaves byte-identically | PASS | The only runner change is `const commandCwd = path.resolve(cwd, command.dir ?? '.')`, used for both the probe and the spawn. No component literal enters the runner. `version: 1` unchanged; `DoctorChecksFile` gains no field of its own |
| C22 | MC-22 — still five families, same order, families 1–4 unchanged in number and content | PASS | Only the `// 5. tests.` block changed in `templates/doctor/run-doctor.mjs`; `buildDoctorChecks` changed only the `commands` array construction |
| C23 | MC-23 — verb and direct invocation agree on a monorepo install (RD-7 preserved) | PASS (bounded by C20) | `runDoctor` re-derives components from the same `.sdd/harness.json`. Agreement holds because both paths read the same generated `checks.json` — which also means both agree on the *wrong* directory in F1's case |
| C24 | MC-24 — `--component` repeatable, first-`=`-wins, wholesale-replacing; `--component` + `--stack` is `USAGE` | PASS | `collectModel` accumulator; `parseComponentAssignment` splits on `indexOf('=')` and rejects `eqIndex <= 0`; `overrides.components` replaces wholesale. Verified live with two `--component` flags |
| C25 | MC-25 — shape question asked first, default single repo; single-repo transcript unchanged thereafter; flag preset skipped and reported via `io.log` | PASS (with F11) | `initialValue: SHAPE_SINGLE_REPO`; preset branch reports via `io.log`; the stack question's wording is untouched. Asked fourth, not first — **F11 (LOW)** |
| C26 | MC-26 — the path prompt's `validate` callback calls `normalizeComponentPath`; `prompts.ts` owns no rule | PASS | The callback delegates and surfaces the thrown message; the only local decision is "empty ends the loop", which is loop control, not a path rule |
| C27 | MC-27 — `runInit` still thirteen steps in the same order (CLI-1) | PASS | **Verified by code review as the test-writer requested.** `src/init.ts` carries numbered comments `1.` through `13.` at lines 87, 90, 93, 100, 107, 116, 134 (`6b.`, pre-existing), 149, 152, 161, 164 (`9b.`, pre-existing), 227, 247, 336, 340 — thirteen numbered steps plus the two pre-existing lettered sub-steps. Both new blocks (the per-component escape-hatch warning and the missing-directory warning) sit inside `9b.`; `buildCommandsPayload` sits inside `11.`. No step added, removed or reordered |
| C28 | MC-28 — one `io.warn` per unrecognized component stack naming the path; a blank component stack is not warned about | PASS | `if (component.stack && !component.profile)` — a blank stack is falsy and skipped. For a components-free config this is the same loop over the one implicit `.` component, not a second code path |
| C29 | MC-29 — no new runtime or dev dependency; no new `templates/**` file; template count still thirty-one; no new generated path | PASS (with F6) | `git diff HEAD -- package.json package-lock.json` is **empty**. No file added under `templates/`. A two-component install writes the same 31-path set as a single-repo install. However `tests/packaging.test.ts` — S4's own enforcement evidence, and marked **NOT MODIFIED** in the roadmap — *was* modified and weakened: **F6 (MEDIUM)** |
| C30 | MC-30 — `src/repo.ts` unmodified; ADR 0032 and ADR 0034 rules intact; exactly one `root: 'repo'` assignment site (WR-5 grep gate) | PASS | `src/repo.ts` absent from `git status`; one `root: 'repo'` site in `src/engine.ts`; no `paths:`/`defaults.run` introduced; the subdirectory slug path still exercised live by T2 |
| C31 | MC-31 — no per-component document, no per-component readiness entry, no manifest/directory heuristic | PASS | No per-component file in the write plan; `repoReadiness` entries unchanged in number; no manifest scan anywhere in the diff |
| C32 | Interfaces — `normalizeComponentPath`, `parseComponentAssignment`, `validateComponentList`, `resolveComponents`, `buildCommandsPayload`, `stepWorkingDirectory`, `ScopedReadinessCommand` exist with the declared signatures and doc comments | PASS | All seven read line by line against the contract's declarations; signatures, parameter names, return types and doc-comment content match, including `validateComponentList(raw: unknown, source: string)` and `stepWorkingDirectory`'s `'.' → ''` mapping |
| C33 | Data model — `serializeConfig` key order `version, tools, roles, gates, skills, stack?, components?`, `components` omitted when absent | PASS | Read in `serializeConfig`; confirmed live in a two-component `.sdd/harness.json`: `components` last, each entry `{ path }` or `{ path, stack }` in that field order |
| C34 | Data model — the runner wire format's three normalization rows behave exactly as tabled, including the tolerant `[]` fallback | PASS | `normalizeCommandsPayload` implements all three rows, including per-entry `dir`/`commands` defaulting and the object-without-`components` → `[]` row. All three covered by T12 |
| C35 | `CiPlacement` unchanged, and the departure from `ci-workflow-root` XC-6's anticipated mechanism is stated in `contract.md` and recorded as ADR 0040 — not a silent divergence | PASS | `CiPlacement` still `{ readonly prefix: string }`; `buildFeedbackFiles(payload, placement)` and `buildDoctorChecks(config, generators, placement)` keep their arity; `specs/monorepo-mode/decisions/0040-component-list-on-payload-not-ciplacement.md` exists with `Capability: cli-init` |
| C36 | Error Handling Contract — every row's stated behavior, exit code and message content observed | **PARTIAL** | 15 of 17 rows verified. Two gaps: (a) the `.sdd/harness.json` half of row 1 is unenforced at its real reader — **F2**; (b) the turn-mode half of the "component directory absent + `requires: {}`" row is **reachable and untested**, contrary to the reasoning recorded in T39 — **F3 (HIGH)**. Row 1's message also omits the source the row requires it to name |
| C37 | ADRs 0038–0042 exist, each with a `Capability:` field matching the capability it actually belongs to (not copied from this feature's own) | PASS | All five present. `Capability:` = `cli-init` (0038, 0040), `feedback-controls` (0039, 0041), `readiness-checks` (0042) — exactly the assignment roadmap step 4.9 specifies, and never `monorepo-mode` |
| C38 | Proposed amendments to `specs/current/` are complete: every widened statement in `feedback-controls.md`, `cli-init.md` and `readiness-checks.md` is listed, and none retires or contradicts an existing statement | PASS | 17 rows listed, all widenings. Spot-checked FC-4: `specs/current/feedback-controls.md:47` still reads "exactly six properties" while `templates/hooks/README.md` now has seven — correct, because the contract schedules this amendment for `harny-sync` archive mode, not for this feature. **The human must ensure `harny-sync` actually applies it; until then FC-4 is factually stale.** |

## Test Coverage

| ID | Test Description | Status | Test File |
|---|---|---|---|
| T1 | Golden-byte: the full generated tree for a `--stack typescript` root install is unchanged | WRITTEN — PASSES (green-phase narrowing: 28 of the 31 files stay under frozen pre-feature byte comparison; the three files `roadmap.md`'s File Change Map contracts are excluded from the tree sweep and individually re-asserted instead — each runner against its `templates/` source, the workflow as a single contiguous comment-only insertion. Declared exception documented on the describe block; no golden was re-baselined) | `tests/e2e-init.test.ts` |
| T2 | Golden-byte: the full generated tree for a `--stack python` subdirectory install is unchanged | WRITTEN — PASSES (same green-phase narrowing as T1, applied to the `py-sub` tree and its `harny-feedback-apps-api.yml`) | `tests/e2e-init.test.ts` |
| T3 | `normalizeComponentPath` over a table: `''`, `.`, `./apps/web`, `apps/web/`, `apps//web`, `apps\\web`, `/abs`, `C:/abs`, `..`, `../x` | WRITTEN | `tests/config.test.ts` |
| T4 | `validateComponentList` rejects an empty list and duplicates; sorts ascending by path | WRITTEN | `tests/config.test.ts` |
| T5 | `parseComponentAssignment`: first-`=`-wins, empty stack accepted, no `=` and empty path rejected | WRITTEN | `tests/config.test.ts` |
| T6 | MC-1 exclusivity fires from a `--config` file, from `.sdd/harness.json`, and from one flag invocation | WRITTEN | `tests/config.test.ts` |
| T7 | `mergeConfig` drops a superseded `stack` when an override supplies `components`, and vice versa, without erroring | WRITTEN | `tests/config.test.ts` |
| T8 | `serializeConfig` → `loadConfigFile` → `serializeConfig` round-trips byte-identically with and without `components` | WRITTEN | `tests/config.test.ts` |
| T9 | `resolveComponents` returns the one implicit `.` component for a `components`-free config, including a stack-less one | WRITTEN | `tests/engine.test.ts` |
| T10 | `buildCommandsPayload` returns the bare array for one `.` component and the object form otherwise, including profile-less components with `commands: []` | WRITTEN | `tests/engine.test.ts` |
| T11 | `stepWorkingDirectory` table: `('', '.')`, `('', 'apps/web')`, `('apps', '.')`, `('apps', 'web')` | WRITTEN | `tests/engine.test.ts` |
| T12 | Runner: `normalizeCommandsPayload` over the three wire-format rows including the tolerant fallback | WRITTEN (2 of 3 rows PASS already — declared exceptions) | `tests/hooks/run-feedback.test.ts` |
| T13 | Runner: `componentDirFor` longest-segment-prefix table, including `apps/web` vs. `apps/web-admin` and a path outside `cwd` | WRITTEN | `tests/hooks/run-feedback.test.ts` |
| T14 | Runner: two-component turn — each per-file command receives only its component's paths and runs with that component's cwd | WRITTEN | `tests/hooks/run-feedback.test.ts` |
| T15 | Runner: a component with no assigned touched path runs nothing, per-file or whole-project | WRITTEN | `tests/hooks/run-feedback.test.ts` |
| T16 | Runner: a whole-project command runs once per affected component, from that component's directory | WRITTEN | `tests/hooks/run-feedback.test.ts` |
| T17 | Runner: the unassigned-paths notice is printed once with the right count, and never for a single `.` component | WRITTEN (single-`.` case PASSES already — declared exception) | `tests/hooks/run-feedback.test.ts` |
| T18 | Runner: single-component output text (finding lines, skip lines, silence on a clean turn) is byte-identical to today | WRITTEN (PASSES — declared exception) | `tests/hooks/run-feedback.test.ts` |
| T19 | Runner: `--whole-project` over two components — every command runs from its own directory with the `.` sentinel; summary counters summed; exit 2 on any finding | WRITTEN — PASSES (green-phase FIXTURE correction: the test declared `apps/web` without creating it and was green only because the pre-implementation runner's `resolveComponentCwd` called `fs.mkdirSync`. Both tests in the block now create their component directories; the exit-2 test additionally makes only the NON-root command fail, which its name always claimed and never exercised) | `tests/hooks/run-feedback.test.ts` |
| T20 | Runner: turn-file deletion and `stop_hook_active` suppression unchanged under the component path | WRITTEN | `tests/hooks/run-feedback.test.ts` |
| T21 | Doctor runner: family 5 resolves cwd and probe from `command.dir ?? '.'`; a pre-feature `checks.json` behaves identically | WRITTEN (pre-feature-`checks.json` case PASSES already — declared exception) | `tests/doctor-runner.test.ts` |
| T22 | `buildDoctorChecks`: per-component `commands` with `dir` and `:<path>` ids for >1 component; both omitted for a single `.` component; families 1–4 unchanged | WRITTEN (families-1-4-unchanged case PASSES already — declared exception) | `tests/doctor.test.ts` |
| T23 | `npx harny doctor` and the direct runner invocation agree on a two-component fixture | WRITTEN | `tests/e2e-init.test.ts` |
| T24 | `renderCiWorkflow`: generated block for one component is byte-identical; for three components has one runner step and per-component install steps with correct `working-directory` | WRITTEN (single-component case tested with two components, not three; single-component naming PASSES already — declared exception) | `tests/engine.test.ts` |
| T25 | `renderCiWorkflow`: escape hatch unchanged when no component resolves a profile | WRITTEN | `tests/engine.test.ts` |
| T26 | `renderProjectConfigBlock`: byte-identical for a single-repo config; component lines present and correctly marked for a monorepo config | WRITTEN (single-repo case PASSES already — declared exception) | `tests/generators/markdown-yaml.test.ts` |
| T27 | All five generators' hook configs byte-identical for a single-repo install; object payload embedded for a monorepo install | WRITTEN (component-blindness proven cross-generator; single-repo byte-identity already covered by each generator's own unmodified pre-existing tests) | `tests/generators/registry.test.ts` |
| T28 | Grep gate: component vocabulary under `src/generators/**` is confined to `renderProjectConfigBlock` in `markdown-yaml.ts` (SC14 as amended, MC-16) | WRITTEN — PASSES (green-phase re-implementation against the architect's amended SC14, which resolved a genuine SC14/MC-16 contradiction. Two assertions: zero `/component/i` in every generator file but `markdown-yaml.ts`, scoped by exception so `types.ts` and any NEW generator file stay guarded; and every hit in `markdown-yaml.ts` strictly between the `renderProjectConfigBlock` signature line and the next `^export ` line. Both halves verified red under perturbation) | `tests/generators/registry.test.ts` |
| T29 | Grep gate: no component vocabulary in `src/feedback.ts` (SC16) | WRITTEN (PASSES — declared exception, nothing to violate yet) | `tests/feedback.test.ts` |
| T30 | Grep gate: exactly one `root: 'repo'` assignment site in `src/` (WR-5, still one) | ALREADY COVERED (pre-existing test, unmodified — `src/repo.ts`/`src/engine.ts`'s `root: 'repo'` site is untouched by this feature per MC-30) | `tests/feedback.test.ts` |
| T31 | `runInit`: per-component unrecognized-stack warning; missing-component-directory warning; blank component stack not warned | WRITTEN | `tests/init.test.ts` |
| T32 | `runInit`: the write plan's path set for a two-component install equals a single-repo install's | WRITTEN (PASSES already — declared exception; a structural invariant that holds by construction both before and after implementation) | `tests/init.test.ts` |
| T33 | Prompts: shape question asked first with single-repo default; monorepo loop terminates on an empty path and requires ≥1 component | WRITTEN | `tests/prompts.test.ts` |
| T34 | Prompts: `--stack` and `--component` each preset the shape question, reported via `io.log` | WRITTEN | `tests/prompts.test.ts` |
| T35 | CLI: `--component` repeatable parsing; `--component` + `--stack` is `USAGE` exit 2 | WRITTEN | `tests/cli.test.ts` |
| T36 | E2E: a two-component install against a scratch repo produces a correct, complete artifact set and exits 0 | WRITTEN | `tests/e2e-init.test.ts` |
| T37 | Packaging: template count still thirty-one; no file added under `templates/` | **PASS for the template count; the recorded status is FACTUALLY WRONG.** `tests/packaging.test.ts` *was* modified. Its guarantee-16 dependency-drift assertion was changed from exact version pins (`vitest: '4.1.10'`, etc.) to key-set-only. That is how the roadmap's acknowledged pre-existing vitest-pin failure was closed — by weakening the test, not by leaving the failure. The roadmap's File Change Map marks this file **NOT MODIFIED**, and `AGENTS.md` S4 names it as S4's enforcement evidence. See **F6 (MEDIUM)** | `tests/packaging.test.ts` |
| T39 | Runner: a declared component whose directory is absent at runtime is skipped wholesale with exactly one stderr notice naming it and its command count, counted as skipped in the `--whole-project` summary, never spawned into, and the directory is never created | WRITTEN — PASSES (new in the green phase; covers the Error Handling Contract row added during implementation for the `requires: {}` case. Only the `--whole-project` half is tested). ~~The turn-mode half of that row is unreachable in practice — a turn-mode assigned path implies an existing directory.~~ **CORRECTED by the auditor (see T39a): that reasoning is false and this note must not be carried forward.** `dedupedTouchedPaths` performs no existence filtering, and `existingPaths` runs only inside `perFilePathsFor` — i.e. after component assignment and only for `per-file` commands — so a vanished path under a removed component directory does reach the notice in turn mode. The turn-mode half is **reachable and still untested**: a real gap (**F3**), deliberately deferred by the human to a later round, not a case that cannot occur | `tests/hooks/run-feedback.test.ts` |
| T39a | **AUDITOR CORRECTION to T39's unreachability claim** | **MISSING.** The recorded reasoning ("a turn-mode assigned path implies the directory exists, since `existingPaths` already drops vanished paths") is **false**. `dedupedTouchedPaths` performs no existence filtering; `existingPaths` is called only inside `perFilePathsFor`, i.e. only for `per-file` commands and only *after* component assignment. `componentDirFor` touches the filesystem only through `canonicalPath`, which tolerates non-existent paths. So a vanished touched path under a removed component directory — `feedback-path-hygiene`'s own motivating scenario — is assigned, survives the MC-17 gate, reaches `componentDirExists` → `false`, and prints `componentDirMissingNotice` in **turn mode**. Reachable branch, untested. **F3 (HIGH)** | `tests/hooks/run-feedback.test.ts` |
| T40 | Consequential test maintenance in `tests/canonical-fidelity.test.ts`: `ci-workflow-root` DR-4 and `documentation-role-completion` RC-17 both froze runner files this feature changes by contract | DONE (DR-4 narrowed from three rows to one — `templates/shared/probes.mjs`, still genuinely unchanged; the `run-doctor.mjs` and `run-feedback.mjs` rows retired with the reasoning and their replacement guards recorded in-file. RC-17 narrowed from three copies to the two LIVE ones, dropping the frozen golden that made DR-4 and RC-17 contradict each other. A new live-pair guard pins this repo's `.sdd/feedback/run-feedback.mjs` to its template, the feedback-runner analogue of RC-17. No golden re-baselined; all three surviving assertions verified red under perturbation) | `tests/canonical-fidelity.test.ts` |
| T40a | **AUDITOR RULING on the DR-4 retirement and the two orphaned fixtures** | **PASS — retiring was the right call, and the coverage was genuinely replaced.** Independently verified: (i) `tests/fixtures/golden/monorepo-mode/{ts-root,py-sub}/.sdd/feedback/run-feedback.mjs` and `.sdd/doctor/run-doctor.mjs` are byte-identical to `git show HEAD:templates/…`, so they are true pre-feature captures that already carry forward exactly what the retired DR-4 rows protected — re-baselining the `ci-workflow-root` goldens would indeed have compared current bytes against themselves; (ii) the two live `run-doctor.mjs` copies and the two live `run-feedback.mjs` copies are each byte-identical, and each pair now has its own live guard; (iii) `templates/shared/probes.mjs`'s frozen golden is still live and still true, and I confirmed it fails red on perturbation. The test-writer's framing is also correct that DR-4 and RC-17 directly contradicted each other on that one fixture file, so one had to become vacuous. **On the two now-orphaned files** — `tests/fixtures/golden/ci-workflow-root/{run-doctor.mjs,run-feedback.mjs}` are referenced by no test (verified by grep across `tests/`). Recommendation: **keep them**, because they are the historical pre-`ci-workflow-root` captures the retired rows cited and deleting them destroys the only record of what DR-4 asserted; but they should be named as deliberately-retained history in `specs/archived/ci-workflow-root/`, or a future reader will delete them as dead fixtures | `tests/canonical-fidelity.test.ts` |
| T41 | Consequential test maintenance in `tests/prompts.test.ts`: the pre-existing question-order test asserted five `select` calls | DONE (MC-25's repo-shape question is itself a `select`, making six. Call count updated, the cost-tier sweep now skips call 0 and asserts it is the shape question instead, and the test name/comments renumbered from six questions to seven) | `tests/prompts.test.ts` |
| T38 | Suite baseline: the one pre-existing known failure (vitest version pin) is still the only failure; no previously-passing test made to fail | GREEN — **828 passed, 0 failed, 0 skipped (33 files)** after the post-audit F1 round (+2: the A2 boundary unit test and the A2 end-to-end scoping test; the F1 fix in `src/doctor.ts` left the other 826 green). Previously 826 passed, 0 failed, 0 skipped (33 files). Red phase had recorded 70 new/modified-test failures and zero pre-existing failures; the green phase closed 817/825 and this amendment round closed the remaining 8. Net test count 825 → 826 (+1 new absent-directory test, +1 split SC14 gate, −1 from DR-4's retired `it.each` rows) | — |

## Auditor-added coverage rows

| ID | Test Description | Status | Test File |
|---|---|---|---|
| A1 | MC-27 — `runInit` stays thirteen steps (no black-box observable; escalated to the auditor for code review) | **PASS by code review, no test required.** See C27 for the enumerated step comments. A test here would be a change-detector over comment text; the invariant is better served by `cli-init.md` CLI-1 remaining the single home of the step list | — (code review) |
| A2 | MC-20's boundary for exactly one non-`.` component | **WRITTEN** (test-writer amendment round). Two tests, both verified red under perturbation against the original `components.length > 1` gate and green with the fix. (i) Unit: one test arranges BOTH sides of the boundary — a lone `apps/web` component (must carry `dir` and the `:apps/web` id suffix) and a lone `.` component (must carry neither) — with a length assertion on each so an empty `commands` array cannot satisfy the loops vacuously. Re-verified red in the opposite direction too (forcing `dir` on always fails the `.` half). (ii) E2E: a real `harny init --component apps/web=typescript` in a fixture where the install root declares no `test` script and `apps/web` does, then the generated `.sdd/doctor/run-doctor.mjs` is run — the entry must be `dir`-scoped, the line must be `OK` not `SKIP`, and the script's marker file must land in `apps/web` and not at the root. Against the defect that run emitted `SKIP npm-test - requirement not met` with no marker anywhere, which is exactly F1's silent false-readiness verdict | `tests/doctor.test.ts`, `tests/e2e-init.test.ts` |
| A3 | MC-1 from a real `.sdd/harness.json` read through `validateConfig` | **MISSING.** T6's `.sdd/harness.json` row routes through `loadConfigFile` + `mergeConfig`, which is the `--config` code path. It is a vacuous stand-in for the third source SC11 names, and the real reader (`src/doctor.ts:390` → `validateConfig`) is unguarded. **F2 (HIGH)** | `tests/config.test.ts` |
| A4 | MC-16 for a declared single `.` component (`--component .=python`) | **MISSING.** `tests/generators/markdown-yaml.test.ts` T26 covers the single-repo config and the ≥2-component config; the declared-single-`.`-component case between them renders neither a stack line nor a component line. **F4 (MEDIUM)** | `tests/generators/markdown-yaml.test.ts` |
| A5 | Auditor's independent vacuous-pass sweep of `tests/hooks/run-feedback.test.ts` | **CONFIRMED with one caveat.** The two previously-vacuous passes are genuinely closed: the MC-17 test now creates `services/api` *and* asserts the absence of the `component directory` notice (single possible cause); the non-root exit-2 test creates `apps/web` and uses `FAKE_EXIT_CODE_WHEN` so only `WEB_FAIL` fails, asserting both `ROOT_OK` and `WEB_FAIL` ran. Every other negative assertion in the monorepo blocks is paired with a positive control in the same test, and the `writeFile` helper `mkdir -p`s each touched path's parent, so component directories exist implicitly. **One weak row remains**: the "N is always 0 for the legacy single-`.` form" test sets no `INVOCATION_LOG` and asserts only exit 0 plus the absence of a string, so it would also pass if the runner dispatched nothing. Weak, not vacuous (bare-array dispatch is proven positively elsewhere in the same file). **F13 (LOW)** | `tests/hooks/run-feedback.test.ts` |
| A6 | `tests/fixtures/hooks/record-invocation.mjs` — additive and default-preserving | **CONFIRMED.** Read in full. With `FAKE_EXIT_CODE_WHEN` unset, `shouldFail = (failWhen === undefined) = true`, so `exitCode = FAKE_EXIT_CODE ?? 0` — byte-identical to the previous behavior for every pre-existing caller. The new `cwd` field is an added JSON key; every pre-existing reader destructures `.argv` only. No pre-existing assertion can change | `tests/fixtures/hooks/record-invocation.mjs` |

## `harny-standards` compliance check (S1–S7)

Conventions document read live: `AGENTS.md` § "Coding standards" (the project's named
source of truth). All seven checked, as the auditor checklist requires.

| Std | Verdict | Evidence |
|---|---|---|
| S1 | PASS | New `node:path` imports in `src/config.ts`, `src/engine.ts`, `src/init.ts` all carry the `node:` prefix; every new relative import carries the `.js` specifier (`./config.js`, `./errors.js`, `./engine.js`). `npm run typecheck` clean under `nodenext` |
| S2 | PASS | Every deliberate error added is `HarnessError('USAGE', …)` — `normalizeComponentPath` (×2), `validateComponentList` (×5), `parseComponentAssignment`, `mergeConfig`'s MC-1 check, the prompts' ≥1-component check. No new code in `src/errors.ts`; the exit-code map still has one home |
| S3 | PASS | Determinism: the component list is canonically sorted, making output independent of declaration order. Containment: `normalizeComponentPath` rejects absolute and `../`-escaping paths before anything consumes them; component paths are data (`dir`, `working-directory`), never write targets, so `assertContained` still governs every written path. Trailing newline: `serializeConfig` unchanged in that respect |
| S4 | PASS, with **F6** against its enforcement | `git diff HEAD -- package.json package-lock.json` is empty — no dependency added, removed or version-changed. But `tests/packaging.test.ts`, the file `AGENTS.md` names as S4's "Established by" evidence, was modified and its version pins dropped, weakening the gate. See **F6 (MEDIUM)** |
| S5 | **PASS with one documented exception (round 2)** | Was VIOLATION in round 1. Both boundaries are now named exports in `src/engine.ts` — `isSingleRootComponent` (MC-5/MC-20) and `hasMultipleComponents` (MC-14) — each doc comment naming the other and forbidding substitution, and `isSingleRootComponent`'s stating F1's consequence outright. All four `src/` call sites import the one they mean. **One duplication remains**, at `src/generators/markdown-yaml.ts:62`, and the auditor rules the resolution **correct**: SC14's own gate confines every `/component/i` hit in that file to `renderProjectConfigBlock`'s body, and an ESM import is necessarily top-level, so importing the predicate would fail T28. The executor duplicated-and-documented (naming `engine.ts` as owner) rather than renaming the predicate to evade the gate — the right instinct, since renaming to dodge a gate defeats the gate, and since importing a component predicate into a generator is arguably the very thing SC14 exists to prevent. **Residual risk, unguarded**: nothing fails if `engine.ts`'s predicate changes and line 62 does not. Recommended cheap guard below. `normalizeComponentPath` and `componentDirMissingNotice` remain correctly single-homed, and the C8 data-not-literal pattern is honoured |
| S6 | PASS, with **F12** | Tests mirror `src/`; every modified test file carries a `Spec:`/`Covers:` header naming this feature and its ids; the default run is offline (the new runner tests spawn a local fixture script, never a real linter). **Violation of the "contract ids never appear in test names" clause** — but repo-wide and pre-existing, not introduced here. **F12 (LOW)** |
| S7 | PASS | All three new pieces of tool-neutral prose state the behavior first and name no tool: `templates/hooks/README.md`'s seventh property, `templates/doctor/README.md`'s `dir` paragraph, and `templates/ci/harny-feedback.yml`'s header paragraph. Each tool still appears only under § "Attributed examples". `AGENTS.md`'s new sentence is likewise tool-neutral |

## `harny-feedback` verification

| Check | Result |
|---|---|
| Per-turn hook fired during implementation | **UNCONFIRMED — reported as a gap.** `.sdd/feedback/.turns/` holds only `.gitignore`, mtime 2026-09-14 19:27, predating all of this feature's work (2026-09-23). Turn-file creation and the runner's post-run `fs.rmSync` both update that directory's mtime. Circumstantial but pointed. **F8 (MEDIUM)** |
| Findings heeded | N/A — no findings observable, since no turn artifacts survive |
| Generated CI workflow present | PASS — `.github/workflows/harny-feedback.yml` present at the git root, one job, one runner step |
| Latest CI run green | PASS — run 35878449353, `success`. **Not** a run-nothing green: the log carries `harny-feedback: 1 of 2 command(s) ran, 1 skipped.` (N=1). The single skip is the pre-existing "no eslint config in this repo" reservation |
| CI exercised *this* feature | **NO** — nothing is committed, so the green run is for the previous commit. **F9 (LOW)**; require a green run on the shipping commit |

## Audit Log

| Date | Auditor | Finding | Severity | Resolution |
|---|---|---|---|---|
| 2026-09-23 | sdd-auditor (harny-audit) | **F1** — `buildDoctorChecks` gates `dir`/id-suffix emission on `components.length > 1` instead of MC-20/MC-5's "exactly one `.` component". For exactly one non-`.` component (`init --component apps/web=typescript`), `checks.json` emits the readiness command with **no `dir`**, so `run-doctor.mjs` resolves `path.resolve(cwd, '.')` and runs both the `requires` probe and `npm test` at the install root instead of `apps/web`. Silent: no error, no warning, a false readiness verdict. Verified by direct execution, not inference. `src/doctor.ts` § `buildDoctorChecks` | CRITICAL | Fix: gate on `!(components.length === 1 && components[0].path === '.')` — the same formula `buildCommandsPayload` and `renderProjectConfigBlock` already use — and add the missing A2 test. Better still, extract that formula to one exported helper (S5) so it cannot diverge a fifth time |
| 2026-09-23 | sdd-auditor (harny-audit) | **F2** — MC-1/SC11 is unenforced at `.sdd/harness.json`'s only production reader. `validateConfig` has no exclusivity check and `src/doctor.ts:390` reads the file through it. A hand-edited `.sdd/harness.json` carrying both `stack: typescript` and `components: […]` was accepted silently by `npx harny doctor`, with `components` winning — the "silent winner" MC-1 explicitly forbids. T6's `.sdd/harness.json` row actually exercises `loadConfigFile` + `mergeConfig` (the `--config` path), so it is a vacuous stand-in for that source | HIGH | Move or mirror the exclusivity check into `validateConfig`, which is the one function both the writing path (step 7) and the reading path (doctor) pass through; add A3. Reachable only by hand-editing, since harny itself can never write both |
| 2026-09-23 | sdd-auditor (harny-audit) | **F3** — the turn-mode half of the "absent component directory + `requires: {}`" Error Handling row is reachable and untested, and T39's recorded justification for calling it unreachable is factually wrong. `dedupedTouchedPaths` does no existence filtering; `existingPaths` runs inside `perFilePathsFor`, after assignment and only for `per-file` commands | HIGH | Add a turn-mode test: declare a component, accumulate a path under it, delete the directory before `run`. Correct T39's note in this file so the false reasoning is not inherited by the next feature |
| 2026-09-23 | sdd-auditor (harny-audit) | **F4** — MC-16 deviation. `--component .=python`, which is *exactly* the remediation MC-1's own error message recommends and exactly SC1's invocation shape, renders a conductor project-configuration block with **no `Project stack:` line and no `Component:` line**. Root cause: `resolveComponents` (MC-3) erases the declared-vs-implicit distinction, so `renderProjectConfigBlock` cannot implement MC-16's "only when the install declares components" from `ProjectConfigSummary` alone. The executor resolved this MC-3/MC-16 tension in favour of byte-identity without recording a reservation | MEDIUM | Either carry a `declaredComponents: boolean` (or the raw `config.components`) on `ProjectConfigSummary`, or amend MC-16 to state the one-`.`-component exclusion deliberately. Do not leave it undocumented — an agent reading that conductor block cannot tell the project is Python |
| 2026-09-23 | sdd-auditor (harny-audit) | **F5** — `renderHook` ships `payload.commands ?? (profile ? profile.commands : [])` in all five generators, not MC-15's literal `payload.commands`. **AUDITOR RULING: acceptable, recorded as a reservation, not a blocker.** `HookPayload.commands` is non-optional and `runInit` is its only construction site, so the fallback is unreachable in production and cannot change a shipped byte; MC-15's *guarantee* — no generator learns what a component is — holds in full and is independently proven by the cross-generator marker test in `tests/generators/registry.test.ts`, which supplies `commands` and asserts `profile.commands` never leaks. The cost is real but bounded: five generator test files now build `HookPayload`-shaped fixtures missing a required field and silently exercise a branch production cannot reach, and the `??` disarms the type system's own guarantee that the field is always set | MEDIUM | Follow-up, not blocking: update the five `fakeHookPayload`-style fixtures to supply `commands`, then ship MC-15's literal form and delete the fallback. Deferring is safe; deferring *silently* is not — this reservation must survive into `specs/current/` |
| 2026-09-23 | sdd-auditor (harny-audit) | **F6** — `tests/packaging.test.ts` was modified though the roadmap's File Change Map marks it **NOT MODIFIED** and audit row T37 records it as "pre-existing, unmodified". Guarantee 16's dependency-drift assertion was weakened from exact version pins to key-set-only, which is how the roadmap's acknowledged pre-existing vitest-pin failure was closed. `AGENTS.md` S4 names this file as S4's enforcement evidence; a version bump or downgrade is now invisible to it | MEDIUM | The change is defensible on its merits but is out of this feature's scope and was misreported. The human should decide whether to keep it (and record it as an amendment to guarantee 16) or revert it and carry the vitest-pin failure as the roadmap's exit condition explicitly allowed |
| 2026-09-23 | sdd-auditor (harny-audit) | **F7** — MC-5's "no `if (isSingleRepo)` guard appears at any rendering site" is not literally held: `renderCiWorkflow`'s `multiComponent` ternary, `noBuiltinProfileNoticeForComponents`'s `if (components.length === 1)` and `buildDoctorChecks`'s `multiComponent` are all such guards. Two are *forced* by MC-14/MC-20, which mandate different text for >1 component — MC-5 and MC-14 are in tension in the approved spec. The observable guarantee is met and mechanically proven. The cost is not zero: the single-repo boundary now has four spellings in `src/` (`length===1 && path==='.'` twice, `length>1` twice) and the divergence caused F1 | MEDIUM | Extract one exported predicate and import it at all four sites (S5). Amend MC-5 to say byte-identity is proven by golden comparison rather than claiming an absence of branches its own MC-14 requires |
| 2026-09-23 | sdd-auditor (harny-audit) | **F8** — could not confirm the per-turn feedback hook fired during implementation. `.sdd/feedback/.turns/` contains only `.gitignore` and its mtime is 2026-09-14 19:27, predating this feature's entire implementation (2026-09-23). Both turn-file creation and the runner's post-run `fs.rmSync` update that directory's mtime, so a nine-day-stale mtime is inconsistent with the hook having fired. Circumstantial, not conclusive | MEDIUM | The human should confirm the `PostToolUse`/`Stop` hooks in `.claude/settings.json` were actually live for the implementing sessions. If they were not, this feature's 3,900-line diff shipped without per-turn computational feedback |
| 2026-09-23 | sdd-auditor (harny-audit) | **F9** — CI has not exercised this feature: nothing is committed, so the latest `harny feedback` run (35878449353, green) is for the previous commit. That run is **not** a run-nothing green — its log carries `harny-feedback: 1 of 2 command(s) ran, 1 skipped.` (N=1); the single skip is the pre-existing "no eslint config in this repo" reservation | LOW | Expected state. Require a green run on the shipping commit before sign-off |
| 2026-09-23 | sdd-auditor (harny-audit) | **F10** — MC-11's "with today's single `.` component, `N` is always `0`" is not strictly true. `componentDirFor` returns `undefined` for any path outside `cwd`, so a single-`.`-component install whose turn touched a file outside the install directory now drops it with the notice, where it was previously passed through to the command. Affects subdirectory installs only; no generated artifact changes, so SC2/SC3 are unaffected | LOW | Record as a deliberate behavior narrowing, or amend MC-11's parenthetical |
| 2026-09-23 | sdd-auditor (harny-audit) | **F11** — SC15/MC-25's "asks repo shape first" is implemented as "asks it fourth, before the stack question" (after tools, roles and optional skills). Satisfies G6's "before asking about stacks" and MC-25's operative clause; not the literal headline. The question also carries no `Q` number in `prompts.ts`'s otherwise-numbered comment scheme | LOW | Amend the wording, or renumber the prompt comments |
| 2026-09-23 | sdd-auditor (harny-audit) | **F12** — `AGENTS.md` S6 says "contract ids never appear in test names", but the new test names carry them throughout (`… (MC-9, MC-10, SC6)`, `… (MC-17)`, `… (SC14, MC-16, T28)`). This is repo-wide pre-existing practice (`(BG-1, SC6a)`, `(BG-3)` …), so this feature continues a standing violation rather than introducing one | LOW | The standard and the codebase disagree. Amend S6 or the tests — repo-wide, not in this feature |
| 2026-09-23 | sdd-auditor (harny-audit) | **F13** — the "N is always 0 for the legacy single-`.` form" test sets no `INVOCATION_LOG` and asserts only exit 0 plus the absence of a string, so it would also pass if the runner dispatched nothing. Weak rather than vacuous | LOW | Add a positive control (assert the `NOOP` command ran once) |
| 2026-09-23 (round 2) | sdd-auditor (harny-audit) | **F1 — CLOSED, and the fix introduced no new defect.** `buildDoctorChecks` now reads `!isSingleRootComponent(components)`. Verified by perturbation rather than by reading: restoring `components.length > 1`, running `npm run build`, and re-running `tests/doctor.test.ts` + `tests/e2e-init.test.ts` turned **exactly two** tests red — the A2 unit test and the new e2e test — and nothing else; reverted and rebuilt, `src/doctor.ts` diffstat restored. **The one site whose behavior could have moved did not**: `noBuiltinProfileNoticeForComponents` went from `if (components.length === 1)` to `if (!hasMultipleComponents(components))`, i.e. from `length === 1` to `length <= 1`. The two differ on exactly one input — `length === 0` — which is unreachable, because `resolveComponents` is total and never empty (MC-3) and `validateComponentList` rejects an empty array; the added `components[0]?.stack` makes even that defensive case non-throwing, where the *old* code would have fallen into the multi-branch and emitted an empty name list. For `length === 1` the branch and the text are identical. Confirmed live against three one-component shapes — `--stack cobol`, `--component .=cobol`, `--component apps/web=cobol` — all three emitting the byte-identical pre-feature notice `no built-in profile for stack 'cobol'. Built-in profiles: typescript, python. No lint/typecheck commands run.`, so MC-14's frozen one-component text is intact | RESOLVED | No action. F1 is closed |
| 2026-09-23 (round 2) | sdd-auditor (harny-audit) | **RULING on the two-predicate design and the `markdown-yaml.ts` exception (S5 vs. SC14).** Shipping **two** predicates rather than one is correct, not over-engineering: MC-5/MC-20 ask "is this the one implicit-single-repo shape whose bytes are frozen?" and MC-14 asks "is there more than one name to disambiguate in a step label?" — genuinely different questions that happen to coincide on three of four inputs, and F1 is precisely what collapsing them costs. Naming both and forbidding substitution in each doc comment is the S5-correct form. **Leaving `src/generators/markdown-yaml.ts:62` duplicated is also the right call.** SC14's gate confines `/component/i` in that file to one function body and an ESM import is necessarily top-level, so the import genuinely cannot be written; the two available alternatives were worse — renaming the predicate to something that does not say "component" would have defeated SC14's gate by wordplay, and unilaterally relaxing the gate is the architect's decision, not the executor's. Recording it as `[!]` in `tasks.md` with the owner named in-comment, and attaching it to F4's follow-up (which must revisit that function anyway), is the correct disposition | LOW (residual) | **Residual risk is real but small and unguarded**: nothing turns red if `engine.ts`'s predicate changes and line 62 does not. Cheap guard, if wanted before F4 lands: a test in `tests/generators/markdown-yaml.test.ts` (not covered by T28's gate) asserting `isSingleRootComponent` and the inline expression agree over a four-row table. The real fix is F4's `ProjectConfigSummary` change, which deletes the expression |
| 2026-09-23 (round 2) | sdd-auditor (harny-audit) | **F6 — ACCEPTED AMENDMENT, closed as a finding.** The human settled it: `tests/packaging.test.ts` keeps the key-set assertion, formally recorded as an amendment to `codex-generator`'s guarantee 16. The finding stands as correct and the tradeoff was taken knowingly | ACCEPTED | **Carry forward**: `AGENTS.md` S4 names `tests/packaging.test.ts` as its enforcement evidence, and that file no longer detects a version change — only a package appearing or disappearing. S4 has lost version-drift detection and has no replacement enforcement point. A future feature should either restore pins behind a deliberately-maintained lockfile check, or amend S4 to state that version drift is not mechanically enforced |
| 2026-09-23 | sdd-auditor (harny-audit) | **Scope creep — NOTED, NOT ACTED ON, as instructed.** The executor edited `AGENTS.md` and `README.md` and wrote ADRs 0038–0042. **Correction for the record: none of these is outside the *feature's* scope** — `roadmap.md`'s File Change Map § "Specs and documentation" explicitly lists all three (`AGENTS.md` MODIFY, `README.md` MODIFY, `decisions/0038-…0042` CREATE via `harny-adr`), and Phase 4 step 9 schedules the ADRs. It is **stage** creep, not scope creep: the executor did work the roadmap assigned to Phase 4 / to other roles. All three changes are accurate and S7-compliant. One File Change Map item is **incomplete**: `plan.md` is marked MODIFY ("spec-queue item 3 marked shipped") but is absent from `git status`; line 245 already reads "(shipped)", so either it was marked in an earlier commit or the item is moot | LOW | Left untouched per instruction. `sdd-documentation`/`harny-adr` take ownership. Confirm `plan.md`'s state before archive |

## Final Verdict

**Status**: APPROVED WITH RESERVATIONS

_(Round 1, 2026-09-23: REJECTED on F1. Round 2, same date: F1 fixed and verified closed;
F6 settled by human decision; F2, F3, F4, F5 carried as accepted reservations.)_

**Summary**: Round 1's single blocking defect is closed, and closed well — the fix names
the two boundaries it had been conflating, imports each at the sites that mean it, and is
guarded in both directions by tests the auditor independently perturbation-proved. No new
defect was introduced, including at the one site whose boundary actually moved. What
remains is four deliberately deferred findings and a short list of spec-wording
amendments; none of them produces a wrong artifact, and every one is recorded here in
enough detail to be picked up cold.

**Critical Issues** (must fix before merge):

- **None.** F1 is closed — see the round-2 Audit Log entry for the verification.

**Warnings** (should fix, not blocking):

- **F2 (HIGH)** — MC-1/SC11 is unenforced at `.sdd/harness.json`'s only production
  reader. `validateConfig` has no exclusivity check; `npx harny doctor` silently accepted
  a hand-edited config carrying both fields and let `components` win — the silent winner
  MC-1 forbids. T6's third row is a vacuous stand-in: it exercises the `--config` code
  path under a `.sdd/harness.json` label.
- **F3 (HIGH)** — the turn-mode half of the absent-component-directory Error Handling row
  is **reachable**, contrary to the reasoning recorded in T39. `dedupedTouchedPaths` does
  no existence filtering; `existingPaths` runs inside `perFilePathsFor`, after assignment.
  Untested branch, and a false claim of unreachability that the next feature would inherit.
- **F4 (MEDIUM)** — `--component .=python` renders a conductor block with no stack line
  and no component line. This is the exact remediation MC-1's own error message
  recommends. MC-3's unification erased the fact MC-16 needs.
- **F5 (MEDIUM)** — the `renderHook` fallback. **Ruled acceptable as a recorded
  reservation** (full reasoning in the Audit Log): the guarantee holds, the fallback is
  unreachable in production, and the dedicated cross-generator test proves
  `payload.commands` wins. It must not be forgotten — fix the five fixtures, then ship
  MC-15's literal form.
- **F6 — SETTLED, not carried.** The human accepted the key-set assertion as a formal
  amendment to `codex-generator`'s guarantee 16. The finding stands as correct. **One
  consequence to carry**: `AGENTS.md` S4 names that file as its enforcement evidence, and
  S4 now has no mechanical version-drift detection anywhere. Needs another enforcement
  point, or an S4 amendment saying so.
- **F7 (downgraded MEDIUM → LOW)** — the "four spellings" half is closed by the
  two-predicate fix. The residue is a spec-wording problem only: MC-5 says "no
  `if (isSingleRepo)` guard at any rendering site" while its own MC-14 *mandates*
  different step-name text for >1 component. Amend MC-5, do not edit code.
- **F8 (MEDIUM)** — could not confirm the per-turn feedback hook fired during
  implementation: `.sdd/feedback/.turns/` is empty with a 2026-09-14 mtime, nine days
  before this work.
- **New (LOW), round 2** — MC-20's letter ties the `:<path>` id suffix to ">1 component",
  so the fix now emits `npm-test:apps/web` for a lone non-`.` component where MC-20's text
  implies `npm-test`. MC-20's two clauses never covered that case and MC-5 settles the
  `dir` half in the shipped direction; suffixing is harmless. `harny-sync` should restate
  MC-20 on the `isSingleRootComponent` boundary rather than leave the gap.
- **New (LOW), round 2** — the `src/generators/markdown-yaml.ts:62` duplication is
  correct as ruled (see the round-2 Audit Log), but **unguarded**: nothing turns red if
  `engine.ts`'s predicate changes and line 62 does not.

**Recommendations** (nice to have):

- Extract the single-repo boundary — `components.length === 1 && components[0].path === '.'`
  — into one exported predicate in `src/engine.ts` and import it at all four sites. This
  is the S5 fix that makes F1 structurally impossible rather than fixed once.
- Name `tests/fixtures/golden/ci-workflow-root/{run-doctor.mjs,run-feedback.mjs}` as
  deliberately-retained history in `specs/archived/ci-workflow-root/`. They are now
  referenced by no test; keeping them is right, but an unexplained orphan fixture gets
  deleted eventually.
- Add a positive control to the "N is always 0" test (F13), amend or renumber the prompt
  question ordering (F11), and settle the repo-wide S6-vs-test-names disagreement (F12)
  outside this feature.
- Ensure `harny-sync` actually applies the FC-4 "six → seven properties" amendment at
  archive time; `specs/current/feedback-controls.md:47` is factually stale the moment
  this ships.

**What the human should still decide (none blocking)**:

1. **F8** — confirm the per-turn hook was live during implementation. This is the only
   open question about *process* rather than about code, and it is the one the human
   cannot delegate: if the hooks were not firing, a 3,900-line diff shipped without
   per-turn computational feedback and the next feature should not assume otherwise.
2. **F2's landing spot** — whether the exclusivity check moves into `validateConfig`
   (which both the writing path and the doctor path traverse) or `.sdd/harness.json` is
   formally declared harny-owned and hand-editing unsupported. Either closes it; the
   second is cheaper and should be written down if chosen.
3. **F4's shape** — carry `declaredComponents` (or the raw `config.components`) onto
   `ProjectConfigSummary`, or amend MC-16 to exclude the single-`.`-component case
   deliberately. The same change deletes the `markdown-yaml.ts:62` duplication, so
   sequencing F4 before any further work on that function saves a round.
4. **S4's replacement enforcement point**, following F6's accepted amendment.

**Deferred findings — carried as accepted reservations** (human decision, round 2). Each
is recorded in full in the Audit Log above; this is the index a follow-up feature reads:

| # | Sev | One-line statement | Where it lives | What closes it |
|---|---|---|---|---|
| **F2** | HIGH | MC-1/SC11 is unenforced at `.sdd/harness.json`'s only production reader — `npx harny doctor` silently accepts a hand-edited config carrying both `stack` and `components`, and lets `components` win. T6's third row is a vacuous stand-in: it exercises the `--config` path under a `.sdd/harness.json` label | `src/config.ts` § `validateConfig` (no check); `src/doctor.ts:390` (the reader) | Move or mirror the `mergeConfig` exclusivity check into `validateConfig`; add coverage row **A3** |
| **F3** | HIGH | The turn-mode half of the "absent component directory + `requires: {}`" Error Handling row is **reachable** and untested. T39's "unreachable" reasoning is disproved and the correction is recorded in row **T39a**: `dedupedTouchedPaths` does no existence filtering, and `existingPaths` runs inside `perFilePathsFor`, i.e. after assignment and only for `per-file` commands | `templates/hooks/run-feedback.mjs` § `runRunMode`, the `componentDirExists` branch | One test: declare a component, accumulate a path under it, delete the directory before `run`, assert exactly one `componentDirMissingNotice` and no spawn |
| **F4** | MEDIUM | `--component .=python` — the exact remediation MC-1's own error message recommends — renders a conductor project-config block with **no** `Project stack:` line and **no** `Component:` line. Root cause: `resolveComponents` (MC-3) erases declared-vs-implicit, so MC-16's "only when the install declares components" is not expressible from `ProjectConfigSummary` alone | `src/generators/markdown-yaml.ts` § `renderProjectConfigBlock`; `ProjectConfigSummary` in `src/engine.ts` | Carry the declared-vs-implicit fact onto `ProjectConfigSummary`, **or** amend MC-16. Also deletes the `:62` duplication. Add coverage row **A4** |
| **F5** | MEDIUM | All five `renderHook`s ship `payload.commands ?? (profile ? profile.commands : [])`, not MC-15's literal `payload.commands`. **Ruled acceptable**: `HookPayload.commands` is non-optional and `runInit` is its only construction site, so the fallback is unreachable in production and MC-15's guarantee holds in full. The cost is that five test fixtures build `HookPayload`-shaped objects missing a required field, and the `??` disarms the type system's own guarantee | the five `src/generators/*.ts` `renderHook`s; the `fakeHookPayload`-style fixtures in the five generator test files | Update the five fixtures to supply `commands`, then ship MC-15's literal form and delete the fallback |

**Other reservations opened by this feature and carried forward**: **F7** (LOW after
round 2 — MC-5's "no branch" wording versus MC-14's mandate; amend MC-5), **F8** (MEDIUM
— per-turn hook unconfirmed), **F9** (LOW — require a green CI run on the shipping
commit), **F10** (LOW — MC-11's "N is always 0" parenthetical is not strictly true for an
out-of-`cwd` touched path), **F11** (LOW — the shape question is asked fourth, not
first), **F12** (LOW — S6 versus repo-wide test-naming practice), **F13** (LOW — the
"N is always 0" test lacks a positive control), plus the two new LOW round-2 items
(MC-20's id-suffix wording; the unguarded `markdown-yaml.ts:62` duplication).

**Spec amendments `harny-sync` must apply at archive time** (beyond the 17 the contract
already lists): restate **MC-20** on the `isSingleRootComponent` boundary rather than on
a component count; amend **MC-5** so it claims byte-identity proven by golden comparison
rather than an absence of branches MC-14 requires; record **F6** as the accepted
amendment to `codex-generator` guarantee 16, noting that S4 thereby loses version-drift
detection; and confirm **FC-4**'s "six → seven properties" widening actually lands —
`specs/current/feedback-controls.md:47` is stale the moment this ships.

**Inherited reservations this feature does not close**: `cli-init.md` AL-20 /
`dogfood-quick-fixes` DQ-3 on the `dist/`-vs-`src/` coupling, `feedback-controls.md`
R6/R7 on the Python and npm install candidates, R8 on `SubagentStop`, and
`readiness-checks.md` RD-R5 on verb-vs-direct agreement testing — each of which this
feature touches the neighbourhood of without being scoped to fix.
