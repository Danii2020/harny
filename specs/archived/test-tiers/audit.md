# Audit: Test Tiers

## Requirements Checklist
| ID | Requirement | Source | Status | Notes |
|---|---|---|---|---|
| R1 | Tests proposed at the tier suited to the feature (unit / integration / e2e), each justified by spec ids | intent.md G1, SC5 | PASS | Shipping is verified by T3 and T4. On Claude Code, M1 and M3 proposed unit (in-process render) plus integration (a real in-process HTTP server), with a rationale and covered ids for each row. M2 proposed unit only for pure logic. In M1 and M3 the agent chose not to propose e2e for the UI feature and gave a rubric-based reason ("component output → render in-process"). The rubric allows that call, but it means the e2e proposal path was never seen at runtime (TT-R1, AL-5) |
| R2 | Frameworks inferred per tier from repository evidence, never hard-coded; frameworks only as attributed examples | intent.md G2, SC5 | PASS | M1's plan cites `package.json`, `tests/todos.test.ts` and `src/api/server.ts` as evidence and names exact dev dependencies with ranges. S7 was checked by reading: every framework name in the five files and the rubric is an attributed example. In M3, Context7 was unreachable and the agent said so rather than claiming a docs-verified version |
| R3 | Propose, then confirm, then write: routed through the orchestrator when delegated, inline when direct, skipped only for unit-only **with no setup**; any setup asks, even when unit-only (the human's answer to Q1) | intent.md G3, SC5, SC6 | PARTIAL | The skill text is complete, and T4 coupling passes. On Claude Code, M1 showed the delegated stop, marker, verify, relay and CONFIRMED sequence; M2 showed NOT REQUIRED with no pause; M3 showed the inline ask. In M1 the unit tier alone needed setup and correctly asked (Q1). Not PASS for two reasons. First, the role's Step 5 sends a NOT REQUIRED plan to "Step 7", which in the role skips writing the tests (AL-1). Second, three error-handling rows are not carried (AL-2). The edit-relay and decline paths were never exercised at runtime |
| R4 | Rubric shipped as a bundled `harny-test` resource to all five tools; the dangling `high-value-tests` skill reference is gone | intent.md G4, SC1, SC2, SC3 | PASS | T1, T2, T6 and T7 are green. `grep -rn '\`high-value-tests\` skill' templates/` returns nothing |
| R5 | Skill and role cite the rubric identically; the role's vendoring conditional is removed | intent.md G4, G5, SC4 | PASS | T3 is green. Both files cite `` `high-value-tests.md` § "The one question" `` and § "Picking the right tier". "vendors a dedicated rubric" no longer appears |
| R6 | Exactly three human gates; the checkpoint is conditional and not a gate | intent.md G5, SC7 | PASS | T5 is green. By reading: the diagram line says "(conditional checkpoint … — not a gate)", the summary sentence is extended rather than replaced, the pause-list entry is labelled "**not a gate**", and hard rule #6 was added with #1–#5 byte-unchanged in the diff |
| R7 | All five tools render the changes; no `src/` change; vendor limits respected | intent.md G5, SC8 | PASS | T9 and T16 are green. `git diff --name-only 43f260b 022dcdd -- src bin package.json package-lock.json .agents .claude` is empty. Role bodies are 9,472 (test-writer) and 8,554 (auditor) characters and the conductor is 10,220, all under 30,000. There is no `'''`, CR or control character in any of them. Descriptions are 793 (`harny-test`) and 611 (`harny-audit`) characters, under the 1,024 limit |
| R8 | Suite green; counts, manifest and goldens updated, with only the contracted golden files changed | intent.md SC9 | PASS | The auditor ran `npm run build` (clean), `npm run typecheck` (clean) and `npx vitest run`: **46 files, 966 tests, all passed**. Golden diff against 43f260b: exactly 7 paths per tree (6 M + 1 A each, so 12 M + 2 A in total) |
| R9 | Manual walkthroughs on Claude Code recorded before 2026-09-27 | intent.md G6, SC10 | PARTIAL | Performed 2026-09-24 on Claude Code 2.1.281, headless. M2 and M5 PASS; M1, M3 and M4 PARTIAL (see the M rows). The recording is on time, but not every observation the roadmap planned was made |
| R10 | Dogfood copies untouched (`.agents/`, `.claude/`) | intent.md § Non-Goals | PASS | T8 is green, and the diff under `.agents/` and `.claude/` against 43f260b is empty |
| R11 | The auditor takes tiers into account: it reads the plan, records Tier Results and raises tier findings in the existing buckets | intent.md G7, SC11 | PASS | T14 and T15 are green. The TT-28 tables in the skill and role are line-identical to each other and to the contract table (mechanical diff). M5 on Claude Code wrote `### Tier Results` in the pinned shape and flagged the seeded e2e test as HIGH |
| R12 | All five tools receive the updated test-writer, auditor, conductor and skills | intent.md SC12 | PASS | T1 and T16 are green |

## Contract Compliance
| ID | Contract Item | Status | Verified By |
|---|---|---|---|
| C1 | TT-1 rubric ships to every skill root of every tool | PASS | T1 (a new, non-vacuous per-tool block) and T10. The rubric ends in exactly one `\n` (T6) |
| C2 | TT-2 rubric content and pinned section structure | PASS | T6 checks structure. By reading: the principle sits under the H1; "the one question" has the Yes/Yes, No and Yes/No outcomes; "Don't write these" has exactly six items and "Do write these" exactly five; the tier list has five bullets; the SDD-workflow rule points to the Test Plan's "Not covered" line |
| C3 | TT-3 rubric neutrality, no frontmatter, no `'''` | PASS | T6 and T7. A case-sensitive grep for the residue list returns nothing. Playwright and Cypress appear only as "for example … attributed examples" |
| C4 | TT-4 no dangling skill or resource references in the shipped tree | PASS | T2 (a real five-tool `--skills all` install), plus a repo-wide grep of `templates/` for the literal |
| C5 | TT-5 skill and role cite the rubric identically; conditional removed | PASS | T3. The role restates "the one question" in one paragraph, and the wording agrees with TT-2 |
| C6 | TT-6 tier selection from the closed vocabulary with rationale | PASS | By reading, in skill Step 3 and role Step 3. Seen in M1, M2 and M3. The e2e path was not seen (TT-R1) |
| C7 | TT-7 framework inference with cited evidence and exact setup | PASS | By reading. In M1 the evidence paths and exact dev dependencies with ranges (`@testing-library/react ^16.0.0`, `jsdom ^25.0.0`) appear in the Test Plan |
| C8 | TT-8 Test Plan recorded only in `audit.md` § Test Coverage, pinned shape | PARTIAL | The recording location (audit.md only, never `tasks.md`, update in place) is shipped. The **pinned shape and position are not**: the skill defers to "(contract § Data Models)", which in a downstream install means that feature's own contract, not this one. Neither file says "immediately above its table" or that the status line comes before the table. At runtime, M1 put the plan below the table with the status line after it (AL-3) |
| C9 | TT-9 exact confirmation rule (non-unit tier, or any setup) | PARTIAL | The skill (Step 5) is correct. The role's Step 5 says "continue straight to Step 7", but in the role Step 7 is "Update Audit Tracking", so the text skips Step 6, "Setup Scope, Then Write Tests" (AL-1). At runtime, M2 (which runs this role body) did write the tests. M1 correctly asked for a unit tier that needed setup |
| C10 | TT-10 stop (delegated) or ask inline (direct) | PASS | By reading. M1: marker, PROPOSED, no tests, `package.json` unchanged. M3: inline plan and question, no tests written. The "then continue in the same conversation" leg of M3 was not exercised |
| C11 | TT-11 only a human confirms; edits that add setup re-propose | PASS | By reading, in skill Step 6 and role Step 5. M1: CONFIRMED only after the relayed human decision, with the channel "via the orchestrator". The edit, re-propose and decline paths were not seen at runtime (TT-R1) |
| C12 | TT-12 setup limited to the confirmed "Setup needed" entries | PASS | By reading. M1 `git diff package.json`: exactly the two named dev dependencies and nothing else, no runtime dependency. The install failure was recorded against the tier and not bypassed (the "confirmed setup step fails" error row) |
| C13 | TT-13 default run offline; existing rules preserved | PASS | By reading. All six listed rules survive in both files: no ids in names, spec-linked header, AAA, fake seams, never paper over a bug, PENDING→WRITTEN. See AL-4 for content removed outside this list |
| C14 | TT-14 per-tier red status, never claimed without a run | PASS | By reading. M1 and M2 both reported "not run" honestly and claimed no red status. The "red for the right reason" leg was never seen at runtime because no clone had `node_modules` (see M1) |
| C15 | TT-15 conductor verifies `PROPOSED`, asks, and relays to the same role | PASS | By reading hard rule #6 and "Verify, don't trust". In M1 the conductor states it checked `audit.md` directly before presenting |
| C16 | TT-16 exactly three gates; checkpoint is not a gate; rules #1–#5 intact | PASS | T5, plus a read of the diff (rules #1–#5 unchanged, #6 added, pause and flow entries added) |
| C17 | TT-17 protocol tokens identical in skill, role and conductor | PASS | T4 and T14 (all five files) |
| C18 | TT-18 post-red-tests gate shows tiers and per-tier red status | PASS | By reading the conductor's test-review bullet. Seen in M2 (plan status plus a "not run" tier). M1 never reached the gate (see M1) |
| C19 | TT-19 `harny-test` shape contract, `allowed-tools` unchanged, version "1.1" | PASS | The existing shape tests and the D1 `allowed-tools` test are green. By reading: `description` 793 characters and mentions proposing first; `compatibility` 161 characters and names the bundled file; `harny-writes` matches the contract byte for byte; Inputs 5–6 and the Guardrails are present |
| C20 | TT-20 role metadata unchanged except `invocation`/`handoff`; renders on all five tools | PARTIAL | The metadata diff touches only `invocation` and `handoff`. All five tools render (T9, T16). The AL-5 block is intact. **However**, "its body carries the same flow as the skill" is not fully met: the Step-number cross-reference is wrong (AL-1), and the skill and role have drifted on retained instructions (AL-7) |
| C21 | TT-21 no `src/`, `bin/` or `package.json` change; no new dependency (S4) | PASS | The `git diff` against 43f260b is empty for `src bin package.json package-lock.json`, and the packaging dependency guard is green |
| C22 | TT-22 dogfood untouched; divergence declared | PASS | T8. The `DIVERGENCE_TABLE` entries match TT-22 exactly, and the diff for `.agents/` and `.claude/` is empty |
| C23 | TT-23 install counts and packaging manifest | PASS | T9 and T11. The counts are 39 ×5, 95/36, 101/42, 92/33 and 32, all as contracted |
| C24 | TT-24 only the seven contracted golden paths per tree change (12 modified, 2 created in total); T41 allowlist (test-writer and auditor roles) | PASS | `git diff --name-status 43f260b 022dcdd -- tests/fixtures/golden` lists exactly the 14 contracted entries. The golden byte test is green against the current generator, so the goldens match the build. T13 allowlist entries are added by exact match. The declared-exception list did not grow |
| C25 | TT-25 runtime behavior verified manually on Claude Code, remainder carried as TT-R1 and TT-R2 | PARTIAL | M1–M5 were performed on 2026-09-24. M1, M3 and M4 are PARTIAL, so TT-R1 stays at MEDIUM under its own downgrade rule (AL-5). TT-R2 is carried (AL-6) |
| C26 | TT-26 auditor reads the Test Plan and writes `### Tier Results` in the pinned shape and location, within `audit.md` only | PASS | By reading: skill Step 5a sits between steps 5 and 6, and role Step 6 carries the same text. In M5, `### Tier Results` sits directly below `### Test Plan` (lines 40–53 of m3's `audit.md`), the columns match the pinned header exactly, the plan status was left as PROPOSED, and `git status` in m3 shows only `audit.md` changed besides the seeded file |
| C27 | TT-27 per-tier checks: tests exist, listed ids covered at that tier, setup equals the plan, tier run or `not run: <reason>` | PASS | By reading: (a)–(d) appear verbatim in both files. M5 checked all four per tier and gave the unplanned e2e tier its own row |
| C28 | TT-28 tier findings mapped to existing buckets, identical in skill and role | PASS | T15, plus a mechanical line diff: the 9 rows are identical in the skill and the role and match the contract table. The four bucket definitions are unchanged. M5 rated the seeded e2e test, the PROPOSED plan and the unnamed setup HIGH, and the unrunnable tier LOW |
| C29 | TT-29 `harny-audit` shape kept, version "1.1", `allowed-tools`/`harny-writes` unchanged; role metadata unchanged | PASS | Shape and D1 tests are green. `allowed-tools` and `harny-writes` are byte-unchanged in the diff. Neither file contains `S1–S7` or `seven standards (S1`. The role's metadata is not in the diff. Minor wording issue: AL-8 |
| C30 | TT-30 all five generators' test-writer, auditor and conductor artifacts carry the protocol tokens; every install carries the updated skills | PASS | T16 and T1 |

## Test Coverage
| ID | Test Description | Status | Test File |
|---|---|---|---|
| T1 | Each of the five tools, installed alone, writes every `harny-test` file (including `high-value-tests.md`) under its own skill root, byte-equal to the template | PASS | `tests/skills-placement.test.ts` |
| T2 | In a real five-tool `--skills all` install, every "`<name>` skill" reference resolves to a shipped skill, and every bundled `.md` reference in a `SKILL.md` exists beside it | PASS | `tests/skill-references.test.ts` |
| T3 | Every rubric citation in the skill and the role names an existing rubric heading; each cites "The one question" and "Picking the right tier"; the role no longer carries the vendoring conditional | PASS | `tests/test-writer-templates.test.ts` |
| T4 | The awaiting marker and the three plan-status values appear identically in the test-writer skill and role, the conductor, and the auditor skill and role | PASS | `tests/test-writer-templates.test.ts` |
| T5 | The conductor's pipeline diagram has exactly three human gates, and the three pre-existing load-bearing phrases survive | PASS | `tests/test-writer-templates.test.ts` |
| T6 | The rubric has one H1, the five pinned H2 headings in order, and no frontmatter | PASS | `tests/test-writer-templates.test.ts` |
| T7 | The rubric carries none of the dogfood domain residue | PASS | `tests/skills-templates.test.ts` |
| T8 | `harny-test` and `harny-audit` are declared divergences: the dangling-skill sentence is absent from shipped `harny-test`, and the rubric, the marker and `### Tier Results` are present in the shipped copies, all checked against the dogfood oracle | PASS | `tests/skills-fidelity.test.ts` |
| T9 | Install counts rise by one per skill root across the single-tool, five-tool and `--skills` scenarios | PASS | `tests/e2e-init.test.ts` |
| T10 | The default skill-library path list includes the rubric in every single-tool scenario | PASS | `tests/e2e-init.test.ts` |
| T11 | The npm package includes the rubric | PASS | `tests/packaging.test.ts` |
| T12 | The `monorepo-mode` golden trees match the built CLI, with only the seven contracted paths regenerated per tree | PASS | `tests/e2e-init.test.ts` (existing golden test; fixtures regenerated) |
| T13 | The templates non-mutation guard allows the two contracted role edits | PASS | `tests/canonical-fidelity.test.ts` |
| T14 | The auditor skill and role both name `### Tier Results` and carry the protocol tokens (part of T4's five-file coupling) | PASS | `tests/test-writer-templates.test.ts` |
| T15 | The auditor skill and role list the same tier conditions at the same severities, with the five contracted conditions at HIGH | PASS (partial parity, see AL-9) | `tests/test-writer-templates.test.ts` |
| T16 | Each of the five generators renders test-writer, auditor and conductor artifacts carrying the protocol tokens | PASS | `tests/canonical-fidelity.test.ts` |

Suite run by the auditor on 2026-09-24 at commit 022dcdd: `npm run build` clean, `npm run typecheck` clean, `npx vitest run` **46 files / 966 tests, all passed** (the baseline was 44/932). The default run is offline.

This feature has no `### Tier Results` table. This repository's own pipeline runs the dogfood test-writer and auditor, which have no tier flow (intent § Non-Goals, TT-R3), so no Test Plan exists to audit against.

**Manual verification on Claude Code (not automatable, contract TT-25; deadline 2026-09-26; the other four tools are TT-R2):**

| ID | Walkthrough | Status | Evidence |
|---|---|---|---|
| M1 | Delegated conductor flow on a frontend+API sample. The plan proposes unit, integration and e2e with evidence and exact e2e setup. The report begins with the marker. No test or manifest change before confirmation. The conductor asks, relays an edit, and the same role resumes, confirms, sets up and writes. The post-red-tests gate shows per-tier status | PARTIAL | Run on 2026-09-24 with Claude Code 2.1.281 (headless `claude -p`), on the `demo-todo` clone `m1`, feature `add-todo`. Outputs are `m1.out` and `m1b.out`, and the record is `walkthrough-evidence.md` (session scratchpad). **Observed (pass):** the test-writer stopped with PROPOSED. The conductor checked `audit.md` itself before presenting ("Verify, don't trust"). Before confirmation, `git status` showed only `audit.md` changed and `m1-pkg-before.json` equals `HEAD:package.json`. Correctly, the unit tier alone required confirmation because it needed setup (Q1). After the relayed confirmation the status became `CONFIRMED (2026-09-24, by the human, via the orchestrator)`. `git diff package.json` adds exactly the two named dev dependencies. All 7 planned tests were written with spec-linked headers. **Deviations:** (1) No e2e tier was proposed; the agent gave a rubric-based reason, so exact e2e setup was never seen. (2) The plan was confirmed as-is, so edit relay was not exercised. (3) The Test Plan and its status line landed *below* the Test Coverage table (AL-3). (4) `npm install` was blocked by the sample's scaffolded permissions ask rule in headless mode, so the new tests never ran and the session stopped at a permission prompt, not at the post-red-tests gate. **Auditor's judgment on TT-14:** this counts as evidence *for* TT-14's honesty half. The test-writer logged the failed install against the tier (TT-12 and its error row), reported every tier "not run" and did not claim red, and did not work around the denial. It is **no** evidence for the "red for the right reason" half or for TT-18 at M1's gate. The environment caused the gap, not a defect, so M1 is PARTIAL rather than FAIL, and the unverified half stays in TT-R1 |
| M2 | Unit-only pure-logic feature: `NOT REQUIRED (unit-only, no setup)`, no marker, no pause | PASS | Run on 2026-09-24, Claude Code, clone `m2`, feature `title-limit`, output `m2.out`. `**Plan status**: NOT REQUIRED (unit-only, no setup)` is recorded (m2 `audit.md` line 16). There was no marker and no checkpoint pause, and tests were written in one delegated invocation (`git status`: only `audit.md` and `tests/todos.test.ts` changed; `package.json` untouched). The conductor stopped at the post-red-tests gate and showed the plan status plus an honest "not run" for the unit tier, which observes TT-18. This path ran through the role body with the wrong Step-7 reference (AL-1) and the agent still wrote the tests |
| M3 | Direct `harny-test` invocation: proposes inline, asks, waits, then proceeds in the same conversation | PARTIAL | Run on 2026-09-24, Claude Code, `/harny-test add-todo` in clone `m3`, output `m3.out`. The plan was recorded as PROPOSED and presented inline with a question, and no tests were written (`git status` in m3: only `audit.md`, `package.json` unchanged). The agent disclosed that Context7 was unreachable rather than claiming a docs-verified version. **Not exercised:** answering, then continuing in the same conversation. The headless run ended at the question |
| M4 | Missing framework: manifest and lockfile byte-unchanged before confirmation. Declining e2e drops it and installs nothing | PARTIAL | Run on 2026-09-24, Claude Code, across M1, M2 and M3. Before any confirmation, `package.json` was byte-unchanged in every clone, and it changed in M1 only after confirmation and only by the named setup. `node_modules` is absent everywhere, and the sample has no lockfile. The missing framework in this case was the component-render library with jsdom (unit tier), proposed with exact packages and ranges. **Not exercised:** declining the e2e tier. No e2e was ever proposed, so the TT-11 decline path and "plan becomes unit plus integration, CONFIRMED" were never seen |
| M5 | Tier-aware audit: `### Tier Results` written in the pinned shape, and a seeded deviation (an e2e test under a `PROPOSED` plan, or an unnamed dev dependency) reported as HIGH | PASS | Run on 2026-09-24, Claude Code, `sdd-auditor` on clone `m3` (plan PROPOSED) with an untracked, seeded `e2e/add-todo.spec.ts` (Playwright). Output is `m5.out`. `### Tier Results` sits directly below `### Test Plan`, with an 8-column header that matches the pinned shape exactly and one row each for unit, integration and "e2e (not in plan)". Findings: HIGH "e2e test exists under a plan that isn't CONFIRMED", HIGH "depends on setup the plan never named", HIGH "plan still PROPOSED", and LOW "auditor couldn't run the tier". The Test Plan status was left untouched. **Deviation:** it ran on an unimplemented feature (verdict REJECTED for that reason) rather than continuing M1 through the executor. The tier-audit behavior under test was fully shown |

## Audit Log
| Date | Auditor | Finding | Severity | Resolution |
|---|---|---|---|---|
| 2026-09-24 | sdd-architect (planned, pre-implementation) | **TT-R1, planned reservation.** Tier inference, the confirmation rule, stop/ask behavior, setup scope and per-tier red reporting are prompt instructions. No automated test can prove an agent follows them. This now also covers the auditor's tier checks (TT-26 to TT-28). Evidence is limited to M1–M5 on Claude Code | MEDIUM (design, deliberate) | Carry forward, as LOW if M1–M5 all pass |
| 2026-09-24 | sdd-architect (planned, pre-implementation) | **TT-R2, named reservation (the human's decision on Q4).** The manual walkthrough targets Claude Code only. Cursor, Kiro, GitHub Copilot and Codex receive identical content (TT-1, TT-30 are tested), but their runtime behavior is not walked through: checkpoint routing (resume versus fresh invocation, the ask-the-human mechanism) and the tier-aware audit. This has the same class as tool-generators AL-30 and CG-1 | MEDIUM (open, human-gated) | Carry forward |
| 2026-09-24 | sdd-architect (post-specs gate revision) | Human answers recorded: Q1 any setup asks even if unit-only (TT-9); Q2 auditor checks tiers, now in scope (TT-26 to TT-30); Q3 readiness manifest unchanged (non-goal); Q4 walkthrough on Claude Code, other four tools named as TT-R2; Q5 `harny-test` version "1.1" (TT-19) | N/A (decision record) | Specs revised; no open reservation added beyond TT-R2's widened scope |
| 2026-09-24 | sdd-architect (planned, pre-implementation) | **TT-R3, planned reservation.** The dogfood `harny-test` and `harny-audit` (which this repo's own pipeline runs) intentionally lack the tier flow and tier audit. The dogfood `harny-test` still points at the dogfood-only `high-value-tests` skill. Both are declared in `DIVERGENCE_TABLE` | LOW (design, deliberate) | Carry forward. A future dogfood-sync feature may close it |
| 2026-09-24 | sdd-auditor | **AL-1: the role's Step 5 skips the step that writes the tests** (contract TT-9, TT-20; intent G5). `templates/roles/sdd-test-writer.md:51` says a NOT REQUIRED plan should "continue straight to Step 7 with no stop and no marker". In the role, Step 7 is "Update Audit Tracking" (line 74) and the tests are written in **Step 6**, "Setup Scope, Then Write Tests" (line 59). The sentence was copied from the skill, where Step 7 is correct because the skill's numbering differs by one. So the role and the skill disagree, and the role's literal instruction contradicts TT-9 ("continues to write the tests in the same invocation"). This byte-propagates to all five tools and both golden trees (`…/.claude/agents/sdd-test-writer.md:45`). It is rated HIGH rather than CRITICAL because the guarantee held in the one runtime observation: M2 ran exactly this role body as the delegated sub-agent and wrote the tests in one invocation. It remains a wrong cross-reference in shipped content | HIGH | `harny-implement`: change "Step 7" to "Step 6" in the role, and regenerate the two golden `sdd-test-writer.md` files (a TT-24-class, same-path regeneration) |
| 2026-09-24 | sdd-auditor | **AL-2: four Error Handling Contract rows are missing from the shipped prompts**, which the severity ratings treat as unhandled error conditions. (a) "Re-invoked with a 'confirmed' decision, but `audit.md` has no `### Test Plan`": rebuild, record PROPOSED and stop. This is absent from the `harny-test` skill and the role. The nearest text, "never treat a `PROPOSED` status found on disk as permission", covers a different case. (b) "Stack evidence is ambiguous": recommend one option, cite evidence for both, and name the alternative in Rationale. Absent from both. (c) "Auditor cannot tell which tier a test belongs to": classify it by the rubric, note it, raise LOW. Absent from the `harny-audit` skill and the `sdd-auditor` role. (d) "No human is reachable": the conductor says "never confirm on the human's behalf", but the wait-at-checkpoint rule and the recorded-waiver exception are not stated. The rows that *are* carried: install failure, a tier that cannot run, conductor marker/status mismatch, re-audit rewrite in place | HIGH | `harny-implement`: add one sentence per row to the skill and the role (and to the conductor for (d)), then regenerate the affected golden paths |
| 2026-09-24 | sdd-auditor | **AL-3: the pinned Test Plan shape and position do not ship** (contract TT-8, § Data Models). `templates/skills/harny-test/SKILL.md:95` says "in the pinned shape (contract § Data Models)". Downstream, "contract" is the feature's own `contract.md`, which has no such section, so the reference points at nothing. Neither the skill nor the role gives the template, says "immediately above its table", or says the `**Plan status**:` line comes before the plan table. The column list is carried as prose. At runtime this showed: M1's plan and status line landed **below** the Test Coverage table, with the status after the plan table, and M2 put its status after the plan table too. The M5 auditor still parsed the status, so the effect is moderate | MEDIUM | `harny-implement`: inline the pinned Test Plan skeleton (heading, status line, table header, "Not covered" and "Default run" lines) and its position in both files, and drop the "(contract § Data Models)" pointer |
| 2026-09-24 | sdd-auditor | **AL-4: the spec-to-test mapping block was removed without contract authority.** The old `harny-test` Step 3 and role Step 3 ("Design the test plan": every public interface gets a happy-path test; every guarantee gets a dedicated test; every **error-handling row** gets a test; data-model validation; every constraint gets a test; the edge-case list) are gone from both files. TT-6 authorizes replacing only the SC→integration line, and roadmap Phase 2.3 names only two replacements. The shipped test-writer is no longer told to test error-handling rows or constraints, while the shipped auditor still checks "every contract guarantee has at least one test" | MEDIUM | `harny-implement`: restore the mapping bullets, adjusted to "at the cheapest suitable tier", or get a human-approved contract amendment recording the removal |
| 2026-09-24 | sdd-auditor | **AL-5: TT-R1 stays at MEDIUM.** Its downgrade condition ("LOW if M1–M5 all pass") is not met because M1, M3 and M4 are PARTIAL. Never seen at runtime: an e2e proposal with exact e2e setup; an edit relayed through the conductor; the TT-11 decline path; the M3 inline answer-then-continue; "red for the right reason" for any tier (no clone could install dependencies). The positive observations are the delegated stop and verify, the inline ask, NOT REQUIRED with no pause, unit-plus-setup asking (Q1), exact setup scope, honest "not run" reporting, and the tier-aware audit | MEDIUM (carried) | Human decision at the post-audit gate. Before the 2026-09-27 demo, consider one interactive (non-headless) Claude Code run of M1 with `npm install` allowed and an edit that drops a tier. That would close most of the gap |
| 2026-09-24 | sdd-auditor | **AL-6: TT-R2 is carried unchanged.** Cursor, Kiro, GitHub Copilot and Codex receive the content (T1, T16 green) but were not walked through | MEDIUM (carried) | Carry forward (the human's decision on Q4) |
| 2026-09-24 | sdd-auditor | **AL-7: the skill and the role drift on instructions they used to share** (intent G5, "agree on every rule"). The skill dropped three instructions the role keeps: "Verify a library's API via Context7 (or equivalent) before asserting against it", which the skill narrowed to naming a framework (PR-4); "Report any failures with clear descriptions"; and "Fix tests that fail due to test bugs (not implementation bugs — those go in audit.md)". None of these is in TT-13's list, but G5 applies | LOW | `harny-implement`: restore the three lines in the skill |
| 2026-09-24 | sdd-auditor | **AL-8: a harny-internal id appears in shipped content.** `templates/skills/harny-audit/SKILL.md:94` says "Raise every TT-28 finding below". TT-28 is this repository's contract id and means nothing in a downstream install. The role phrases the same thing correctly ("§ Severity Ratings' Tier findings table"). `NEUTRALITY_CHECKS` does not catch it | LOW | `harny-implement`: reword to "every tier finding in the table below"; optionally add `TT-` to the `harny-audit` neutrality needles |
| 2026-09-24 | sdd-auditor | **AL-9: T15's parity check covers only 5 of the 9 TT-28 rows.** The `TT28_CONDITIONS` docblock says it includes "the two MEDIUM and one LOW conditions", but the array omits them, as well as the sixth HIGH row ("no test at any tier"). Task R.13 asked for "(condition, level) pairs … equal". The auditor checked full parity with a mechanical line diff: all 9 rows are identical in the skill, the role and the contract. The shipped content is correct; only the guard is narrower than described | LOW | `harny-test`: parse every row of both tables and compare the sets, or correct the docblock |
| 2026-09-24 | sdd-auditor | **AL-10 (S6): contract ids appear in new test names**, for example `…(TT-2, TT-3) (T6)`, `…(TT-4)`, `…(TT-30) (T16)`, `…non-vacuously (TT-1)`. This contradicts `tasks.md` § Notes' own statement. It is the same repo-wide, pre-existing pattern already logged as LOW by ci-workflow-root AL-8 and documentation-role-completion AL-4 | LOW (carried class) | Not this feature's to fix. Reconcile S6 wording or the suite in a separate change |
| 2026-09-24 | sdd-auditor | **`harny-standards` S1–S7 check.** S1: new tests use the `node:` prefix and `.js` specifiers (pass). S2: no `src/` change (N/A, pass). S3: every touched template and golden ends in exactly one `\n`, and the goldens are byte-equal to the generator (pass). S4: `package.json` and lockfile unchanged (pass). S5: no new shared constant is re-literalled in `src/`; skill-root literals in tests are expected-value oracles, as in existing practice (pass). S6: `Spec:`/`Covers:` headers on every touched test file and an offline default run (pass), except AL-10. S7: every framework and tool name in the five files and the rubric is an attributed example ("e.g.", "for example … attributed example only"); `Context7 (or the target tool's equivalent)` is unchanged, pre-existing wording that context7-mcp already accepted (pass) | N/A (S6 → AL-10) | — |
| 2026-09-24 | sdd-auditor | **AL-11: `harny-feedback` verification is incomplete.** The per-turn hook is wired (`Stop` and `SubagentStop` in `.claude/settings.json`). Its turn-state directory `.sdd/feedback/.turns/` was last touched 2026-09-24 18:26 and is empty (consumed), which is consistent with it firing. No ESLint config exists, so only the `tsc` whole-project command maps, and `tsc` is clean at 022dcdd. **CI:** the GitHub API returns `total_count: 0` workflow runs for `claude/test-tiers`. The workflow triggers on `pull_request` and push to `main`, and no PR is open, so there is no green run to read and no `N of M command(s) ran` line to check | LOW | Once the PR is opened, confirm the `harny-feedback` run is green with N > 0 in its trailing summary line |
| 2026-09-24 | sdd-auditor | **AL-12: tracking is out of date.** `tasks.md` Phase 5 (Tasks 5.1–5.6) is still `[ ]`, and § Execution status still says Phase 5 is "deferred", although the walkthroughs ran on 2026-09-24 (recorded above in rows M1–M5) | LOW | Conductor or `harny-implement`: mark 5.1, 5.2 and 5.6 `[x]`, and mark 5.3–5.5 `[x]` or `[!]` with the PARTIAL notes (the auditor writes `audit.md` only) |

## Final Verdict

**Status**: APPROVED WITH RESERVATIONS

**Summary**: The delivery layer matches the contract where it matters most, with an in-scope diff. The rubric ships to every root of all five tools, and no dangling reference remains. The protocol tokens, the three-gate invariant and the TT-28 table are identical wherever they are pinned. Counts, manifest and exactly 7 golden paths per tree are correct, and build, typecheck and 46 files / 966 tests are green. On Claude Code, the walkthroughs showed the core propose-confirm-write flow and the tier-aware audit working. Two HIGH text defects in the shipped prompts and PARTIAL walkthrough coverage keep this from a clean APPROVED. No finding is CRITICAL.

**Critical Issues** (must fix before merge):
- None.

**Warnings** (should fix, not blocking):
- AL-1 (HIGH): in `templates/roles/sdd-test-writer.md:51`, "continue straight to Step 7" should read "Step 6". As written, a NOT REQUIRED plan skips the step that writes the tests. This is a one-word fix plus regenerating 2 golden files, and it should land before the 2026-09-27 workshop.
- AL-2 (HIGH): four Error Handling Contract rows are missing from the shipped prompts: re-invoked with no plan; ambiguous evidence; auditor cannot classify a tier; no human reachable or waiver.
- AL-3 (MEDIUM): the pinned Test Plan shape and position are not shipped, and "(contract § Data Models)" points at nothing downstream. M1 put the plan below the table.
- AL-4 (MEDIUM): the spec-to-test mapping block (error-handling rows, constraints, edge cases) was removed without contract authority.
- AL-5 / TT-R1 (MEDIUM, carried): M1, M3 and M4 are PARTIAL. Never seen at runtime: an e2e proposal, an edit relay, the decline path, the inline continuation, and "red for the right reason" (the headless `npm install` was blocked by the scaffolded ask rule, so red status was honestly reported "not run").
- AL-6 / TT-R2 (MEDIUM, carried): the other four tools were not walked through.

**Recommendations** (nice to have):
- AL-7: restore the three instructions the skill dropped but the role keeps. AL-8: remove "TT-28" from the shipped `harny-audit` skill.
- AL-9: widen T15 to full-table parity. AL-11: verify the CI run once the PR is open. AL-12: update `tasks.md` Phase 5 and Execution status.
- Before the workshop, run M1 once interactively with `npm install` permitted and one tier-dropping edit. That would cover most of TT-R1's gap and rehearse the exact demo.
- TT-R3 (LOW, carried) and AL-10 / S6 test names (LOW, pre-existing repo-wide pattern).
