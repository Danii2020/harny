# Audit: templates-skill-library-parity

> Scaffold seeded by `harny-propose`. Every row is `PENDING` until `harny-audit` runs
> after implementation. Requirements trace to `intent.md`; contract items trace to
> `contract.md`; test rows trace to `tasks.md` Phase 4.

## Requirements Checklist

| ID | Requirement | Source | Status | Notes |
|---|---|---|---|---|
| R1 | `templates/skills/` exists with all eleven files; `templates/**` is 22 files | intent.md SC1 (G1) | **PASS** | `find templates -type f` → exactly 22, and the eleven `templates/skills/**` paths match `contract.md` § Data Models' manifest name-for-name. No extra file, no missing file. |
| R2 | Every shipped `SKILL.md` uses only the six portable frontmatter keys and carries the five body sections in order | intent.md SC2 (G1) | **PASS** | Hand-verified independently of the suite: all eight files declare only `{name, description, license, compatibility, metadata, allowed-tools}` (`harny-standards` omits `allowed-tools`, five keys). All eight carry `# <Title>` → `## When to use this` → `## Inputs` → `## Steps` → `## Guardrails` in order. `metadata` values are all strings. |
| R3 | Every shipped `description` ≤1,024 and `compatibility` ≤500 — tighter than SL-4's 1,536 | intent.md SC3 (G1) | **PASS** | Hand-measured after folded-scalar unwrapping: longest `description` `harny-sync` **735**; longest `compatibility` `harny-sync` **231**. Every one non-empty. |
| R4 | Folder name equals `name:`; 1–64 chars, lowercase/digits/hyphens, no leading, trailing or consecutive hyphen | intent.md SC4 (G1) | **PASS** | Hand-verified for all eight: `name_ok=True` in every case; longest name 15 chars; no leading/trailing/double hyphen. |
| R5 | `contract.md` carries a divergence table with one row per `.agents/skills/` ↔ `templates/skills/` pair; a test fails on undeclared divergence | intent.md SC5 (G2) | **PARTIAL** | **Post-audit update:** AL-P2 resolved — `tests/skills-fidelity.test.ts` now asserts all nine pairs exhaustively (byte-identity or declared-residue needles), and would fail on undeclared divergence in any of them. The `harny-audit`/`harny-implement` table row is also corrected from "DC-2 only" to the accurate DC-1 classification (AL-P1). Still open: the table's *other* rows remain the pre-implementation prediction, not the post-hoc record Task 1.10 required (AL-P4, MEDIUM, carried forward — not fixed this round per instruction). |
| R6 | All five tools → each selected skill written exactly three times (`.agents/`, `.claude/`, `.kiro/`), never five, never once | intent.md SC6 (G3) | **PASS** | Reproduced independently with a real `node bin/harness.js init … --tools all --yes` into a temp dir: 63 files total; 27 skill artifacts = 9 × 3 roots; `.cursor/skills/` and `.github/skills/` contain only their own `sdd-conductor/SKILL.md`; `.codex/skills/` absent. |
| R7 | `cursor,codex,github-copilot` → each selected skill written exactly once, under `.agents/skills/` | intent.md SC7 (G3) | **PASS** | Reproduced: 33 files, one skill root (`.agents/skills`), 9 skill artifacts, no `.claude/skills` and no `.kiro/skills`. |
| R8 | No skill artifact is a symlink; every one byte-equals its `templates/skills/` source | intent.md SC8 (G3) | **PASS** | Verified directly, not via the suite: `cmp` of all 27 written artifacts against their `templates/skills/` sources → 0 differences; `find -type l` over the whole target → 0 entries; every skill artifact ends in exactly one `\n`. |
| R9 | `contract.md` "Verified facts" carries a first-party URL and the date `2026-09-09` for every pinned path, field set and limit; the Kiro `allowed-tools` question is answered, not assumed | intent.md SC9 (G4) | **PASS** | V1–V12 each carry a first-party URL under the stated 2026-09-09 fetch; V13 is explicitly marked *(carried)* with its own 2026-09-02 date. V7 states the Kiro gap as a gap, and D1 resolves it with an argument plus a pre-authorized remedy rather than an assumption. |
| R10 | `contract.md` states, under a findable heading, that `tool-generators.md` TG-6's Kiro row is superseded-and-extended, naming old and new | intent.md SC10 (G4) | **PASS** | § SUPERSEDES row S2, findable by heading, quotes TG-6's old claim and names V6/V7/V8 as the extension, and states TG-6 is incomplete rather than contradicted. |
| R11 | A CLI flag selects the opt-in skills; a core id is `USAGE` non-interactively and `io.warn` interactively; `--help` documents it | intent.md SC11 (G5) | **PARTIAL** | Verified: `--skills harny-propose` → exit **2** with a message naming all six always-on ids; `--skills bogus-id` → exit 2; `all`/`none`/`harny-adr` → exit 0; `--help` lists `--skills <list>`. SC11's *interactive* `io.warn` branch is not implemented — `parseSkillList` throws at flag-parse time in both modes — and the warn branch that was built (`src/init.ts:124–133`) is unreachable (AL-P7). |
| R12 | A flagless run writes exactly the contracted default set, and `.sdd/harness.json` round-trips it | intent.md SC12 (G5) | **PASS** | Flagless run → six core + `harny-standards`, `harny-adr` absent from disk entirely. `harness.json` emits `skills` after `gates` at `version: 1`; feeding it back via `--config` reproduces the identical array; a hand-stripped pre-feature config (no `skills` key) resolves to the defaults without error. |
| R13 | The `templates/roles/sdd-*.md` decision is recorded with its argument; no scaffolded pipeline can lose a role's instructions from both places | intent.md SC13 (G6) | **PASS** | § Data Models records the full-body decision with the SL-5 silent-degradation argument and names the `templates-thin-roles` follow-up. Confirmed on real output: all 30 tool artifacts still carry their complete role bodies. |
| R14 | Skill paths honour `--dry-run`, `CONFLICT`/`--force`, determinism, containment and the single trailing newline | intent.md SC14 (G7) | **PASS** | Reproduced all five: `--dry-run` printed 10 `skills/` paths and wrote 0 files; re-running into a populated dir → exit **3** listing skill paths; `--force` overwrites; two identical runs produced a byte-identical tree (`diff -r` clean); every path relative and inside `targetDir`; single trailing `\n` everywhere. |
| R15 | `tests/packaging.test.ts` updated to 22; `npm pack --dry-run` contains every `templates/skills/**` file and nothing under `src/`, `tests/`, `specs/` | intent.md SC15 (G8) | **PASS** | Ran `npm pack --dry-run` myself: exactly 22 `templates/**` entries including all eleven under `templates/skills/`; zero `src/`, `tests/`, `specs/` entries. `EXPECTED_TEMPLATE_FILES` is 22 and pinned with `toHaveLength(22)`. |
| R16 | The `Generator`-interface decision is stated; if the interface changed, the TG-1 amendment is recorded by name; all five ids still resolve | intent.md SC16 (G9) | **PASS** | D3 states the decision and its rejected alternatives; S1 records the TG-1 amendment by name. `availableToolIds()` still returns exactly the five ids from an immutable `ReadonlyMap`. |
| R17 | `templates/` describes one archive lifecycle; no surviving "archive in place" / "Do NOT move" instruction contradicts SW-7 | intent.md SC17 (G10) | **PASS** | Independent recursive grep over `templates/` for `archive the spec in place\|archive in place\|do NOT move\|keep … in place` → no match. `sdd-documentation.md`, `harny-document` and `harny-sync` all describe stamp-then-hand-off. |
| R18 | A zero-skill run is byte-identical to the pre-change build **except** the five `sdd-documentation` artifacts (Amendment A1); tool-artifact count stays 30 | intent.md SC18 (G11) + contract.md Amendment A1 | **PASS** | `git diff templates/` shows **exactly one** file touched — `templates/roles/sdd-documentation.md`, three hunks (lines 6, 19, 43), all confined to the archive-lifecycle prose S5 names. Conductor body and the other four role bodies byte-for-byte unchanged. `--tools all --skills none` → 60 files = 30 tool + 6 shared + 24 skill. |
| R19 | `npm run typecheck` and `npm test` pass; `src/vocabulary.ts` imports nothing; no import cycle | intent.md SC19 (G11) | **PASS** | Ran all three myself after a fresh `npm run build`: build clean, `tsc --noEmit` clean, **335 passed / 1 skipped / 0 failed** across 24 files, offline. `src/vocabulary.ts` has zero `import`/`require`. **Post-audit update:** after the AL-P1/AL-P2/AL-P5 fixes, re-ran fresh: **343 passed / 0 skipped / 0 failed** across 24 files (the retired test deleted, six new fidelity-sweep assertions and two new neutrality-sweep assertions added). |
| R20 | This feature's five spec files satisfy SW-2 traceability and are archivable by `harny-sync` | intent.md SC20 (G12) | **PARTIAL** | **Post-audit update:** AL-P5 resolved — § SUPERSEDES now carries row S7 recording the `codex-generator` Gu 12 test retirement, closing that half. Still open: § Data Models' divergence table's other rows remain the pre-implementation prediction rather than the post-hoc record (AL-P4, MEDIUM, carried forward), which would freeze inaccurately if archived now. |
| R21 | The declared SL-4 divergence is honoured: `.agents/skills/` keeps 1,536; only `templates/skills/` is tightened to 1,024 | intent.md § Problem Statement (declared contradiction); contract.md S6 | **PASS** | `tests/skill-library.test.ts:46` still `DESCRIPTION_MAX_LENGTH = 1536` for the dogfood tree; `tests/skills-templates.test.ts:39` uses 1024 for `templates/skills/`. Two caps, two trees, no repeal. |

## Contract Compliance

| ID | Contract Item | Status | Verified By |
|---|---|---|---|
| C1 | Gu 1 — every skill artifact is a regular file; no `fs.symlink`/`fs.link` anywhere in `src/` | **PASS** | `grep -rE "symlink\|\.link\(\|linkSync" src/` → no match. `find <target> -type l` over a real all-five-tools run → 0. Every one of the 27 artifacts `lstat`s as a regular file. |
| C2 | Gu 2 — placement matches each tool's verified root (V6, V9, V10, V11, V13) | **PASS** | Five real runs: `claude-code` alone → `.claude/skills` only; `kiro` alone → `.kiro/skills` only; `cursor,codex,github-copilot` → `.agents/skills` only; all five → exactly those three. |
| C3 | Gu 3 — one copy per distinct root, per run | **PASS** | `skillRootsFor` = `[...new Set(map(g => g.skillsDir))].sort()`; observed 3 roots for five tools, 1 for the shared subset, 1 for each single tool. Never five, never one-per-run. |
| C4 | Gu 4 — six portable frontmatter keys only, no Claude-Code-only key | **PASS** | Hand-extracted top-level keys from all eight files; the union is exactly the six portable fields. No `disable-model-invocation`, `context`, `paths`, `model`, `when_to_use`, `agent`. |
| C5 | Gu 5 — folder name equals `name:`; both spec-legal | **PASS** | Hand-checked all eight; see R4. |
| C6 | Gu 6 — `description` ≤1,024 | **PASS** | Hand-measured; max 735 (`harny-sync`). |
| C7 | Gu 7 — `compatibility` ≤500 | **PASS** | Hand-measured; max 231 (`harny-sync`). |
| C8 | Gu 8 — five required body sections, in order | **PASS** | Read all eight bodies; heading order correct in every file. |
| C9 | Gu 9 — whole-file identity to source (not substring containment) | **PASS** | `cmp` of every written artifact in all three roots against `templates/skills/**` → 0 differences. `buildSkillFiles` pushes `file.contents` unmodified; no concatenation anywhere in `src/engine.ts:130–158`. |
| C10 | Gu 10 — only the path differs between copies | **PASS** | Same `cmp` sweep across `.agents/`, `.claude/`, `.kiro/` — all three roots byte-equal the source, therefore each other. |
| C11 | Gu 11 — declared-divergence fidelity between the two skill trees | **PASS** | **Post-audit update (AL-P1/AL-P2 resolved):** post-fix measured state — 4 of 11 pairs byte-identical (`harny-propose`, `harny-test`, `capability-template.md`, `adr-template.md`), 7 diverge (`harny-audit`, `harny-implement` newly diverging post-AL-P1-fix, `harny-adr`, `harny-document`, `harny-standards`, `harny-sync`, `README.md`), every divergence within a declared, corrected class. The guarantee's mechanism is now in place: `tests/skills-fidelity.test.ts` is exhaustive by construction — a `DIVERGENCE_TABLE` entry is required for every discovered name (fails loudly on a gap), covering all nine `SKILL.md`/`README.md` pairs plus the two bundled resources. |
| C12 | Gu 12 — bundled resources travel with their skill into every root | **PASS** | `harny-sync/capability-template.md` present beside its `SKILL.md` in all three roots on a real run; `adr-template.md` likewise under `--skills all` (69-file run). |
| C13 | Gu 13 — the shape-contract README ships into every distinct root | **PASS** | `README.md` present once per root in every run; byte-equal to `templates/skills/README.md`. |
| C14 | Gu 14 — core skills structurally undeselectable | **PASS** | `mergeConfig` computes `withCoreSkills([...CORE_SKILL_IDS, ...optional])` — core membership is not expressible as absent. `--skills none` still writes the six core skills (60-file all-tools run = 24 skill artifacts = 8 per root). |
| C15 | Gu 15 — `--skills` follows CLI-3's USAGE/warn shape | **PARTIAL** | The USAGE half is exact and verified (exit 2, message names all six core ids and both optional ids, distinct from the unknown-id message). The `io.warn` half exists in `src/init.ts:124–133` but is unreachable, and intent.md SC11's stated interactive behaviour is not what ships (AL-P7). |
| C16 | Gu 16 — defaults are `CORE_SKILL_IDS` + `harny-standards` | **PASS** | Flagless run writes exactly seven skills; `harny-adr` absent from disk and from `harness.json`. |
| C17 | Gu 17 — config round-trip survives; absent `skills` key resolves to defaults at version 1 | **PASS** | Serialized `harness.json` fed back through `--config` reproduces the identical `skills` array; a hand-stripped pre-feature config resolves to the defaults at `version: 1`. |
| C18 | Gu 18 — determinism, containment, single trailing newline, sorted ordering | **PASS** | Two independent `--tools all --skills all` runs → `diff -r` clean. Every skill path relative, inside `targetDir`, single trailing `\n`. Roots sorted in `skillRootsFor`; per-skill files sorted by `name`; skills ordered by `SKILL_IDS` index. |
| C19 | Gu 19 — conflict detection and `--dry-run` cover skill paths | **PASS** | Re-run into a populated target → exit **3**, conflict list includes skill paths, nothing written. `--dry-run` printed the skill paths and wrote 0 files. |
| C20 | Gu 20 — roots derive from resolved generators only | **PASS** | `skillRootsFor(resolvedGenerators)` is called on the post-skip list at `src/init.ts:201`; `skillRootsFor([])` → `[]`. |
| C21 | Gu 21 — `runInit` still has 13 steps; skills are a third clause of step 11 | **PASS** | Read `src/init.ts` end to end: step numbering 1–13 intact; the new emission is one `files.push(...)` inside step 11. The one insertion (`6b`) is an interactive re-check beside the existing step-6 re-check, not a new numbered step. |
| C22 | Gu 22 — tool-artifact mechanism untouched; 30 artifacts with all five tools | **PASS** | `git diff src/generators/` shows one added field per generator file and one added interface member — nothing else. `roleFileName`/`mapModel`/`mapCapabilities`/`renderRole`/`renderConductor`/both serializers untouched. Real all-five run emits exactly 30 tool artifacts. |
| C23 | Gu 23 — no generated configuration block or provenance comment in any skill artifact | **PASS** | Written artifacts are byte-equal to source, which contains no `harny:begin`. Confirmed on the real tree. |
| C24 | Gu 24 — `templates/` never mutated, including `templates/skills/` | **PASS** | `src/templates.ts` uses only `readFile`/`readdir`/`existsSync`. No write API anywhere under `src/` targets `templates/`. `git status` after five real runs shows no change under `templates/`. |
| C25 | Gu 25 — the archive lifecycle is single-valued across `templates/` | **PASS** | Independent recursive grep, see R17. |
| C26 | Gu 26 — packaging manifest closed and correct at 22 `templates/**` files | **PASS** (closure caveat) | `npm pack --dry-run` run by me: 22 `templates/**`, 0 `src/`/`tests/`/`specs/`. Caveat: the test asserts *containment* of 22 named paths, not that no 23rd `templates/**` file exists — the manifest is enumerated but not enforced closed (AL-P9). |
| C27 | Gu 27 — no dependency drift | **PASS** | `git diff package.json package-lock.json` → empty. |
| C28 | Gu 28 — no import cycles; `src/vocabulary.ts` imports nothing | **PASS** | `src/vocabulary.ts` has zero import statements. `tsc` clean; `src/config.ts` still imports `CanonicalTemplates` type-only, keeping config↔templates acyclic. |
| C29 | Gu 29 — all five tool ids still resolve, each now carrying `skillsDir` | **PASS** | `availableToolIds()` → the five ids from an immutable `ReadonlyMap`; each generator carries the pinned value. |
| C30 | Gu 30 — shipped skill content is stack-neutral and tool-neutral | **PARTIAL** | **Post-audit update:** AL-P1 resolved — `harny-audit:79–80`, `harny-implement:74–75, 85` and `harny-standards:6–7, 25` no longer name harny's own standard ids or count; `grep -nE "S1|S7|seven" templates/skills/harny-{audit,implement,standards}/SKILL.md` → no match. Still open, two portability defects: `harny-test:14, 53, 115`'s unhedged dependency on a `high-value-tests` skill that is never scaffolded (AL-P3, MEDIUM, deferred by design), and `harny-implement:46` / `harny-propose:37` naming `CLAUDE.md` as *the* conventions source of truth where `templates/roles/sdd-executor.md:19` and `sdd-architect.md:19` already say "`CLAUDE.md`, `AGENTS.md`, or this project's equivalent conventions doc" (AL-P12, MEDIUM, **new in round 2** — my round-1 sweep covered `AGENTS.md` but not `CLAUDE.md`). |
| C31 | D1 — `allowed-tools` kept uniformly; the Kiro copy is not stripped | **PASS** (format caveat) | Verified in all three written roots, not just the source: the same six `allowed-tools:` lines appear byte-identical under `.agents/skills/`, `.claude/skills/` and `.kiro/skills/`. `harny-standards` omits it in both trees, as before. Caveat: the values are comma-separated while V4 pins the portable format as space-separated (AL-P6). |
| C32 | D2 — `githubCopilotGenerator.skillsDir` is `.agents/skills`, fixed and selection-independent | **PASS** | `src/generators/github-copilot.ts:138` is a bare string literal on a `readonly` member of a module-level `const`, registered into a `ReadonlyMap`. Its only consumer is `skillRootsFor` (`src/engine.ts:113`), which reads it unconditionally. `grep -rn skillsDir src/` returns 7 lines total — no conditional, no reassignment, no `config.tools` reference. Confirmed empirically: `--tools github-copilot` alone and `--tools claude-code,github-copilot` both place skills at `.agents/skills`. |
| C33 | D3 — the interface gained `skillsDir` and no `renderSkill`; emission lives in `src/engine.ts` | **PASS** | `src/generators/types.ts:48` is the only added member; no `renderSkill` exists. `buildSkillFiles`/`skillRootsFor` live in `src/engine.ts` beside `buildSharedFiles`. |
| C34 | D4 — TG-3/TG-4 wording unchanged; skills governed by Gu 9/Gu 10 instead | **PASS** | `specs/current/tool-generators.md` untouched (`git status` clean for `specs/current/`). Skills are asserted with whole-file equality, never substring containment. |
| C35 | S1–S7 — every supersession applied and recorded, none left implicit | **PASS** | **Post-audit update (AL-P5 resolved):** § SUPERSEDES now carries row S7, recording the `codex-generator` Gu 12 zero-diff regression test's retirement, its trigger (S1/D3), and where its surviving guarantees now live. All seven supersessions are applied and traceable; none left implicit. |
| C36 | Amendment A1 — G11/SC18 scoped to mechanism; exactly one canonical body changed | **PASS** | `git diff templates/` → one file, three hunks, all archive-lifecycle prose. Conductor and the other four role bodies byte-identical. Artifact count still 30. |
| C37 | Error Handling Contract — every row's condition produces the stated code and exit | **PASS** (test-strength caveat) | Exercised live: unknown id → 2; core id → 2 naming the always-on set; conflict → 3 with nothing written; `--tools all` with no generator is unreachable so `NO_GENERATOR` was checked by code path only. The selected-skill-with-no-template row is `HarnessError('TEMPLATE')` at `src/engine.ts:85–90`, but its test never asserts `code === 'TEMPLATE'` (AL-P10). |
| C38 | § Data Models artifact counts — 21 / 33 / 63 / 69 / 60 observed for the five named selections | **PASS** | Reproduced all five exactly with real CLI runs: 21, 33, 63, 69, 60. |
| C39 | § Data Models default-set argument — `harny-standards` on, `harny-adr` off, each justified from its own `compatibility` field | **PASS** | Both arguments check out against the shipped `compatibility` strings I read: `harny-adr` does declare a dependency on an already-archived feature and `specs/current/_index.md`; `harny-standards`' precondition is satisfiable day one and its Guardrails do require reporting the fallback. |
| C40 | § Data Models role decision — full-body role files retained; `templates/` gains no thinned layer | **PASS** | No thinned layer exists; all five `templates/roles/sdd-*.md` retain full bodies and are carried whole into all 30 artifacts. |

## Test Coverage

| ID | Test Description | Status | Test File |
|---|---|---|---|
> Verified with a fresh `npm run build` first (per AL-20), then `npm test`:
> **335 passed / 1 skipped / 0 failed** across 24 files, fully offline. The single skip is
> `tests/canonical-fidelity.test.ts:477` — see AL-P5.
>
> **Post-audit update (2026-09-10, executor):** after the AL-P1/AL-P2/AL-P5 fixes below,
> re-verified with a fresh `npm run build` then `npm test`: **343 passed / 0 skipped /
> 0 failed** across 24 files, fully offline. The formerly-skipped test is deleted, not
> merely skipped (see AL-P5's Resolution and T32).

| ID | Test Description | Status | Test File |
|---|---|---|---|
| T1 | Six-key frontmatter only, no Claude-Code-only key (Task 4.5) | **PASS** | `tests/skills-templates.test.ts:105–124` — glob-discovered, never a hardcoded name list; independently corroborated by hand. |
| T2 | Folder name equals `name:` and is spec-legal (Task 4.6) | **PASS** | `tests/skills-templates.test.ts:126–148` — asserts all six sub-rules separately. |
| T3 | `description` ≤1,024 and `compatibility` ≤500 (Task 4.7) | **PASS** | `tests/skills-templates.test.ts:150–183` — also asserts non-empty. |
| T4 | Five body sections present, in order (Task 4.8) | **PASS** | `tests/skills-templates.test.ts:185–208` — cursor-advancing `indexOf`, so order is genuinely asserted, not just presence. |
| T5 | All five tools → exactly three roots (Task 4.9) | **PASS** | `tests/skills-placement.test.ts:126–165` — exact set equality per root plus explicit "never a fourth/fifth root" assertions. Corroborated by my own CLI run. |
| T6 | `cursor,codex,github-copilot` → exactly one root (Task 4.10) | **PASS** | `tests/skills-placement.test.ts:167–189`. |
| T7 | `claude-code` alone and `kiro` alone write to their own roots only (Task 4.11) | **PASS** | `tests/skills-placement.test.ts:191–233`. |
| T8 | Written artifact byte-equals source, read independently of the loader (Task 4.12) | **PASS** | `tests/skills-placement.test.ts:235–263` — reads both sides with `node:fs`, never via `loadCanonicalTemplates`; guards against vacuity with a `comparisons > 0` assertion. |
| T9 | Copies across roots byte-equal each other (Task 4.13) | **PASS** | `tests/skills-placement.test.ts:265–294`. |
| T10 | `lstat` proves no artifact is a symlink (Task 4.14) | **PASS** | `tests/skills-placement.test.ts:296–330` — asserts both `isSymbolicLink() === false` **and** `isFile() === true`, correctly avoiding the AL-S2 conjunction tautology the `sdd-skill-library` audit caught. |
| T11 | Bundled resources land beside their `SKILL.md` in every root (Task 4.15) | **PASS** | `tests/skills-placement.test.ts:332–353`. |
| T12 | README written once per distinct root (Task 4.16) | **PASS** | `tests/skills-placement.test.ts:355–377`. |
| T13 | Declared-divergence fidelity between the two skill trees (Task 4.17) | **PASS** | **Post-audit rewrite (AL-P2 resolved) — re-verified by the auditor's own mutation testing, not by accepting the executor's report.** `tests/skills-fidelity.test.ts:155–290` is now exhaustive by construction: `allCoveredNames()` derives from `fs.readdirSync` (never a hardcoded list), a dedicated test requires a `DIVERGENCE_TABLE` entry for every discovered name, and `it.each` exercises all nine pairs — 4 tests → 10, which accounts for most of the 335 → 343 delta. Four auditor mutations, each reverted and the trees confirmed restored: AL-P1's exact residue back into `harny-audit` (previously uncovered) → RED; arbitrary prose into `harny-test` (previously uncovered) → RED at the `byte-identical` branch; a tenth skill in both trees → RED twice on the missing-entry check; a tenth skill in `templates/` only → RED at the bijection. Residual (recommendation, not a finding): the `diverges` branch is a one-sided needle check, so drift elsewhere in those seven files is still undetectable. |
| T14 | Core skills undeselectable via flag, config and prompt (Task 4.18) | **PARTIAL** | `tests/config.test.ts:381–437` covers the flag and config-file halves properly. The prompt half is not asserted: `tests/prompts.test.ts` checks that Q3's option list excludes core ids, but never asserts `runInitPrompts`' returned `result.skills` contains `CORE_SKILL_IDS`. Structurally guaranteed by `src/prompts.ts:182–183`, so low risk. |
| T15 | `--skills` USAGE on core and unknown ids; `all`/`none` resolve (Task 4.19) | **PASS** | `tests/config.test.ts:334–379` — distinguishes the core-id message from the unknown-id message. I additionally confirmed exit code 2 through the real CLI in both cases. |
| T16 | Flagless default set is the core six plus `harny-standards` (Task 4.20) | **PASS** | `tests/config.test.ts:308–320` — asserts both presence of `harny-standards` and absence of `harny-adr`. |
| T17 | Config round-trip; pre-feature config with no `skills` key (Task 4.21) | **PASS** (weak) | `tests/config.test.ts:439–465`. The round-trip assertion is per-id `toContain`, not array equality, so it would not catch a leaked extra optional id; Gu 17 says "reproduces the same `skills` array." I verified exact equality myself through the CLI. |
| T18 | Six-question ordering and the `--skills` preset-skip branch (Task 4.22) | **PASS** | `tests/prompts.test.ts` — Q3 asserted at `multiselect.mock.calls[2]` with `required: false`, `initialValues === DEFAULT_OPTIONAL_SKILL_IDS`, options exactly `OPTIONAL_SKILL_IDS` and no core id; Q5 correctly re-indexed to `calls[3]`. The `clearAllMocks` → `resetAllMocks` fix genuinely removes a cross-test once-value leak. |
| T19 | `skillRootsFor` dedupes, sorts, and ignores skipped tools (Task 4.23) | **PASS** | `tests/engine.test.ts:167–186` — scrambled input, duplicate value, and the empty-generators case. |
| T20 | `buildSkillFiles` emission order; no `harny:begin` marker in any artifact (Task 4.24) | **PASS** | `tests/engine.test.ts:188–252` — deliberately scrambled `payload.skills` input proves emission follows `SKILL_IDS`, not input order; full expected path array asserted. |
| T21 | `buildPayload` raises TEMPLATE for a selected skill with no template (Task 4.25) | **PASS** (weak) | `tests/engine.test.ts:150–176` asserts `isHarnessError` and that the message names the skill id, but never `code === 'TEMPLATE'` — so a `USAGE` regression would pass. Implementation is correct (`src/engine.ts:85–90`). |
| T22 | Skill `files` sorted independently of filesystem order; absence tolerated at load (Task 4.26) | **PASS** (weak) | `tests/templates.test.ts` — tolerated-absence and no-`skills/`-directory cases are strong. The "independent of filesystem enumeration order" case never varies enumeration order; it only asserts the resulting order. |
| T23 | Every generator exposes `skillsDir`; the five pinned values match (Task 4.27) | **PASS** | `tests/generators/registry.test.ts:54–69` pins all five against `contract.md`'s table. Uses `(gen as any).skillsDir` casts left over from the red phase, which are no longer needed and weaken the type-level signal (AL-P10). |
| T24 | `--dry-run` lists skill paths and writes nothing; `CONFLICT` and `--force` (Task 4.28) | **PASS** | `tests/init.test.ts` — all three cases, including asserting the pre-existing file is still untouched after the CONFLICT. Reproduced by me through the real CLI. |
| T25 | Determinism, containment, single trailing newline for skill artifacts (Task 4.29) | **PASS** | `tests/init.test.ts` — plus my own `diff -r` between two identical runs. |
| T26 | Shipped skill content names no harny-specific language, runner or foreign tool path (Task 4.30) | **PARTIAL** | **Post-audit update:** AL-P1 resolved — `NEUTRALITY_CHECKS` extended with `harny-audit/SKILL.md` (`seven standards (S1`, `S1–S7`) and `harny-implement/SKILL.md` (`S1–S6`) entries, and the `harny-standards` entry gained `seven standards`/`seven coding standards`; the check list now covers 6 files. Still open: `high-value-tests` in `harny-test` has no needle yet (AL-P3, MEDIUM, carried forward — not fixed this round per instruction). |
| T27 | No "archive in place" / "Do NOT move" survives anywhere under `templates/` (Task 4.31) | **PASS** | `tests/skills-templates.test.ts:294–311` — recursive over all `templates/**/*.md`, not a fixed file list. Independently re-grepped. |
| T28 | Packaging manifest is 22 files; `src/`, `tests/`, `specs/` still excluded (Tasks 4.2, 4.3) | **PASS** | `tests/packaging.test.ts:37–80`. `npm pack --dry-run` run independently: 22 / 0 / 0 / 0. Containment-only, not closure (AL-P9). |
| T29 | Exact e2e path sets and totals: 21 / 33 / 63 / 69 / 60 (Task 4.4) | **PASS** | `tests/e2e-init.test.ts` — exact expected-set equality per tool plus `isSkillLibraryPath`, which correctly separates the skill library from the same-directory conductor artifact for Claude Code, Kiro and Codex. All five totals reproduced by me through the spawned CLI. |
| T30 | Four role bodies and the conductor body unchanged; only `sdd-documentation` differs (Task 4.33) | **PASS** | `tests/canonical-fidelity.test.ts` T40/T41 body-fidelity blocks re-derive from the raw template on disk, so they carried through the S5 edit as designed. Independently confirmed with `git diff templates/`: one file, three hunks. |
| T31 | 30 tool artifacts with all five tools; dependencies byte-identical and git-clean (Task 4.34) | **PASS** | `tests/e2e-init.test.ts` (30 tool artifacts inside the 63-file set) and `tests/packaging.test.ts`. `git diff package.json package-lock.json` empty. |
| T32 | *(auditor-added row)* The retired `codex-generator` Gu 12 zero-diff regression check | **N/A — deleted, resolved** | Round 2: the `describe`/`it` block is gone from `tests/canonical-fidelity.test.ts` entirely, not disabled. `grep -rn "it\.skip\|describe\.skip\|test\.skip\|\.only(" tests/` returns nothing and the suite reports **0 skipped** (was 1). Its retirement is recorded in `contract.md` § SUPERSEDES row S7, with a pointer comment left at the former location. Closes AL-P5. |

Additional test-writer note: `tests/fixtures/templates/well-formed/skills/**` (seven minimal skill
stubs: the six `CORE_SKILL_IDS` plus `harny-standards`) was created per `roadmap.md`'s File Change
Map so that `tests/init.test.ts`'s and `tests/templates.test.ts`'s full-`runInit`/loader tests have
something to load once Phase 2/3 land — it is fixture data, not production content, and is never
asserted against the shape rules in T1-T4 (those run only against the real `templates/skills/`
tree).

## Audit Log

| Date | Auditor | Finding | Severity | Resolution |
|---|---|---|---|---|
| 2026-09-10 | sdd-auditor | **AL-P1 — Three of the eight shipped skills still carry harny's own coding-standard ids, which DC-1 names by name as text that must be neutralized.** `templates/skills/harny-audit/SKILL.md:79–80` instructs a scaffolded repo's auditor to "Run the `harny-standards` skill and check all **seven standards (S1–S7)**"; `templates/skills/harny-implement/SKILL.md:74–75` tells the executor to "confirm the change satisfies **S1–S6** (the executor's subset of the coding-standards checklist)" and `:85` repeats "`harny-standards` **S1–S6** were checked"; `templates/skills/harny-standards/SKILL.md:6–7` (the frontmatter `description`, which is what a harness selects on) and `:25` both still assert "the same **seven** standards". `contract.md` § Data Models DC-1 reads, verbatim: "Text naming harny's own language, test runner, **or standard ids (`S1`–`S7`**, …) is replaced by a stack-neutral equivalent, because a scaffolded repo may be any language." The divergence table nevertheless classifies `harny-audit` and `harny-implement` as "Near-identical — DC-2 only", so the table positively asserts a state that is false as shipped. Three aggravating facts: (1) it is **self-contradictory** — the very Step 3 the executor neutralized in `harny-standards` now says "The exact standards — their count, their ids, and what each covers — are whatever this project's own conventions document declares", while the two skills that invoke it hardcode seven and six; (2) it is a **regression against what `npx harny init` ships today** — I grepped `templates/roles/*.md` and `templates/conductor/*.md` and they contain no `S1`–`S7` residue at all, so this feature newly introduces harny's standard ids into scaffolded output; (3) it lands in the executor and auditor, which `contract.md` § "the default optional-skill set" itself calls "the pipeline's two most consequential roles". This is precisely the failure `intent.md` § Problem Statement item 5 names as Phase 1's reason to exist. Task 1.6's DC-2 sweep and the T26 neutrality check both look only at `harny-standards`, `harny-sync`, `harny-adr` and `README.md`, so nothing caught it | **HIGH** | Neutralize the three files under DC-1 the way `harny-standards`' Steps already were: in `harny-audit` replace "check all seven standards (S1–S7)" with "check every standard that project's conventions document declares"; in `harny-implement` replace both "S1–S6" occurrences with "every standard that document marks as binding on implementation work"; in `harny-standards` replace "the same seven standards" / "the same seven coding standards" with "the same standards". Then extend `tests/skills-templates.test.ts`'s `NEUTRALITY_CHECKS` with entries for `harny-audit/SKILL.md` and `harny-implement/SKILL.md` (needles `S1–S7`, `S1–S6`) and add `seven standards` to the `harny-standards` entry, so the sweep would have caught this | **RESOLVED (2026-09-10, executor).** `harny-audit`'s Step 6 now reads "check every standard that project's conventions document declares"; `harny-implement`'s Step 4 and Final checklist now read "every standard that document marks as binding on implementation work" / "`harny-standards` was checked"; `harny-standards`' `description` and body both now read "the same standards" (no count). `contract.md` § Data Models' divergence table row for `harny-audit`/`harny-implement` corrected from "DC-2 only" to "**Diverges** — DC-1 (corrected post-audit, AL-P1)", naming the specific residue removed. `tests/skills-templates.test.ts`'s `NEUTRALITY_CHECKS` extended with `harny-audit/SKILL.md` (`seven standards (S1`, `S1–S7`) and `harny-implement/SKILL.md` (`S1–S6`) entries, and `harny-standards/SKILL.md`'s entry gained `seven standards`/`seven coding standards`. `tests/skills-fidelity.test.ts` (AL-P2's fix, same round) independently also covers this residue via its own `DIVERGENCE_TABLE` entries for these two files. Verified: `grep -nE "S1|S7|seven" templates/skills/harny-audit/SKILL.md templates/skills/harny-implement/SKILL.md templates/skills/harny-standards/SKILL.md` → no match |
| 2026-09-10 | sdd-auditor | **AL-P2 — `tests/skills-fidelity.test.ts` does not implement Gu 11 / SC5 for five of the nine pairs, and the tree it is meant to guard is simultaneously exempted from the only other guard that covered it.** Read end to end, the suite asserts: name-set bijection; byte-identity for `harny-propose` only; byte-identity for the two bundled resources; three negative residue needles across `harny-standards`/`harny-sync`/`harny-adr`; `allowed-tools` line retention. There is **no assertion of any kind** touching `templates/skills/harny-test/SKILL.md`, `harny-implement/SKILL.md`, `harny-audit/SKILL.md`, `harny-document/SKILL.md` or `README.md` beyond "the directory exists". Those five files could be rewritten arbitrarily — content deleted, contradictions inserted, harny paths reintroduced — and the whole 335-test suite stays green. That is the exact `canonical-role-templates` AL-2 drift failure `intent.md` G2 exists to prevent, and `roadmap.md`'s risk table rates it "**High** if unguarded" while `tasks.md` calls this "the load-bearing test… if one test in this feature must be excellent, it is that one." The gap compounds with the T41 change in `tests/canonical-fidelity.test.ts:221–224`, which now permanently allowlists `relativePath.startsWith('templates/skills/')` out of the non-mutation check — so `templates/skills/**`, the largest body of canonical content in the repo at 11 files, is currently guarded by neither mechanism. (For contrast, the four `harny-test`/`harny-implement`/`harny-audit`/`harny-document` files happen to be byte-identical to their dogfood counterparts today — I verified with `cmp` — which is exactly what makes a byte-identity assertion cheap to add) | **HIGH** | Restructure the suite to be exhaustive by construction rather than by enumeration: iterate the bijection's name set, and for each name require *either* byte-identity *or* a row in a declared-divergence table encoded in the test file itself (a `Map<skillName, DivergenceClass>`), failing on any name that is neither. Concretely today: `harny-propose`, `harny-test`, `harny-implement`, `harny-audit` and both bundled resources assert byte-identity; `harny-standards`, `harny-sync`, `harny-adr`, `harny-document` and `README.md` assert their residue needles. Mutation-test it before declaring it done. Separately, narrow the T41 allowlist to the specific paths this feature adds rather than a `startsWith` prefix, or replace T41 with the content-hash oracle `codex-generator`'s CG-7 already recommended | **RESOLVED (2026-09-10, executor).** `tests/skills-fidelity.test.ts` rewritten to be exhaustive by construction: a `DIVERGENCE_TABLE` keyed by every discovered `harny-*` name plus `README.md` (nine entries total), each either `{ kind: 'byte-identical' }` (`harny-propose`, `harny-test`) or `{ kind: 'diverges', forbiddenInTemplate, requiredInTemplate }` (`harny-audit`, `harny-implement`, `harny-standards`, `harny-sync`, `harny-adr`, `harny-document`, `README.md`) — the additive `harny-document` divergence (DC-3) uses a new `requiredInTemplate` shape since it has nothing to forbid, only a clarifying clause to require. A dedicated `it` asserts every discovered name has a table entry (fails loudly on a gap, e.g. a future 10th skill), and `it.each` then exercises the full nine-pair sweep — up from three. Mutation-tested per the recommendation: appending stray content to a `diverges`-classed file did not trip the specific needle assertions (an inherent, pre-existing limit of needle-based checks, not new here — see report). The T41 `startsWith('templates/skills/')` allowlist narrowing and the CG-7 content-hash-oracle alternative were **not** done this round (Recommendations, not one of the two blocking HIGH findings; carried forward) |
| 2026-09-10 | sdd-auditor | **AL-P3 — `templates/skills/harny-test/SKILL.md` ships a hard, unhedged dependency on a `high-value-tests` skill that `npx harny init` never writes.** Three sites: `:14` (`compatibility`: "and, ideally, a `high-value-tests` skill in the same skill set"), `:53` (Steps: "**Run the `high-value-tests` skill first** — it is the rubric for *whether* a test is worth writing. Run every candidate test through 'the one question' in that skill"), `:115` (Guardrails: "Never write a test that fails 'the one question' in the `high-value-tests` skill"). `high-value-tests` exists only at `.claude/skills/high-value-tests/` in harny's own repo, is git-ignored, is not one of `SKILL_IDS`, and is absent from the shipped `templates/skills/` manifest — so in every scaffolded repo this reference dangles permanently. The mitigating fact is that the rubric body is inlined immediately after `:53`, so degradation is partial rather than total. The damning comparison is `templates/roles/sdd-test-writer.md:34`, shipped by an earlier feature, which handles the identical situation correctly: "If this repo or the target tool vendors a dedicated rubric skill/rule file for this (a `high-value-tests` reference), consult it as the authoritative version of this rubric; **otherwise apply the rubric above directly**." So `templates/` now contains two artifacts giving contradictory guidance about the same non-existent skill, and the newer one is the worse one. Under `AGENTS.md` S7 / Gu 30 clause 1 this names one environment's mechanic as the only possibility in tool-neutral content | **MEDIUM** | Apply `templates/roles/sdd-test-writer.md:34`'s exact hedge to all three sites in `templates/skills/harny-test/SKILL.md`, so the rubric is the primary instruction and the skill is an attributed optional upgrade. Add `high-value-tests` as a needle to that file's neutrality-sweep entry |
| 2026-09-10 | sdd-auditor | **AL-P4 — `contract.md`'s divergence table is still the pre-implementation prediction, not the post-implementation record Task 1.10 required, so the "oracle" Phase 4.7 was told to assert against is inaccurate.** Measured actual state (`cmp`, all eleven pairs): byte-identical — `harny-propose`, `harny-test`, `harny-implement`, `harny-audit`, `capability-template.md`, `adr-template.md`; diverging — `harny-adr`, `harny-document`, `harny-standards`, `harny-sync`, `README.md`. Against that, the table says: `harny-propose` "None expected" (a prediction, not the "byte-identical" SC5 asks for); `harny-test`/`harny-implement`/`harny-audit` "DC-2 only **if** it names a harny-specific path" (a conditional — SC5 requires "either 'byte-identical' or the exact permitted divergence"); `harny-adr` labelled "Near-identical" in the Relationship column although it does in fact diverge in two places; `harny-document` classified **DC-3** ("dogfood-only mechanics… the `.claude/skills/` symlink-bridge instructions") when the shipped change is nothing to do with symlinks and is DC-2 in kind; and `harny-sync`'s row names only the ADR-registry anecdote, omitting its second real divergence — the removal of the "Per `contract.md` § Amendment A2" cross-reference at `:89`, which is also harny-repo history. Task 1.10 is checked `[x]` but was not performed | **MEDIUM** | Rewrite the table from the measured state before this spec set is archived: one row per pair, each stating literally "byte-identical" or naming the class and quoting the change. This also becomes the encoded oracle AL-P2's fix needs |
| 2026-09-10 | sdd-auditor | **AL-P5 — the retirement of `codex-generator`'s Behavior Guarantee 12 regression test is a supersession of a prior archived feature, recorded only in `tasks.md` § Executor deviations and never in `contract.md` § SUPERSEDES.** I verified the *reasoning* independently and it is sound, not a unilateral test weakening: the skipped block (`tests/canonical-fidelity.test.ts:477–502`) asserts `git status --porcelain` is empty for `templates` plus `src/generators/{types,markdown-yaml,claude-code,cursor,kiro,github-copilot}.ts`, and separately for `src` minus `src/generators`. `contract.md` S1 narrows TG-1's no-amendment claim and D3 mandates one new `skillsDir` member on the interface **and on all five generator files**; Phase 1 creates eleven files under `templates/` and S5 edits `templates/roles/sdd-documentation.md`; Phase 3 modifies seven modules outside `src/generators/`. Every one of those is contract-mandated and every one falsifies an assertion in that block, so it can never pass again while these changes are uncommitted and can never be un-skipped afterwards. The re-assertion claim also checks out: `templates/` non-mutation → T41 (same file, still running); canonical body fidelity → T40/T41 body blocks (still running, and they re-derive from disk so they survived the S5 edit); the five `skillsDir` values → `tests/generators/registry.test.ts:54–69`; the two skill trees → `tests/skills-fidelity.test.ts` (subject to AL-P2). One residual gap: nothing now asserts `src/generators/markdown-yaml.ts` and the TOML serializer are untouched, though both are covered behaviourally by `tests/generators/*.test.ts` and are in fact unmodified. Independent corroboration that this test was already known-obsolete: `specs/archived/codex-generator/audit.md:131` records **CG-7** — "The no-regression sweep (T23, C12) is a `git status` oracle and goes silent once the work is committed… It is a *this-feature* gate, not a standing regression test." The defect is therefore bookkeeping, not judgment: § SUPERSEDES' own preamble says "Each item below changes a statement that is currently true. **Nothing here is implicit**", and this retirement is exactly such an item | **MEDIUM** | Add a § SUPERSEDES row (S7): "`specs/archived/codex-generator` Gu 12's zero-diff regression test — retired; the guarantee was scoped to that feature's audit window (CG-7) and is structurally unsatisfiable after S1/D3." **Recommend deleting the `it.skip` block outright rather than keeping it** — see Recommendations | **RESOLVED (2026-09-10, executor).** `contract.md` § SUPERSEDES gained row **S7**, worded per this Resolution and citing CG-7. The `it.skip` block at `tests/canonical-fidelity.test.ts:477–502` deleted outright (not left skipped); a three-paragraph comment stands at the former location naming the retirement, S1/D3, CG-7 and where the surviving guarantees now live, per the Recommendations section below |
| 2026-09-10 | sdd-auditor | **AL-P6 — every shipped `allowed-tools` value uses a comma-separated list, while this feature's own verified fact V4 pins the portable format as a space-separated string.** All seven declaring skills ship e.g. `allowed-tools: Bash, Read, Write, Edit, Glob, WebFetch, WebSearch`, byte-identical across `.agents/skills/`, `.claude/skills/` and `.kiro/skills/` on a real run. `contract.md` V4 states: "`allowed-tools` is a **space-separated string**" per https://agentskills.io/specification. Under that grammar a spec-compliant parser reads the tokens as `Bash,`, `Read,` … — none of which is a real tool name. The values are inherited byte-for-byte from `.agents/skills/`, so this is not a declared-divergence breach and D1 (which only decided *whether to keep the key*) does not reach it. What is squarely this feature's own new work is `templates/skills/README.md:20`, the shape contract a scaffolded user copies from, which documents the non-conforming form as canonical: `allowed-tools: Read, Glob, Grep      # OPTIONAL. Least privilege. Omit to inherit.` The README's opening paragraph simultaneously claims the library is "shaped to the portable Agent Skills specification… so any compliant tool can discover it directly." Not recorded anywhere in the spec set | **MEDIUM** | Decide and record explicitly, the way D1 handled the adjacent question. Either (a) re-verify V4 against https://agentskills.io/specification and, if it holds, convert all seven values to space-separated **uniformly in both trees** (preserving the byte-identity D1 protects) and fix the README example, or (b) record it as a new open reservation with a dated justification for keeping the comma form. Do not leave the shipped shape contract documenting a format the shipped verified facts contradict |
| 2026-09-10 | sdd-auditor | **AL-P7 — `intent.md` SC11's interactive branch is not implemented, and the warn branch that was built in its place is unreachable.** SC11 says "naming an always-on skill through it is a `USAGE` error non-interactively **and an `io.warn` interactively**". As shipped, `parseSkillList` throws `USAGE` inside `buildOverrides` at flag-parse time, before interactivity is ever consulted — I confirmed exit **2** for `--skills harny-propose` in both modes. That matches `contract.md`'s Error Handling table (which states the exit-2 outcome unconditionally) and Gu 15's literal wording ("the same situation arising through an interactive **answer**"), which is vacuous because the Q3 multiselect only offers `OPTIONAL_SKILL_IDS`. So contract and intent disagree and the implementation follows the contract. Separately, the branch Task 3.14 actually built (`src/init.ts:124–133`) warns when a flag-supplied optional skill is absent from the final resolved set — but `src/prompts.ts:88–90` skips Q3 entirely whenever `preset.optionalSkillIds !== undefined`, so a flag-named optional skill is always applied verbatim and the condition can never be true. Unlike its `roleOverrides` model at `:108–118`, which is reachable because Q2 (role membership) and `--model` are different questions, there is no second question that can remove a `--skills` selection. Dead code, and no test exercises it | **MEDIUM** | Reconcile the two documents: either amend SC11 to match the contract's unconditional exit-2 (the behaviour I'd keep — failing fast on an impossible flag is right), or move the core-id check out of `parseSkillList` into `runInit` so it can branch on `options.interactive`. Then either delete `src/init.ts:124–133` as unreachable, or make it reachable by not skipping Q3 when `--skills` is given (which would contradict `contract.md` § Public API — `src/prompts.ts`). Pick one and record it |
| 2026-09-10 | sdd-auditor | **AL-P8 — `AGENTS.md` S6 ("contract ids never appear in test names") is violated by names this feature newly introduces.** `tests/packaging.test.ts:62` `it('includes bin/, dist/, and all twenty-two templates/** files (Gu 26, S4)')` and `:73` `it('excludes src/, tests/, and specs/ entirely, even after the manifest grows (Gu 26)')`; `tests/e2e-init.test.ts` `it('writes nothing at all — not even .sdd/ — and prints the planned file list, including skill artifacts (Gu 14, SC14)')`. `tasks.md` § Notes restates the rule explicitly and claims compliance. Mitigating: contract ids also appear pervasively in `describe(...)` scopes across the new suites (`(Gu 4)`, `(Gu 5)`, `(Gu 11, SC5)`, …), which is indistinguishable from long-standing pre-existing practice in this repo (`describe('npm pack --dry-run (guarantee 20) (T42)')`, `describe('non-mutation… (R16, guarantee 5) (T41)')`), so the standard as written is already systemically unmet and needs clarification rather than a mass rename | **LOW** | Strip the ids from the three `it(...)` names named above (they belong in the file's `Covers:` header, which already lists them). Separately, amend S6 in `AGENTS.md` to say what the repo actually practices — ids permitted in `describe` scopes, forbidden in `it` names — or accept the drift explicitly |
| 2026-09-10 | sdd-auditor | **AL-P9 — a ninth skill directory added to `templates/skills/` is silently never shipped, and no test detects it.** `loadSkillTemplates` (`src/templates.ts:369`) iterates `SKILL_IDS`, so a directory whose name is not a compile-time member is skipped without warning; `tests/packaging.test.ts` asserts *containment* of 22 named paths, never closure, so the tarball would grow silently too; `tests/skills-templates.test.ts` discovers by glob and would happily shape-check the orphan while nothing notices it is never emitted. `audit.md`'s SKP-3 records the closed-vocabulary constraint but frames it as a documentation-honesty issue ("the README's 'nothing else needs to change' promise is weaker in a fork"); this is the concrete silent-failure path it implies | **LOW** | Add one assertion to `tests/skills-templates.test.ts`: the glob-discovered `templates/skills/harny-*` directory set equals `SKILL_IDS` exactly, so an orphan fails loudly at the point of authorship. Optionally make `tests/packaging.test.ts` assert the packed `templates/**` set equals `EXPECTED_TEMPLATE_FILES` rather than merely containing it, which is what "closed manifest" means in Gu 26 |
| 2026-09-10 (round 2) | sdd-auditor | **AL-P1 — RESOLVED.** Re-read all three files in full and swept far wider than the strings originally flagged: `grep -rniE "\b(two\|three\|…\|ten\|[0-9]+) (coding )?standards\b\|\bS[1-7]\b\|S1[–-]S[0-9]\|standard ids" templates/skills/` now returns **no match anywhere in the tree**. `harny-audit:79–81` now reads "check every standard that project's conventions document declares"; `harny-implement:74–76` "every standard that document marks as binding on implementation work" and `:85` "`harny-standards` was checked"; `harny-standards` dropped "seven" from both the `description` (line 7) and the body intro (line 25). Not a string-replacement into something equally wrong — each file reads coherently end to end, and the three now agree with `harny-standards`' Step 3 ("their count, their ids, and what each covers — are whatever this project's own conventions document declares") instead of contradicting it. `contract.md`'s divergence-table rows for `harny-audit` and `harny-implement` are corrected from "Near-identical / DC-2 only" to "**Diverges** / DC-1" with accurate per-row reasons, which I verified against the actual diffs. Shape unaffected: re-measured all eight files — six-key max, folder == `name:`, five sections in order, max `description` 735, max `compatibility` 231 | **RESOLVED** | Closed. Regression now guarded two ways: `tests/skills-templates.test.ts`'s `NEUTRALITY_CHECKS` gained `harny-audit` (`seven standards (S1`, `S1–S7`) and `harny-implement` (`S1–S6`) entries and two more `harny-standards` needles (6 files covered, up from 4), and `tests/skills-fidelity.test.ts`'s `DIVERGENCE_TABLE` encodes the same needles |
| 2026-09-10 (round 2) | sdd-auditor | **AL-P2 — RESOLVED, verified by my own mutation testing rather than the executor's report.** `tests/skills-fidelity.test.ts` is rewritten to be exhaustive **by construction**: `allCoveredNames()` is built from `fs.readdirSync` plus the `README.md` pseudo-entry, a dedicated test requires a `DIVERGENCE_TABLE` entry for every discovered name, and `it.each` then exercises all nine pairs (4 tests → 10). I ran four mutations myself and reverted each: **(1)** reintroduced AL-P1's exact residue ("seven standards (S1–S7)") into `harny-audit/SKILL.md` — a file that previously had *zero* coverage → **RED**, `harny-audit/SKILL.md still carries dogfood-only residue: "seven standards (S1"`; **(2)** injected arbitrary unrelated prose into `harny-test/SKILL.md`, also previously uncovered → **RED** at the `byte-identical` branch; **(3)** created `harny-probe/` in *both* trees with no table entry → **RED twice**, `no DIVERGENCE_TABLE entry for "harny-probe" — a name discovered on disk must be covered explicitly, never silently skipped`; **(4)** created it in `templates/` only → **RED** at the bijection. Both trees confirmed restored afterwards (`git status -- .agents templates` clean apart from the contracted entries; both directories back to 8 skills + README). So it genuinely would have caught the round-1 regression, and a tenth skill cannot pass silently. Residual, recorded as a recommendation not a finding: for the seven `diverges`-classed pairs the check is one-sided (declared needles absent/present), so drift *elsewhere* in those files is still undetectable — an inherent limit of needle checks, materially smaller than the round-1 "no assertion at all" gap | **RESOLVED** (residual → SKP-6, LOW) | Closed. See Recommendations for the optional hardening (bound the `diverges` branch with a pinned content hash, the `sdd-skill-library` T12 manifest-hash precedent) |
| 2026-09-10 (round 2) | sdd-auditor | **AL-P5 — RESOLVED.** `contract.md` § SUPERSEDES gains row **S7**, and I checked it clause by clause against the code: it names the retired block exactly, states the mechanism (S1 narrows TG-1; D3 mandates `skillsDir` on the interface and all five generators; Phase 1 adds eleven `templates/` files; S5 edits `sdd-documentation.md`; Phase 3 modifies seven modules outside `src/generators/`), independently cites `codex-generator`'s own **CG-7** finding, and records the delete-not-skip decision with the reason. The block is genuinely deleted, not disabled — `grep -rn "it\.skip\|describe\.skip\|test\.skip\|\.only(" tests/` returns **nothing**, and the suite reports **0 skipped**. The retirement rationale survives as a comment at the former location naming the S7 row. Re-verified the three cited re-assertions all still run (24/24 files pass): T40/T41 body-fidelity blocks in the same file, `tests/generators/registry.test.ts`'s five pinned `skillsDir` values, and the now-fixed `tests/skills-fidelity.test.ts`. One residual the S7 row does not claim to cover, stated for the record: no test now asserts `src/generators/markdown-yaml.ts` and the TOML serializer are byte-untouched — both are in fact unmodified (`git status`) and are covered behaviourally by `tests/generators/*.test.ts`, so the guarantee's substance holds while its file-identity oracle is gone. S7's hedge ("the guarantees it recorded **that remain load-bearing**") is accurate | **RESOLVED** | Closed. My round-1 delete-rather-than-skip recommendation was followed |
| 2026-09-10 (round 2) | sdd-auditor | **AL-P12 (NEW — my round-1 miss) — two shipped skills name `CLAUDE.md` as *the* conventions source of truth, and the pre-existing role templates already fixed exactly this.** `templates/skills/harny-implement/SKILL.md:46`: "`CLAUDE.md` (repo root), if it exists, is the source of truth for this project's conventions." `templates/skills/harny-propose/SKILL.md:37`: "**Required**: `CLAUDE.md` (repo root), if it exists — treat it as the source of truth for this project's conventions." `CLAUDE.md` is one tool's convention; naming it as the only possibility in tool-neutral content is a direct hit on `AGENTS.md` **S7** and `contract.md` **Gu 30** clause 1. The damning comparison is that the two role templates these skills were extracted from were **already neutralized by an earlier feature** (`canonical-role-templates`' AL-9, which `intent.md` § Prior Art names as one of "the three findings that bind this one"): `templates/roles/sdd-executor.md:19` reads "`CLAUDE.md`, **`AGENTS.md`, or this project's equivalent conventions doc** (repo root), if one exists, is the source of truth…" and `templates/roles/sdd-architect.md:19` the same — otherwise word-for-word identical to the skill text. So `npx harny init` now writes, into one repo, a role file offering three options and its own extracted skill naming one. It also contradicts the freshly-neutralized `harny-standards`, which correctly says "`AGENTS.md`, `CLAUDE.md`, or the project's equivalent" in four places. Mitigating: hedged with "if it exists", and both files immediately fall back ("Otherwise, derive conventions entirely from Step 2's exploration"; "match the repo's existing style"). **I should have caught this in round 1** — my residue grep covered `AGENTS.md` but not `CLAUDE.md`. Same defect class as AL-P1 (shipped skill contradicts its neutralized sibling; regression against content `templates/roles/` already gets right), smaller blast radius | **MEDIUM** | Copy the role templates' exact wording into both skills: "`CLAUDE.md`, `AGENTS.md`, or this project's equivalent conventions doc (repo root)". Add `CLAUDE.md` (repo root), if it exists, is the source of truth` as a `NEUTRALITY_CHECKS` needle for both files so it cannot regress |
| 2026-09-10 | sdd-auditor | **AL-P10 — three new/updated tests are weaker than the guarantee they are filed under.** (a) `tests/engine.test.ts`'s selected-skill-with-no-template case asserts `isHarnessError` and the message text but never `code === 'TEMPLATE'`, so a regression to `USAGE` (exit 2 instead of the contracted exit 5) passes. (b) `tests/config.test.ts`'s round-trip asserts membership per id rather than array equality, so a leaked extra optional id passes, while Gu 17 says "reproduces the same `skills` array" (I confirmed exact equality myself via the CLI). (c) `tests/templates.test.ts`'s "sorts a skill's files by name, **independent of filesystem enumeration order**" never varies enumeration order; it asserts only the resulting order. Also `tests/generators/registry.test.ts` reads `(generator as any).skillsDir` in five places — red-phase casts that are no longer needed now that the interface member exists, and that discard the type-level signal | **LOW** | Assert `code` in (a); assert `toEqual` on the full array in (b); in (c) either drop the unsupported clause from the name or seed a directory whose creation order is reversed; drop the `as any` casts in `registry.test.ts` |
| 2026-09-10 | sdd-auditor | **AL-P11 — one non-reproducible suite failure observed in 1 of 12 full-suite runs.** Recorded for honesty rather than as a diagnosed defect. On one `npm test` invocation the result was `1 failed \| 334 passed \| 1 skipped`; the failing test's identity was not captured, and eleven subsequent consecutive runs (including six run back-to-back specifically to reproduce it) were all `335 passed \| 1 skipped \| 0 failed`. The failing run was the only one issued in the same shell invocation as a bulk `rm -rf` of scratch directories, and several suites in this feature (`tests/skills-placement.test.ts`, `tests/init.test.ts`) create temp trees under `os.tmpdir()`, which in a sandboxed environment can resolve into the same tree, so the most likely cause is auditor-induced interference rather than a product defect. Flagged because an intermittently-red gate erodes the signal the whole pipeline depends on | **LOW** | If it recurs, capture the failing file. Consider giving the temp-dir helpers a repo-scoped or explicitly-isolated base rather than raw `os.tmpdir()`, so an unrelated process cleaning `TMPDIR` cannot perturb a run |

## Carried reservations (seeded at propose time — not findings, and not this feature's to close)

| ID | Reservation | Severity | Source |
|---|---|---|---|
| AL-30 | Cursor/Kiro/Copilot per-tool facts have never been re-verified against a live tool install; a generator writing to a directory a tool never reads fails silently at exit 0. This feature adds skill roots for all three and inherits the same exposure | MEDIUM (human-gated) | `specs/current/tool-generators.md` § Open reservations |
| CG-1 / O4 | Codex facts re-confirmed 2026-09-02 but never loaded into a live Codex CLI install | MEDIUM (human-gated) | `specs/current/tool-generators.md` § Open reservations |
| SKP-1 | `allowed-tools` is retained in the Kiro copy on documentation evidence (V1, V12) rather than live verification; Kiro's page neither lists the key nor states a rule for unrecognized keys (V7). Pre-authorized remedy in `contract.md` D1: remove it uniformly from all roots | MEDIUM | `contract.md` D1 |
| SKP-2 | A scaffolded repo carries each role's guidance twice — full role-file body plus action-shaped skill. Accepted deliberately so that a discovery failure degrades to redundancy rather than instruction loss. Follow-up `templates-thin-roles`, gated on AL-30/CG-1 closing first | MEDIUM | `contract.md` § Data Models — the role decision |
| SKP-3 | `SKILL_IDS` is a closed compile-time vocabulary, so a user adding a ninth skill to `templates/skills/` needs a `src/vocabulary.ts` line — the shipped `README.md`'s "nothing else needs to change" promise is weaker in a fork than in harny's own repo. Consistent with `TOOL_IDS`/`ROLE_IDS` being closed, but worth stating | LOW | `contract.md` § Public API — `src/vocabulary.ts` |
| SKP-4 | The conductor stays a per-tool artifact while `harny-*` skills are shared, so `cursor` and `github-copilot` write a conductor to `.cursor/skills/` and `.github/skills/` while their skills go to `.agents/skills/`. Deliberate (changing it would alter 30 shipped artifacts), but an asymmetry a reader will notice | LOW | `intent.md` § Non-Goals |
| AL-20 | `npm test` alone validates the last-built `dist/`, not current `src/`; this feature changes e2e expectations more than any prior one | MEDIUM | `specs/current/cli-init.md` § Open reservations |

### Auditor's re-check of the carried reservations (2026-09-10)

None of the seven was quietly worsened; each is in the state it was recorded in.

| ID | Re-check | Verdict |
|---|---|---|
| AL-30 | The feature does add three new discovery roots on documentation evidence alone, but the reservation text already says exactly that ("This feature adds skill roots for all three and inherits the same exposure"), and `contract.md` § Data Models' full-body role decision structurally caps the blast radius: I confirmed on real output that all 30 tool artifacts still carry complete role bodies, so a discovery failure degrades to redundancy, never instruction loss | Unchanged, correctly scoped |
| CG-1 / O4 | Codex's `.agents/skills` root is unchanged from the value TG-9 already pinned; this feature adds files to a directory `codex.ts` already writes to | Unchanged |
| SKP-1 | Honoured as decided. I verified `allowed-tools` survives byte-identically in the Kiro copy specifically (`.kiro/skills/harny-*/SKILL.md`), not merely in the `templates/` source, and D1's pre-authorized uniform-removal remedy remains executable in one edit per file | Unchanged (but see AL-P6 on the value *format*, a distinct issue) |
| SKP-2 | Confirmed live: a `--tools all` run writes both the full role bodies and the skill library. The duplication is real, deliberate and exactly as described | Unchanged |
| SKP-3 | Still accurate. AL-P9 records the concrete silent-failure path this reservation implies | Unchanged, extended by AL-P9 |
| SKP-4 | Confirmed live: `.cursor/skills/sdd-conductor/SKILL.md` and `.github/skills/sdd-conductor/SKILL.md` exist while those tools' `harny-*` skills sit in `.agents/skills/`. `tests/skills-placement.test.ts:159–163` and `tests/e2e-init.test.ts`'s `isSkillLibraryPath` both handle the asymmetry explicitly rather than papering over it | Unchanged |
| AL-20 | Respected in this audit: I ran `npm run build` before `npm test`, so the 335/1/0 figures reflect current `src/`. The reservation itself is untouched by this feature | Unchanged |

### Round-2 re-check of every reservation (2026-09-10, after the fix round)

Re-checked all ten (the seven carried plus the three I added in round 1) for regression.
**None was worsened by the fix round; two improved.**

| ID | Round-2 state | Verdict |
|---|---|---|
| AL-30 | Untouched by the fix round — no generator, `skillsDir` value or discovery claim changed (`git diff --stat src/` is byte-for-byte what it was at round 1). Re-confirmed on live output that all 30 tool artifacts still carry full role bodies, so the structural mitigation is intact | Unchanged |
| CG-1 / O4 | Untouched | Unchanged |
| SKP-1 | Re-verified after the three skill edits: `allowed-tools` still present and byte-identical in all three written roots, `harny-standards` still the only omitter | Unchanged |
| SKP-2 | Re-confirmed live on the 63-file run | Unchanged |
| SKP-3 | **Improved.** The closed-vocabulary constraint stands, but its detection gap is now closed — see SKP-7 | Improved |
| SKP-4 | Re-confirmed live | Unchanged |
| AL-20 | Respected again: `npm run build` before every `npm test` in this round; **343 passed / 0 skipped / 0 failed**, reproduced 5×. The reservation itself is untouched | Unchanged |
| SKP-5 | **Unchanged and re-verified open.** All seven declaring skills still ship comma-separated values and `templates/skills/README.md:24` still documents the comma form. Left unfixed by design (MEDIUM, out of scope this round) | Unchanged |
| SKP-6 | **Downgrade HIGH → LOW independently adjudicated and accepted.** For the record, since the coordinator could not locate the original severity: SKP-6 *is* mine, filed at **HIGH** in round 1's "New reservations found by this audit" table, and it had two halves. Half (a), "five of nine pairs have no divergence assertion," is the half that made it HIGH, and it is verifiably closed — I proved it with four of my own mutations, not the executor's report. Half (b), `templates/skills/**` exempted from T41's non-mutation check by a permanent `startsWith` allowlist, is **still open and unchanged** (`tests/canonical-fidelity.test.ts:221–224` verified untouched this round). LOW is justified because T41 goes vacuous the moment this work is committed anyway — that is exactly `codex-generator`'s own CG-7 finding — so the marginal detection lost to the allowlist is close to zero, and the real guard is now `skills-fidelity.test.ts`. I would also have accepted MEDIUM: seven of the eleven pairs are `diverges`-classed and therefore checked only against declared needles, so drift elsewhere in them remains undetectable. LOW stands on the condition that the hardening in Recommendations is treated as a live follow-up, not dropped | **Downgrade accepted**, restated in the auditor's own terms |
| SKP-7 | **Improved — detection half closed.** The concern was that a ninth skill directory would be silently ignored. My probes show all three variants now fail loudly: present in both trees → the `DIVERGENCE_TABLE` coverage test fires ("a name discovered on disk must be covered explicitly, never silently skipped"); present in `templates/` only, or in `.agents/` only → the bijection test fires. This was an unintended but real side benefit of the AL-P2 fix. Residual: `tests/packaging.test.ts` still asserts *containment* of the 22 paths rather than set equality, so the tarball could still grow silently | Narrowed to the packaging-closure half |

### New reservation found in round 2

| ID | Reservation | Severity |
|---|---|---|
| SKP-8 | `templates/skills/harny-implement/SKILL.md:46` and `templates/skills/harny-propose/SKILL.md:37` name `CLAUDE.md` as *the* conventions source of truth, where the role templates they were extracted from already say "`CLAUDE.md`, `AGENTS.md`, or this project's equivalent conventions doc" — an S7 / Gu 30 regression against content `templates/roles/` already gets right. Missed in round 1 by this auditor — see AL-P12 | MEDIUM |

## Final Verdict

> **Round 1 (2026-09-10): REJECTED** on two HIGH findings — AL-P1 (harny's own `S1`–`S7`
> standard ids shipped in three skills) and AL-P2 (the divergence test covered 4 of 9 pairs).
> The verdict below supersedes it after an independent round-2 re-audit of the fix round.

**Status**: **APPROVED WITH RESERVATIONS**

**Summary**: Both blocking HIGH findings are genuinely resolved, and I confirmed that myself
rather than accepting the fix report — a residue sweep far wider than the strings I first
flagged now returns nothing across `templates/skills/`, and four of my own mutations (AL-P1's
exact residue back into `harny-audit`, arbitrary drift into `harny-test`, a tenth skill in
both trees, a tenth skill in one tree) each drove the rewritten `tests/skills-fidelity.test.ts`
red with a precise message, proving it is exhaustive by construction rather than by
enumeration. AL-P5 is closed by an accurate § SUPERSEDES row S7 and an outright deletion of
the dead `it.skip`, leaving **0 skipped** tests. Everything from round 1 re-verified clean:
`build` / `typecheck` / **343 passed / 0 skipped / 0 failed** (reproduced 5×), artifact counts
21 / 33 / 63 / 69 / 60, byte-identity and zero symlinks across all three roots, determinism,
exit codes 2 / 3 / 0, `npm pack` at exactly 22. Approved with reservations rather than
outright because five MEDIUM and four LOW findings remain open by design, and because I found
one new MEDIUM in round 2 that I missed in round 1 (AL-P12).

**Critical Issues** (must fix before merge):

- None. No behavior guarantee is falsified, no interface is missing, and no error-handling row
  misbehaves. Every guarantee governing runtime behavior holds under independent verification.
- **No HIGH findings remain.** Round 1's two are closed and were re-verified by mutation, not
  by report.

**Warnings** (should fix, not blocking):

- **AL-P12 (MEDIUM — new in round 2, and my own round-1 miss)** —
  `templates/skills/harny-implement/SKILL.md:46` and
  `templates/skills/harny-propose/SKILL.md:37` name `CLAUDE.md` as *the* conventions source of
  truth. The role templates these two skills were extracted from were already neutralized for
  exactly this by `canonical-role-templates`' AL-9: `templates/roles/sdd-executor.md:19` and
  `sdd-architect.md:19` say "`CLAUDE.md`, **`AGENTS.md`, or this project's equivalent
  conventions doc** (repo root)", otherwise word-for-word identical. So a scaffolded repo gets
  a role file offering three options next to its own extracted skill naming one, and both
  contradict the freshly-neutralized `harny-standards`. Same defect class as AL-P1, smaller
  blast radius (hedged with "if it exists", with an explicit exploration fallback). My round-1
  grep covered `AGENTS.md` but not `CLAUDE.md`; that was my error, not a new regression.
- **AL-P3 (MEDIUM)** — `templates/skills/harny-test/SKILL.md:14, 53, 115` ships an unhedged
  hard dependency on a `high-value-tests` skill that is never scaffolded, while the
  neighbouring `templates/roles/sdd-test-writer.md:34` handles the same case correctly.
  Re-verified unchanged.
- **AL-P4 (MEDIUM)** — partially corrected: the `harny-audit` and `harny-implement` rows are
  now accurately "**Diverges** / DC-1". The remaining five defects stand — `harny-propose`
  says "None expected" rather than "byte-identical", `harny-test` keeps a conditional "DC-2
  only *if*", `harny-adr` is labelled "Near-identical" though it diverges, `harny-document` is
  classified DC-3 where the change is DC-2 in kind, and `harny-sync`'s second divergence has
  no row. **This is the one reservation with a deadline**: archiving freezes the table.
- **AL-P6 (MEDIUM)** — comma-separated `allowed-tools` values in all seven declaring skills
  contradict this feature's own V4 (space-separated), and `templates/skills/README.md:24`
  documents the non-conforming form as the shape contract. Re-verified unchanged.
- **AL-P7 (MEDIUM)** — `intent.md` SC11's interactive `io.warn` branch is not implemented, and
  the branch built at `src/init.ts:124–133` is structurally unreachable. `src/` was untouched
  this round; re-verified unchanged.
- **AL-P8 / AL-P9 / AL-P10 / AL-P11 (LOW)** — contract ids in `it()` names (a fourth instance
  found this round at `tests/e2e-init.test.ts:471`); the packaging manifest asserted by
  containment rather than closure; three tests weaker than their guarantee plus leftover
  `as any` casts; one non-reproducible suite failure from round 1 that did not recur in any of
  this round's runs.

**Recommendations** (nice to have):

- **Fix AL-P12 and AL-P3 together in one small pass before 2026-09-26.** Both are portability
  defects in `templates/skills/`, both are two-to-three-line edits, and both have a correct
  model to copy sitting in `templates/roles/`. The DevFest portability demo
  (`intent.md` § Constraints — Business) shows a scaffolded repo beside harny's own; these two
  are precisely the kind of inconsistency that segment exposes. Add both as
  `NEUTRALITY_CHECKS` needles so they cannot regress.
- **Harden the `diverges` branch of `tests/skills-fidelity.test.ts`.** All nine pairs now have
  an assertion, but for the seven `diverges`-classed ones it is one-sided: declared needles
  absent/present, with drift elsewhere in the file undetectable. Bound it — pin a content hash
  per diverging shipped file so any unintended change fails and must be consciously re-pinned.
  That is the `specs/archived/sdd-skill-library/audit.md` T12 manifest-hash precedent, and it
  is the condition under which SKP-6's HIGH → LOW downgrade is fair.
- Narrow the T41 allowlist to the specific paths this feature adds instead of a
  `templates/skills/` prefix, or replace T41 with the content-hash oracle CG-7 already
  recommended, so a *future* unsanctioned edit under `templates/skills/` or to
  `templates/roles/sdd-documentation.md` is still caught. (Note that both T41 and the
  retired block go vacuous the moment this work is committed, which is the underlying
  weakness CG-7 named.)
- Consider extracting the `templates/skills/` directory name into `src/vocabulary.ts`
  alongside `SKILLS_README_NAME`; `'skills'` and `'SKILL.md'` are currently literalled in
  `src/templates.ts` and re-literalled in three test files, which is the shape S5 discourages
  even though no single module re-literals today.
- The `.trim()` → `.replace(/\r$/, '')` fix at `tests/canonical-fidelity.test.ts:237` is a
  genuinely good catch and worth calling out: the previous code stripped the leading space of
  a ` M path` porcelain status before a fixed-width `slice(3)`, silently truncating three
  characters of every unstaged-modification path. Worth a line in the CHANGELOG when
  `harny-document` runs.
- `tests/skills-templates.test.ts`' and `tests/skills-fidelity.test.ts`' `normalizeWhitespace`
  guard against soft-wrapped needles is a real improvement over a naive `.includes()`. It was
  correctly reused for AL-P1's new needles and should be the pattern for AL-P3's and AL-P12's.
