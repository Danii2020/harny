# Audit: Component-Level Docs

## Requirements Checklist
| ID | Requirement | Source | Status | Notes |
|---|---|---|---|---|
| R1 | Find the components | G1, SC1 | PASS | Unit tree covers manifests, depth limit, declared, source-root domains, ignore list, hidden dirs, `fixtures`; harny itself resolves to exactly `src/generators`. |
| R2 | Doctor names what is missing | G2, SC2 | PASS | Five-tool monorepo probe: one OK component with both bridges OK, two WARN with exact file. |
| R3 | Every tool loads the one file | G3, SC4 | PASS (Kiro reservation) | Claude Code bridge per first-party docs; Kiro per search-sourced docs (CLD-R1). Native three get nothing. |
| R4 | Init wires what exists | G4, SC3 | PASS | Bridges only for documented components; never overwrites; warns on an import-less `CLAUDE.md`; re-run conflict-free. |
| R5 | Documentation role writes content | G5, SC5 | PASS (inferential) | Identical step in both SKILL roots; tool-neutral with attributed examples. Its execution is judgment, not testable deterministically (CLD-F3). |
| R6 | Dogfood | G6, SC6 | PASS | `src/generators/AGENTS.md` written from evidence; bridge generated by `buildComponentBridgeFiles`; doctor reports both OK; both files tracked. |

## Contract Compliance
| ID | Contract Item | Status | Verified By |
|---|---|---|---|
| C1 | CL-1 one discovery | PASS | `src/component-docs.ts` loads `templates/shared/components.mjs`; the runner imports the shipped copy; no second implementation (grep). |
| C2 | CL-2 discovery rules | PASS | `tests/shared/components.test.ts` |
| C3 | CL-3 doctor entries | PASS | `tests/doctor/run-doctor-components.test.ts` + live probe |
| C4 | CL-4 compatibility | PASS | No `componentDocs`, and no shipped module: no entries, exit 0 (tests) |
| C5 | CL-5 init bridges | PASS | `tests/component-docs.test.ts` (three cases + `runInit`) |
| C6 | CL-6 native tools untouched | PASS | Generator values asserted; probe shows no extra files |
| C7 | CL-7 bridge content | PASS | Exact bytes asserted; Kiro frontmatter now via `renderFrontmatter` (Amendment A1) |
| C8 | CL-8 skill step | PASS | Both roots carry the identical step; the pre-existing six-line root difference unchanged |
| C9 | CL-9 determinism | PASS | Sorted output asserted equal across calls; paths contained |
| C10 | CL-10 dogfood | PASS | See R6 |

## Test Coverage
| ID | Test Description | Status | Test File |
|---|---|---|---|
| T1 | Discovery | PASS | `tests/shared/components.test.ts` |
| T2 | Bridges and init | PASS | `tests/component-docs.test.ts` |
| T3 | Doctor entries | PASS | `tests/doctor/run-doctor-components.test.ts` |

## Audit Log
| Date | Auditor | Finding | Severity | Resolution |
|---|---|---|---|---|
| 2026-09-24 | sdd-auditor (harny-audit) | **Baseline.** Suite **932/932**, typecheck clean; commit `07b9ed7` passed this repo's pre-commit hook. No dependency change. | INFO | — |
| 2026-09-24 | sdd-auditor | **Existing guards did their job.** Implementation first tripped MC-15's no-"component"-vocabulary gate on `src/generators/` and TG-5's no-hand-rolled-frontmatter gate. Both were honored (member renamed `nestedGuidance`, frontmatter via `renderFrontmatter`) and recorded as contract Amendment A1, not weakened. | INFO | — |
| 2026-09-24 | sdd-auditor | **Dogfood caught a real bug.** This repo's `.gitignore` rule `CLAUDE.md` ignored the file at every depth, which would have silently kept every nested Claude Code bridge out of git. Anchored to `/CLAUDE.md`; `git ls-files` confirms the bridge is tracked. | INFO | — |
| 2026-09-24 | sdd-auditor | **CLD-R1 — Kiro facts are search-sourced.** `kiro.dev` is egress-blocked here. `inclusion: fileMatch`, `fileMatchPattern` and `#[[file:…]]` come from search results quoting its steering docs. If wrong, the bridge is inert, never destructive. | MEDIUM (human-gated) | Verify in a live Kiro install (same class as R1/R5). |
| 2026-09-24 | sdd-auditor | **CLD-F1 — Discovery is a heuristic.** `examples/*` or tooling packages with manifests are reported as components too. | LOW | Recommended tier only; tune the `ignore` list in a follow-up if it proves noisy. |
| 2026-09-24 | sdd-auditor | **CLD-F2 — Bridge remediation prints contents as a JSON string** (`\n` escaped). It is exact, but less readable than a block. | LOW | Accept; `harny init` writes the same bytes automatically. |
| 2026-09-24 | sdd-auditor | **CLD-F3 — The documentation role's component step is inferential**; nothing forces it to run. The doctor's warnings are the backstop that surfaces a skipped doc. | LOW | Accept (same posture as RC-R1). |
| 2026-09-24 | sdd-auditor | **CLD-F4 — S6: contract ids in `describe` titles** (the standing AL-4 class). | LOW | AL-4 sweep. |
| 2026-09-24 | sdd-auditor | **CLD-R2 — CI has not run on this change.** | MEDIUM (process) | Confirm green on the PR. |

## Final Verdict

**Status**: APPROVED WITH RESERVATIONS

**Summary**: Components are discovered by one shared, deterministic heuristic. The doctor warns with exact remediations, and `init` wires Claude Code and Kiro to each documented component while leaving the three natively-nesting tools untouched. All of this was verified on a real five-tool monorepo install and dogfooded on harny's own `src/generators`. Remaining: live verification of Kiro's steering syntax and the PR's CI run.

**Critical Issues** (must fix before merge):
- None.

**Warnings** (should fix, not blocking):
- CLD-R1 (MEDIUM): confirm Kiro loads `.kiro/steering/agents-<slug>.md` in a live install.
- CLD-R2 (MEDIUM): confirm the PR's `harny feedback` check is green.

**Recommendations** (nice to have):
- CLD-F1: tune the discovery ignore list if noisy; CLD-F2, CLD-F3, CLD-F4 as noted.
