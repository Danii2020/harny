# Audit: Permissions Baseline

## Requirements Checklist
| ID | Requirement | Source | Status | Notes |
|---|---|---|---|---|
| R1 | One canonical tool-neutral policy | intent.md G1, SC1 | PASS | `templates/permissions/policy.json` is the only rule source; the guard reads the installed copy, and Claude Code's static rules are derived from it (`claudeCodePermissions`). |
| R2 | Guard wired on all five tools | intent.md G2, SC2 | PASS | Rendered and driven per tool in `tests/generators/permissions-guard.test.ts`. |
| R3 | Native deny/ask per tool; fail closed without ask | intent.md G2, SC3, SC4 | PASS | Subprocess-verified per tool. Payload shapes for Cursor, Copilot, Codex and Kiro are verified against docs or source, not against live installs (reservation PB-R1). |
| R4 | Protected-branch git rules | intent.md G3 | PASS | Every refspec spelling tested plus 6 adversarial spellings, all denied. Residual spellings in PB-R3. |
| R5 | Claude Code static rules | intent.md G4, SC5 | PASS | `permissions.deny`/`ask` equal `claudeCodePermissions(policy)`, including the `Read(!.env.example)` carve-out. |
| R6 | Dogfood byte identity | intent.md G5, SC6 | PASS | `tests/canonical-fidelity.test.ts` re-renders `.claude/settings.json` and compares `.sdd/permissions/*`. Also **live**: the guard fired in the auditing session. |
| R7 | Missing/broken policy handling | intent.md SC7 | PASS | Missing → allow plus notice; invalid → deny naming the file; crash → allow (A1-F5 test). |
| R8 | README documents baseline and limits | intent.md SC8 | PASS | `README.md` § Permissions baseline; `templates/permissions/README.md` covers limits and the server-side protection step. |

## Contract Compliance
| ID | Contract Item | Status | Verified By |
|---|---|---|---|
| C1 | PB-1 one canonical policy | PASS | `grep` finds no rule literal or `.sdd/permissions` path outside `templates/permissions/` and `src/permissions.ts`. |
| C2 | PB-2 verbatim install | PASS | `buildPermissionsFiles` test; e2e path sets hold exactly one of each file in every scenario. |
| C3 | PB-3 decision order (+A1) | PASS | Subcommand split, `sh -c` unwrap, wrappers, substitutions and heredoc stripping are tested; a deny anywhere beats an ask. |
| C4 | PB-4 secret reads | PASS | Read tool, four payload shapes, shell read programs, `<` redirections, `.env.example` carve-out. |
| C5 | PB-5 command patterns (+A1) | PASS | Patterns match in three forms; `git -C . push --force` and `/bin/rm -rf` are denied after the A1 fix. |
| C6 | PB-6 protected branches | PASS | commit/merge/cherry-pick on `main`; push by refspec, `HEAD:`, `+`, `refs/heads/`, delete, `--all`, `--mirror`; same-line checkout tracking; no-repo skip. |
| C7 | PB-7 branch globs | PASS | `release/*` matches `release/1.4`, not `release/1/2`. |
| C8 | PB-8 native channels | PASS | Each generated command driven as `sh -c` with that tool's payload; Codex/Kiro ask → deny with `requires human approval:`; Cursor always valid JSON. |
| C9 | PB-9 Claude static rules | PASS | Order and content asserted against `claudeCodePermissions`. |
| C10 | PB-10 missing/broken policy | PASS | Three tests in `run-guard.test.ts`, plus the crash-path test per tool. |
| C11 | PB-11 determinism | PASS | Generated bytes come from templates only; goldens are byte-compared. |
| C12 | PB-12 generator backward compatibility | PASS | `it.each` over five generators: no guard or `permissions` without the payload; existing registrations are deep-equal with it. |
| C13 | PB-13 dogfood | PASS | Fidelity tests; plus the live-session hook firing. |
| C14 | PB-14 tool-neutral README | PASS | § The behavior names no tool; tools appear only under § Attributed examples, dated. |

## Test Coverage
| ID | Test Description | Status | Test File |
|---|---|---|---|
| T1 | Guard: reads, patterns, git rules, payload shapes, errors, A1 spellings | PASS | `tests/permissions/run-guard.test.ts` (22 tests) |
| T2 | `src/permissions.ts` parsing, derivation, files | PASS | `tests/permissions.test.ts` |
| T3 | Each generator's guard command driven as a subprocess | PASS | `tests/generators/permissions-guard.test.ts` |
| T4 | Backward compatibility without `permissions` | PASS | `tests/generators/permissions-guard.test.ts` |
| T5 | Dogfood identity | PASS | `tests/canonical-fidelity.test.ts` |
| T6 | Install path set and counts | PASS | `tests/e2e-init.test.ts` |
| T7 | Wrapper allows on guard crash, per tool | PASS | `tests/generators/permissions-guard.test.ts` (added in round 1) |

## Audit Log
| Date | Auditor | Finding | Severity | Resolution |
|---|---|---|---|---|
| 2026-09-24 | sdd-auditor (harny-audit), round 1 | **Baseline.** Suite 876/876 after commit `7ee2b80`, typecheck clean, doctor 24 ok / 0 failed. No dependency change (`package.json`/lockfile diff empty, S4). No `.sdd/permissions` literal outside `src/permissions.ts` (S5). | INFO | — |
| 2026-09-24 | round 1 | **Live evidence (positive).** This repo's dogfood `PreToolUse` guard fired in the auditing session itself: it denied the auditor's `rm -rf <scratch>` with the policy's reason, and denied a command containing `git push origin main`. So the Claude Code wiring (PB-8/PB-13) is confirmed live, not only by subprocess. | INFO | — |
| 2026-09-24 | round 1 | **A1-F1 — Deny patterns are bypassed by git global options.** `git -C . push --force` and `git -c k=v push --force` are allowed: `judgeGit` skips global options but pattern matching (PB-5) sees the raw text, so `git push *--force*` never matches. Agents use `git -C <dir>` routinely. PB-5 holds as written, but intent G2 and SC3 ("force push denied") do not hold for this spelling. Claude Code's static rules have the same gap. | HIGH | Fix: also match patterns against the git subcommand with global options removed. Amend PB-5. |
| 2026-09-24 | round 1 | **A1-F2 — A program given by path escapes patterns.** `/bin/rm -rf build` is allowed; `rm -rf*` expects a bare `rm`. | MEDIUM | Fix: also match with the program token reduced to its basename. Amend PB-5. |
| 2026-09-24 | round 1 | **A1-F3 — Command substitution is not inspected.** `echo $(cat .env)` is allowed; `$(…)` and backticks are neither split nor judged. | MEDIUM | Fix: judge each `$(…)`/backtick body as additional subcommands. Amend PB-3. |
| 2026-09-24 | round 1 | **A1-F4 — Heredoc bodies are judged as commands (false positive).** Observed live: the guard denied the auditor's `cat > file <<'EOF'` whose body contained `git push origin main`, because newline splitting treats heredoc lines as subcommands. This blocks legitimate writes of docs and scripts. | MEDIUM | Fix: skip heredoc bodies up to their delimiter. Amend PB-3. |
| 2026-09-24 | round 1 | **A1-F5 — Contract error row "guard crashes → wrapper allows" has no test.** The wrapper's non-0/2/3 branch is untested for all five tools. | HIGH | Fix: a test driving each generated wrapper against a guard that exits 1. |
| 2026-09-24 | round 1 | **A1-F6 — Residual spellings.** `xargs rm -rf`, combined short flags (`git commit -nm`), `cd` before git, `git pull` on a protected branch and `push.default=upstream` are not caught. Best-effort by contract (intent Non-Goals). | LOW | Carry as a reservation; `commit-checks` and server-side protection are the backstops. |
| 2026-09-24 | sdd-auditor (harny-audit), round 2 | **Round-1 fixes verified.** A1-F1 to A1-F5 closed by commit on top of `7ee2b80` (contract Amendment A1). The adversarial probe re-run: 22 of 24 spellings now as expected; the two misses are the A1-F6 residuals. Suite **884/884**, typecheck clean, doctor 24 ok / 0 failed. | INFO | — |
| 2026-09-24 | round 2 | **PB-R1 — Payload shapes not verified against live installs.** Cursor's `beforeReadFile` path field is documented as `file_path` by one source and `filePath` by another; the guard reads `file_path` only. Copilot, Codex and Kiro shapes come from SDK docs, Codex source and issue reports. Kiro's `preToolUse` in `.kiro/hooks/*.json` is the least certain (R1/R5 class). A wrong field name makes that registration inert, never wrong. | MEDIUM (human-gated) | Probe each tool live; add `filePath` to the normalizer if confirmed. |
| 2026-09-24 | round 2 | **PB-R2 — CI has not run on this change.** The workflow triggers on `pull_request` and push to `main`; this branch has neither yet. | MEDIUM (process) | Open a PR; confirm `harny feedback` is green with N ≥ 1. |
| 2026-09-24 | round 2 | **PB-R3 — Residual spellings (A1-F6).** `xargs rm -rf`, combined short flags (`git commit -nm`), `cd` before git, `git pull` on a protected branch, `push.default=upstream`. | LOW | Backstops: `commit-checks` (plan #5), server-side branch protection. |
| 2026-09-24 | round 2 | **PB-R4 — Two validators.** `validPolicy` in the guard and `parsePermissionPolicy` in `src/permissions.ts` agree on shape but differ on edge cases (an empty `reason` string is accepted at runtime, rejected at `init`). | LOW | Accept; or make the guard reject empty reasons too. |
| 2026-09-24 | round 2 | **PB-R5 — S6: contract ids in `describe` titles** in all three new test files (the standing AL-4 class). | LOW | Move ids into `Covers:` headers in the AL-4 sweep. |
| 2026-09-24 | round 2 | **PB-R6 — README artifact counts stale.** `README.md` "22 / 68 / 91 files total" was already behind the tests (33 / 87 / 93 after this feature) before this change; this feature adds two shared files. | LOW | Documentation stage: correct the three count lines. |
| 2026-09-24 | round 2 | **Non-goals honored.** No git hooks, no doctor checks, no Codex `config.toml` writes, no Cursor `cli.json`, no write-denial. `.claude/settings.local.json` (user-local, not in this checkout) is left for the human to tighten. | INFO | Manual follow-up for the human. |
| 2026-09-24 | round 2 | **harny-feedback.** The per-turn hook is active in this session (a turn file for this session is present in `.sdd/feedback/.turns/`); `tsc` clean. No CI run exists for this change yet (PB-R2). | INFO | — |


## Final Verdict

**Status**: APPROVED WITH RESERVATIONS

**Summary**: Every contract guarantee (PB-1 to PB-14, with Amendment A1) holds and is tested, including subprocess runs of all five tools' generated guard commands and a live firing of the dogfood guard in the auditing session. Round 1 found two HIGH and three MEDIUM defects; all five were fixed and re-verified in round 2. What remains is live-install verification of four tools' payload shapes, a pending CI run, and documented best-effort limits.

**Critical Issues** (must fix before merge):
- None.

**Warnings** (should fix, not blocking):
- PB-R1 (MEDIUM): verify Cursor, Copilot, Codex and Kiro payload shapes in live installs; Cursor `filePath` in particular.
- PB-R2 (MEDIUM): open a PR so `harny feedback` CI runs on this change.

**Recommendations** (nice to have):
- PB-R3: rely on `commit-checks` and server-side branch protection for the residual spellings.
- PB-R4, PB-R5, PB-R6: small hygiene items (single validator, test titles, README counts).
- Human: tighten `.claude/settings.local.json`'s broad `Read(//Users/danielerazo/**)`, and enable server-side protection on `main`.
