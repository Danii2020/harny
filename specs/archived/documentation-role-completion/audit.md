# Audit: documentation-role-completion

> Seeded by the architect; filled in by `sdd-auditor` on 2026-09-23 against the
> working tree at `HEAD = d3c2741` (nothing committed). Every row cites `intent.md`
> or `contract.md`.

## Requirements Checklist

| ID | Requirement | Source | Status | Notes |
|---|---|---|---|---|
| R1 | The readiness runner accepts a family selector; spec-state-only run exits `0` clean / `2` stranded; an unknown selector is a usage error (exit `1`), never a silently-green run | intent.md SC1 (G1) | PASS | Re-derived independently in a synthetic repo: `--only spec-state` with one stranded fixture exits `2`; after archiving, exits `0`; `--only bogus` exits `1` with `--only "bogus" is not a recognized family (expected one of: environment, harness, repo-readiness, spec-state, tests)`; `--only` with no value exits `1` with `--only requires a value (…)`. Neither error message reuses the generic "unknown flag" wording. All five tokens exercised individually. |
| R2 | Shipped-but-unarchived round trip: fixture feature named and exit `2`; after the move under `specs/archived/`, exit `0` | intent.md SC2 (G1) | PASS | Round trip run by hand: `FAIL spec-state:stranded - specs/stranded is shipped and approved but was never archived (run harny-sync archive mode)` / exit `2`; after `mv specs/stranded specs/archived/`, `summary: 0 ok, 0 skipped, 0 warned, 0 failed` / exit `0`. Also covered by `tests/doctor-runner-only.test.ts`. |
| R3 | The selector-scoped run spawns no command from the tests family | intent.md SC3 (G1) | PASS | Drove the runner with a `commands` entry whose `argv` writes a sentinel file; after `--only spec-state` the sentinel is absent (`NO_SPAWN_CONFIRMED`), while the same checks file under `--only tests` does write it. Absence of a *spawn*, not merely absence of output. |
| R4 | Role template and both skill copies state verification as a precondition on reporting, and name the command producing the verdict | intent.md SC4 (G2) | PASS | `templates/roles/sdd-documentation.md` § "Step 6: Verify Before Reporting Completion"; `templates/skills/harny-document/SKILL.md` and `.agents/skills/harny-document/SKILL.md` Step 3 item 4. All three state "Report … only after confirming, via the spec-state family of the readiness runner shipped at `.sdd/doctor/run-doctor.mjs` — invoked, for example, as `node .sdd/doctor/run-doctor.mjs --only spec-state`". Judged on intent, not only on the pinned substrings — see AL-2. |
| R5 | The precondition text reaches all five generators' `sdd-documentation` artifact verbatim, with no generator source change | intent.md SC5 (G2, G5) | PASS | Scaffolded a real target with `--tools all` and grepped the five concrete artifacts (`.claude/agents/sdd-documentation.md`, `.codex/agents/sdd-documentation.toml`, `.cursor/agents/sdd-documentation.md`, `.github/agents/sdd-documentation.agent.md`, `.kiro/agents/sdd-documentation.md`). **All four precondition elements present in all five**, plus `harny-adr`. `git status --porcelain -- src/` is empty. |
| R6 | The shipped conductor requires verifying the archive before declaring the pipeline complete, by running the check rather than reading the role's report | intent.md SC6 (G3) | PASS | `templates/conductor/sdd-conductor.md` § "Conductor mechanics" ("Applies to `sdd-documentation` too: … the conductor confirms the archive landed by running the readiness runner's spec-state family itself … rather than accepting the role's own report that it did") and § "Closing the loop". |
| R7 | The live conductor names the documentation stage, carries the post-audit hard rule, lists `auditor → documentation`, and carries the archive verification | intent.md SC7 (G3) | PASS | `.claude/skills/sdd-conductor/SKILL.md` read in full: pipeline diagram ends `… → sdd-documentation (auto, non-gated)`; **five** numbered hard rules (new #4 = post-audit gate, matching the template's rule #4); `auditor → documentation` under "FLOW automatically"; archive verification in both "Conductor mechanics" and "Closing the loop". **This artifact is gitignored (`.gitignore:5 .claude/skills/*`) and untracked — it is covered by no commit.** See AL-7. |
| R8 | The conductor parity guard fails when the live file is present and incomplete, and skips when it is absent | intent.md SC8 (G3) | PASS | Proven with two throwaway probe copies of `tests/conductor-parity.test.ts` (both deleted afterwards): pointed at a nonexistent path, vitest reports `↓ … 1 skipped` — an observable skip, not a pass; pointed at a copy of the live conductor with `auditor → documentation` removed, it fails with `is present but missing: auditor → documentation listed under automatic flow`. Present-but-empty ⇒ fail is pinned by a fixture sub-test asserting all four elements missing. |
| R9 | `templates/roles/sdd-documentation.md` declares `cost_tier: mid`; the rationale no longer claims the role performs no verification | intent.md SC9 (G4) | PASS | Line 7 is `- cost_tier: mid`; the rationale is the contract's § Interfaces item 2 text verbatim; `no independent verification of its own` no longer appears in the file. |
| R10 | Each of the five generators resolves `sdd-documentation` to its own `mid` model id with no diff under `src/generators/` | intent.md SC10 (G4) | PASS | In the scaffolded target, `sdd-documentation` and `sdd-executor` (an existing `mid` role) resolve to the *same* id per generator: `sonnet` / `"Claude Sonnet 4.5"` / `"claude-4.6-sonnet"` / `"claude-sonnet-4.6"` / `"gpt-5.6-terra"`. `git status --porcelain -- src/` empty; `COST_TIERS` at `src/vocabulary.ts:22` still `['most-capable', 'mid', 'cheapest']`, and `claude-code.ts`'s `MODEL_BY_TIER` still maps `cheapest: 'haiku'` — the tier is unoccupied, not removed. |
| R11 | The role template's hand-off names all three sub-steps in order, matching the skill | intent.md SC11 (G5) | PASS | New § "Step 5: Hand Off to the Knowledge-Base Skills" names `harny-sync` archive → `harny-adr` → `harny-sync` capability/index update, in order. `harny-adr` went from 0 mentions to 1. Step 3 item 4 now stamps only. Matches `harny-document/SKILL.md` Step 3's sub-steps 1–3. |
| R12 | Both `harny-document/SKILL.md` copies remain equal except the declared `DIVERGENCE_TABLE` entry, which still holds | intent.md SC12 | PASS | `diff` of the two copies reports exactly one hunk, the pre-existing additive `templates/`-only sentence "…rather than assuming any prior history exists". `DIVERGENCE_TABLE` is unedited in `git diff -- tests/skills-fidelity.test.ts`. |
| R13 | Baseline preserved: new tests pass; `tests/packaging.test.ts` remains the single known pre-existing failure | intent.md SC13 | PASS | Full suite re-run by the auditor: **730 passed / 1 failed (731)**, the single failure being `tests/packaging.test.ts > adds no TOML parsing or serialization package…` (`vitest: '4.1.10'` vs `^4.1.11`). Neither `package.json` nor that test appears in `git status`. A detached worktree at `HEAD` runs **705 tests total**, matching the contract's declared 704 passing + 1 failing baseline; the feature adds 26. See AL-6 on the worktree's environment-only failures. |
| R14 | FC-13 holds after regeneration: `.sdd/`, `.claude/settings.json`, `.github/workflows/harny-feedback.yml` byte-identical to fresh `harny init` output | intent.md SC14 | PASS | `node bin/harness.js init <scratch> --tools claude-code --stack typescript --skills all --yes`, then `diff -rq <scratch>/.sdd .sdd` ⇒ **no output at all**; `.claude/settings.json` and `.github/workflows/harny-feedback.yml` both byte-identical. `.sdd/harness.json` records `"tier": "mid"` for `sdd-documentation`. |
| R15 | Scope held: no remedy applied to the other four roles; the generalization question carried as a reservation | intent.md § Non-Goals (G6) | PASS | `git status --porcelain -- templates/roles/` lists only `sdd-documentation.md`. No `--only`/tier change touches another role. The generalization question is carried as RC-R2 below, not acted on. `.gitignore` unchanged; the three human gates unchanged; no new check family or `checks.json` field. |

## Contract Compliance

| ID | Contract Item | Status | Verified By |
|---|---|---|---|
| C1 | RC-1 — selector is additive; `--only` absent reproduces today's five-family output byte-for-byte | PASS | Extracted the pre-feature runner with `git show HEAD:templates/doctor/run-doctor.mjs` and ran both old and new against the same synthetic repo and checks file with no `--only`: stdout **byte-identical** (`diff` clean) and both exit `2`. Note: this byte-for-byte clause has no automated test — see AL-1. |
| C2 | RC-2 — `--only spec-state` evaluates family 4 alone and spawns no child process | PASS | Direct run emits only the `FAIL spec-state:…` line + summary: no `node-version`, no `conventions-doc`/harness presence line, no `-- repo readiness --` label, no test-family line. Sentinel-writing `commands` entry never executed. `tests/doctor-runner-only.test.ts` asserts the same, including `expect(result.code).not.toBe(1)` so a rejected flag can't masquerade as "ran and skipped spawning". |
| C3 | RC-3 — exit taxonomy preserved (`0`/`2`/`1`), extended to unknown-value and missing-value cases | PASS | Every row of the Error Handling Contract table exercised by hand: unknown value ⇒ 1 naming value + accepted set; missing value ⇒ 1 naming the flag; repeated flag ⇒ last wins (proved with `--only spec-state --only tests`, which *did* spawn the sentinel); any other unknown flag ⇒ 1 with the message now extended to `… or --only <family>`; `specs/` absent ⇒ 0. `process.exit(failCount > 0 ? 2 : 0)` is unchanged. |
| C4 | RC-4 — determinism: identical state + args ⇒ byte-identical stdout and exit code | PASS | Two consecutive `--only spec-state` runs against a frozen repo produced identical stdout and exit code; `tests/doctor-runner-only.test.ts` folds the same assertion into its RC-2 test. No ordering, timing or environment dependence introduced — the `selected()` predicate reads a parsed argv value only. |
| C5 | RC-5 — family 4's condition reused, never duplicated; `reservedDirs` still excludes `current`/`archived` | PASS | Read the whole diff of `templates/doctor/run-doctor.mjs`: the family-4 block is re-indented inside `if (selected('spec-state')) { … }` with **no logic change** — the `shipped && approved` predicate, the `reservedDirs` filter, the schema-file check and both message strings are character-for-character the pre-feature text. No second detector exists anywhere. Confirmed behaviorally: a feature moved under `specs/archived/` contributes no line. |
| C6 | RC-6 — `src/` untouched; `src/doctor.ts` still spawns without a selector; `npx harny doctor` unchanged | PASS | `git status --porcelain -- src/` is empty. `tests/doctor.test.ts`'s new T10 replaces the runner with an argv-recording stub and asserts `argv` does not contain `--only` and `argv[0] === '--checks'`; T11 pins `checks.json`'s exact top-level and `specs` key sets. |
| C7 | RC-7 — `cost_tier: mid` with corrected `cost_rationale` | PASS | `templates/roles/sdd-documentation.md:7–8`, matching `contract.md` § Interfaces item 2 verbatim. |
| C8 | RC-8 — tier propagates to all five tools with no generator change; `COST_TIERS` unchanged; `cheapest` left unoccupied | PASS | See R10. Independently corroborated at the source level: `src/generators/claude-code.ts` `MODEL_BY_TIER` still contains all three tiers; the change in `tests/generators/claude-code.test.ts` is a consequence of `mid → 'sonnet'`, not a generator edit. |
| C9 | RC-9 — completion precondition present, with all four required elements, in the role template and both skill copies | PASS | All four § Interfaces item 3 elements read in context in all three files: (1) "Report … completion only after confirming … that **this feature** no longer appears as shipped-but-unarchived"; (2) generic name ("the spec-state family of the readiness runner shipped at `.sdd/doctor/run-doctor.mjs`") + attributed example ("invoked, for example, as `node … --only spec-state`"); (3) "A failing line for a different feature is reported as a finding, never silently ignored and never fixed in passing"; (4) "Describing the archive step, or handing it back as a 'next step', is not completing it." Judged on intent, not merely on the pinned substrings. |
| C10 | RC-10 — role-template ↔ skill parity for the three-sub-step hand-off; `harny-adr` named | PASS | See R11. The two artifacts a downstream user receives now agree on the tail. |
| C11 | RC-11 — text reaches all five generated `sdd-documentation` artifacts and is absent from the other four roles | PASS | `grep -rl 'only after confirming'` over the scaffolded target returns exactly the five `sdd-documentation` artifacts and nothing else. Each of the five also contains `--only spec-state`, `not completing it`, and `other feature's archive is not this invocation's business`. See AL-3 on the test's single-anchor weakness. |
| C12 | RC-12 — skill-copy fidelity under the declared divergence; `DIVERGENCE_TABLE` unedited | PASS | See R12. The new text is present identically in both copies, so it is neither `requiredInTemplate` nor `forbiddenInTemplate`; the pre-existing exhaustive-by-construction fidelity sweep in `tests/skills-fidelity.test.ts` passes. |
| C13 | RC-13 — the contract's stated limit is accurate and not overstated anywhere in shipped content | PASS | Grepped every changed shipped artifact for guarantee-language ("guarantees the archive", "always happens", "cannot be skipped", "ensures the archive", "makes it impossible"): the only hits are the pre-existing `SW-7` stamp-ordering sentence "This stamp always happens **in place**", which is about the stamp, not the archive. Nothing in the role template, either skill copy, either conductor, or the runner's header comment claims the archive is guaranteed. **RC-R1 remains open and is not closed by this audit** — the detection is deterministic and cheap; the invocation is still an agent instruction; the backstop is unchanged. |
| C14 | RC-14 — shipped conductor verifies the archive before declaring completion | PASS | See R6. Framed as an application of the existing "Verify, don't trust" mechanic and the "a role should not be trusted to certify its own gate" principle, exactly as contracted. |
| C15 | RC-15 — live conductor drift repaired (diagram, hard rule, automatic flow, verification) | PASS | See R7. Four-for-four. Dogfood-only; the shipped template was already correct, so no downstream user is affected. **Not covered by any commit** (gitignored, untracked). |
| C16 | RC-16 — parity guard's skip/fail semantics exactly as specified, including present-but-empty ⇒ fail and an observable skip | PASS | All four pinned behaviors verified: absence is detected by `fs.existsSync` (line 75), never a caught read error; absent ⇒ `context.skip()`, reported by vitest as `↓ … 1 skipped` (probe-proven); present-but-empty ⇒ `fail` with all four elements named (fixture sub-test, since `''.includes(needle)` is false for every needle); present-and-incomplete ⇒ `fail` naming the element (probe-proven). A CI skip is therefore visibly distinct from a pass, and the four fixture sub-tests still execute the guard's own logic in CI. |
| C17 | RC-17 — exactly three files regenerated; the named untouched set genuinely untouched | PASS | All three `run-doctor.mjs` copies share sha256 `aa32ac23…14`. `git status --porcelain` shows no entry for `.claude/settings.json`, `.github/workflows/harny-feedback.yml`, `.sdd/doctor/checks.json`, `.sdd/spec-schema/*`, `.sdd/feedback/*`, `.sdd/shared/probes.mjs`, `package.json`, `tests/packaging.test.ts`, or `tests/fixtures/templates/**`. |
| C18 | RC-18 — test baseline preserved | PASS | See R13. |
| C19 | A-PR2 — PR-2 amended not repealed: documentation clause → `mid`, other four clauses unchanged | PASS | `.sdd/harness.json` records `most-capable / mid / mid / most-capable / mid` — only the documentation entry changed. `COST_TIERS` and every generator's tier→model map retain `cheapest`. Recording the amendment in `specs/current/pipeline-roles.md` is the documentation role's ship-time job, not the executor's. |
| C20 | A-RD — runner interface extended; five families, their order, `checks.json` schema and exit taxonomy unchanged | PASS | `FAMILY_TOKENS` lists the families in `main`'s execution order; each of the five blocks is wrapped, none reordered. `checks.json` byte-unchanged and its key set pinned by T11. Exit taxonomy verified in C3. |
| C21 | `S4` — no runtime or dev dependency added; `package.json` unmodified | PASS | `package.json` absent from `git status`; no `import` of a non-builtin added to the runner (it still imports only `node:*` plus `../shared/probes.mjs`). |
| C22 | `S7`/`PR-7` — the precondition names the mechanism generically, with any single tool's invocation only as an attributed example | PASS | Both the role template and `templates/conductor/sdd-conductor.md` name "the readiness runner shipped at `.sdd/doctor/run-doctor.mjs`" first and give the concrete command as "invoked, for example, as …". The runner's path and bytes are identical across all five tools (verified by scaffolding `--tools all`), so no tool-specific mechanic is named as the only possibility. The live `.claude/skills/sdd-conductor/SKILL.md` states the command bare, which is correct: it is Claude-Code-specific content, not tool-neutral content. |

## Test Coverage

| ID | Test Description | Status | Test File |
|---|---|---|---|
| T1 | `--only` absent reproduces the full five-family run | PARTIAL | `tests/doctor-runner-only.test.ts` — asserts all four observable families still report, but **not** RC-1's byte-for-byte clause; the auditor verified that manually against `git show HEAD:…`. See AL-1. |
| T2 | `--only spec-state` emits only family-4 lines (no `node-version`, no presence lines, no family-3 label) | PASS | `tests/doctor-runner-only.test.ts` |
| T3 | `--only spec-state` spawns no child process — asserted via a sentinel a `commands` entry would have created | PASS | `tests/doctor-runner-only.test.ts` |
| T4 | Unknown `--only` value exits `1` and names the accepted set | PASS | `tests/doctor-runner-only.test.ts` — also asserts the message is *not* the generic "unknown flag" branch, so it cannot incidentally pass. |
| T5 | `--only` with no following value exits `1` | PASS | `tests/doctor-runner-only.test.ts` |
| T6 | Repeated `--only`: last occurrence wins | PASS | `tests/doctor-runner-only.test.ts` |
| T7 | Two identical runs produce byte-identical stdout and exit code | PASS | `tests/doctor-runner-only.test.ts` (folded into T2's test, as declared) |
| T8 | Stranded fixture ⇒ exit `2`, line names the feature; after the move ⇒ exit `0` | PASS | `tests/doctor-runner-only.test.ts` — plus a second case proving a *different* feature's stranded line is emitted while an unrelated non-stranded feature contributes none. |
| T9 | `specs/` absent or empty ⇒ exit `0`, distinct from the exit-`1` error path | PASS | `tests/doctor-runner-only.test.ts` — and a boundary test contrasting `--only harness` with zero `require` entries (exit 0) against an unknown token under the identical checks file (exit 1). |
| T10 | `src/doctor.ts` spawns with `['--checks', checksJson]` and no selector | PASS | `tests/doctor.test.ts` — behavioral (argv-recording stub), not a source grep. |
| T11 | `.sdd/doctor/checks.json` unchanged; `DoctorChecksFile` gained no field | PASS | `tests/doctor.test.ts` |
| T12 | Role template declares `cost_tier: mid`; the retired rationale claim is absent | PASS | `tests/canonical-fidelity.test.ts` |
| T13 | Each of the five generators resolves the role to its own `mid` model id | PASS | `tests/canonical-fidelity.test.ts` — compares against `sdd-executor`'s rendered id per generator rather than hardcoding literals, so it stays correct if a model id is bumped. |
| T14 | Role template names all three hand-off sub-steps in order, including `harny-adr` | PASS | `tests/canonical-fidelity.test.ts` — asserts ordering by index, not mere presence. |
| T15 | Precondition's four elements present in the role template | PASS | `tests/canonical-fidelity.test.ts` — see AL-2 on substring strength. |
| T16 | Precondition present in **both** `harny-document/SKILL.md` copies | PASS | `tests/skills-fidelity.test.ts` |
| T17 | Precondition reaches all five generated artifacts and is absent from the other four roles | PARTIAL | `tests/canonical-fidelity.test.ts` — asserts one anchor substring (`not completing it`) rather than SC5's "verbatim". The auditor independently verified all four elements in all five scaffolded artifacts. See AL-3. |
| T18 | `DIVERGENCE_TABLE`'s `'harny-document'` entry unedited and its declared divergence still holds | PASS | `tests/skills-fidelity.test.ts` (pre-existing exhaustive sweep, confirmed green; `DIVERGENCE_TABLE` confirmed unedited in the diff) |
| T19 | Shipped conductor requires archive verification before declaring completion | PASS | `tests/canonical-fidelity.test.ts` |
| T20 | Parity guard fails when the live conductor is present and missing an element, naming it | PASS | `tests/conductor-parity.test.ts` — probe-proven by the auditor against a degraded copy. |
| T21 | Parity guard skips, observably, when the live conductor is absent | PASS | `tests/conductor-parity.test.ts` — probe-proven: vitest reports `↓` and `1 skipped`. |
| T22 | Parity guard fails when the live conductor is present but empty | PASS | `tests/conductor-parity.test.ts` |
| T23 | The three `run-doctor.mjs` copies are byte-identical | PASS | `tests/canonical-fidelity.test.ts` — corroborated by sha256. |
| T24 | FC-13: `.sdd/` byte-identical to fresh init output, including `.sdd/harness.json`'s `mid` tier | PASS (manual, by design) | — — verified by the auditor via `diff -rq` against a fresh `bin/harness.js init --tools claude-code --stack typescript --skills all --yes`; no automated test, as the architect scoped this to audit time. Carried forward as reservation RC-R7. |

## Audit Log

| Date | Auditor | Finding | Severity | Resolution |
|---|---|---|---|---|
| 2026-09-23 | sdd-auditor | **AL-1 — RC-1's byte-for-byte clause is untested.** `T1` asserts that the four observable families still report under `--only` absent, but not that stdout is byte-identical to the pre-feature runner. The auditor verified it manually (`git show HEAD:templates/doctor/run-doctor.mjs`, same synthetic repo, `diff` clean). A future refactor of a family block could change spacing undetected. | LOW | Optional: extend `tests/doctor-runner-only.test.ts` to snapshot default-run stdout. Not blocking — the guarantee holds today, verified. |
| 2026-09-23 | sdd-auditor | **AL-2 — the acceptance substrings pinned for RC-9 are loose.** Element 1 is pinned as `['only after', 'spec-state']` and element 3 as `['different feature', 'finding']`; those needles could be satisfied by unrelated sentences elsewhere in the file. The auditor read the shipped wording in context in all three files and judged it to satisfy each element's *intent*, not merely the pins. | LOW | Optional: tighten to longer phrases (e.g. `only after confirming`). The shipped text is correct today; this is a durability concern for future edits. |
| 2026-09-23 | sdd-auditor | **AL-3 — RC-11's generated-artifact assertion uses a single anchor.** `T17` pins only `not completing it`, weaker than SC5's "carries the completion-precondition text verbatim". The auditor independently scaffolded `--tools all` and confirmed **all four** elements plus `harny-adr` in all five artifacts, and confirmed no leakage into the other four roles. | LOW | Optional: assert the full element list against the generated output rather than one anchor. |
| 2026-09-23 | sdd-auditor | **AL-4 — `S6` violation: contract ids appear in test names** (e.g. `… (RC-16)`, `… (RC-7; intent SC9) (T12)`). Checked under `harny-standards` (S1–S7). This is pre-declared in `intent.md` § Constraints, which cites AL-8/AL-P8 and states the feature follows the observed convention rather than reconciling it. S1, S2, S3 (determinism verified; single trailing newline confirmed on every modified file), S4, S5 and S7 all pass. | LOW | Accepted as pre-declared. Reconciling the suite-wide convention remains a separate feature's job. |
| 2026-09-23 | sdd-auditor | **AL-5 — a misconfigured `specs.dir` yields a green `--only spec-state`.** Family 4 swallows the `readdirSync` failure into `featureNames = []`, so a wrong `specs.dir` exits `0` with output indistinguishable from a genuinely clean repo. This is pre-existing family-4 behavior that `RC-5` deliberately reuses, and `contract.md`'s Error Handling Contract explicitly sanctions the `specs/` absent ⇒ exit `0` row. Noted because it is the one remaining "green having evaluated nothing" shape the selector does not close. Running from the wrong cwd is caught loudly (missing `checks.json` ⇒ exit 1). | LOW | Recommendation only; out of this feature's scope (`intent.md` § Non-Goals forbids new checks/fields). |
| 2026-09-23 | sdd-auditor | **AL-6 — CI has not observed this feature.** Per `harny-feedback` verification: the generated workflow `.github/workflows/harny-feedback.yml` is present and its latest run (`35868822301`, push, `main`) is **green**, with the runner's trailing summary `harny-feedback: 1 of 2 command(s) ran, 1 skipped.` — N = 1, so not the "green having run nothing" gap. But that run is for `HEAD = d3c2741`, which **predates this feature's changes**; nothing is committed, per instruction. The per-turn hook is wired in `.claude/settings.json` (`PostToolUse` accumulate + `Stop` run) and `.sdd/feedback/.turns/` was touched during this session, but the evidence that it fired on every implementation turn is indirect (turn state is consumed and cleared). `npm run build` and `npm run typecheck` both exit 0. The pre-existing permanent `eslint` skip (no eslint config in this repo) is a `ci-workflow-root` reservation, out of scope. | LOW | Push the branch so CI evaluates this feature before merge. Not blocking at this gate, since committing was explicitly out of scope. |
| 2026-09-23 | sdd-auditor | **AL-7 — the two self-reported File Change Map deviations are genuinely mechanical and weaken no guard.** (a) `tests/canonical-fidelity.test.ts`'s non-mutation guard allowlists `templates/conductor/sdd-conductor.md` by **exact relative path** — the narrowest form available, matching the pre-existing single-file allowance for `templates/roles/sdd-documentation.md`, and narrower than the seven `startsWith` prefix entries already present. `RC-14`'s content is separately pinned by `T19`, so the file is not left unguarded. (b) `tests/generators/claude-code.test.ts`'s `expectedModelByRole` moved `sdd-documentation` from `'haiku'` to `'sonnet'`; that test compares frontmatter *keys* against the live oracle and the model *value* against the table, so the assertion retains identical strength (still an exact model id) and the live `.claude/agents/sdd-documentation.md` is correctly left at `model: haiku` per `contract.md` § Integration Points. Both were passing before this feature and neither is a red-phase deliverable. | LOW | Accepted. Recording here because `roadmap.md`'s File Change Map named neither file. |
| 2026-09-23 | sdd-auditor | **AL-8 — `.claude/skills/sdd-conductor/SKILL.md` (RC-15) is covered by no commit.** `git check-ignore -v` confirms `.gitignore:5 .claude/skills/*`; `git status --ignored` reports the directory as ignored. The repair is real and complete (all four elements verified by reading the file), but it exists only in this working copy. This is the exact situation RC-R4 describes and `ADR 0037` is reserved for. | MEDIUM (design, deliberate) | Accepted as designed; carried forward as RC-R4. No action at this gate. |
| 2026-09-23 | sdd-auditor | **AL-9 — ADRs 0035–0037 are not yet written.** `specs/archived/*/decisions/` contains no 0035–0037. Expected: the documentation role writes them after this gate, into `specs/archived/documentation-role-completion/decisions/`. Not a finding against the implementation. | N/A (pending) | Documentation role's ship-time deliverable. |
| 2026-09-23 | sdd-auditor | **AL-10 — audit method note.** Two throwaway probe test files (`tests/AUDIT-PROBE-conductor-absent.test.ts`, `tests/AUDIT-PROBE-conductor-degraded.test.ts`) were planted to prove RC-16's skip is observable and its fail really fires, and a detached `git worktree` at `HEAD` was created to corroborate the 705-test baseline. **All three were deleted afterwards**; `ls tests/AUDIT-PROBE*` returns no matches and `git worktree prune` was run. No commit was made. | N/A (method) | Cleaned up. |

## Final Verdict

**Status**: APPROVED WITH RESERVATIONS

**Summary**: Every contract guarantee `RC-1`–`RC-18`, both amendments (`A-PR2`, `A-RD`)
and all fifteen intent requirements verify PASS on independent re-derivation — the
family selector behaves exactly as specified including all nine Error Handling
Contract rows, the completion precondition reaches **all five tools' generated
artifacts** (not just `templates/`), FC-13 byte-identity is exact, and the live
conductor's drift is repaired. The reservations are the six the architect pre-seeded
plus one added here; none is a defect in the implementation, and `RC-R1` (the residual
tail-drop risk) is explicitly **not** closed by this audit.

**Critical Issues** (must fix before merge):

- None.

**Warnings** (should fix, not blocking):

- **AL-6** — CI has not run against this feature's changes; the green run on record is
  for `HEAD = d3c2741`. Push before merge so the workflow evaluates the new tree.

**Recommendations** (nice to have):

- **AL-1** — add a byte-for-byte stdout assertion for the `--only`-absent default run,
  so `RC-1`'s strongest clause is machine-checked rather than audit-checked.
- **AL-2** — tighten the `RC-9` acceptance substrings (e.g. `only after confirming`
  instead of `only after`) so a future prose edit cannot satisfy the pins while losing
  the intent.
- **AL-3** — assert the full four-element list against each generator's rendered
  `sdd-documentation` artifact, matching SC5's "verbatim" wording.
- **AL-5** — consider, in a later readiness feature, distinguishing "no `specs/`
  directory" from "`specs.dir` misconfigured"; today both exit `0`.

### Pre-registered reservations (confirmed by the auditor)

Each was checked against the shipped state and is still accurately described. The open
ones must be carried forward to `specs/current/` rather than closed silently.

| ID | Reservation | Severity | Status after audit |
|---|---|---|---|
| RC-R1 | **The residual tail-drop risk is not closed.** L1's judgment is deterministic but its invocation is an agent instruction; an agent that drops the hand-off can drop the verification step after it. The unchanged backstop is `harny-doctor`'s next session-start run. | MEDIUM (design, deliberate) | **CONFIRMED OPEN.** Verified that nothing in the shipped role template, either `harny-document` copy, either conductor, or the runner's header comment overstates this limit (see C13). This audit does **not** report the risk as eliminated. |
| RC-R2 | **Whether the tail-drop risk generalizes to the other four roles is unexamined.** | LOW (scope, deliberate) | **CONFIRMED OPEN.** No remedy leaked to the other four roles (R15). |
| RC-R3 | **L2's effect is unmeasurable from inside this feature.** | MEDIUM (human-gated) | **CONFIRMED OPEN.** `cost_tier: mid` is verified to propagate; whether it lowers the drop rate needs several post-ship runs. |
| RC-R4 | **The live conductor remains untracked.** The `RC-16` guard detects drift only where the file exists. | LOW (design, deliberate) | **CONFIRMED OPEN**, and upgraded in visibility as AL-8: the `RC-15` repair is covered by no commit. |
| RC-R5 | **`AGENTS.md`'s "six-step hand-off ordering" prose goes stale** against `RC-10`'s shape. | LOW (documentation) | **CONFIRMED OPEN.** `AGENTS.md:177–178` still says "six-step hand-off ordering"; the role template now has eight steps with a three-sub-step hand-off. Correctly left to the documentation role. |
| RC-R6 | **`tests/packaging.test.ts` pre-existing failure** stays failing. | LOW (pre-existing, out of scope) | **CONFIRMED OPEN.** It is the sole failure in a 731-test run; neither it nor `package.json` was touched. |
| RC-R7 | **FC-13 (T24) has no automated test.** Byte-identity against fresh `harny init` output was verified manually at audit time (and matched exactly), but nothing in the suite will catch a future drift. | LOW (coverage) | **NEW, OPEN.** Added by the auditor; consider a generated-output fidelity test in a later feature. |

### Note on this feature's own ship

The auditor is aware, and states explicitly, that **this feature's archive will be the
first live exercise of the mechanism it builds.** If the documentation role drops the
archive step while shipping `documentation-role-completion`, that must be recorded here
as an audit-log finding of the highest relevance — **not quietly re-run**. Conversely, a
clean archive on the first attempt is weak evidence at n = 1 and must be recorded as
such, not as confirmation that `RC-R1` is closed. The role's own new Step 6 names the
command to run: `node .sdd/doctor/run-doctor.mjs --only spec-state`.
